# Metrics

Header: date 2026-09-19, commit ea81d98f merged with origin/main at e2d86a3a (branch point 843d8261), scope apps/platform/src, apps/platform/db, packages/{config,content,db,domain,infrastructure,test-support,ui}/src plus the enforcement layer (tools/dependency-cruiser.config.cjs, tools/dependency-cruiser.tsconfig.json, tools/boundaries.test.mjs, tools/boundary-fixtures/, tools/domain-layout.mjs, tools/domain-layout.test.mjs, tools/domain-layout-fixtures/, knip.json, eslint.config.mjs, workspace package.json export maps, tsconfigs, vite/react-router/vitest configs), mode record update merging two lines of history on top of change review run 8 at e8690f45: main's partial change review of 8ac6a613 (PR #229 head, squash-merged as 7d92dc22; base 79fa1e95; 46 changed implementation files in C1, C6, C7, C8 and C14 plus direct neighbors), the persisted waitlist-mode change (74950bd7), the Radix mobile navigation (1979b1cc) and the database-enforced waitlist capacity (e2d86a3a); and GEN-191 over `843d8261..ea81d98f` — the slices recorded at 422c7797, corrected after the two review fix rounds (`422c7797..186b77c4`), the remediation round (`186b77c4..6fc3157f`), the booking-card rebuild (`bcf6555e..dde3b960`), the second parity round (`7a61673f..d3513898`), the design-system fix round on top of bff79348, the coach-calendar refactor (`19169b9e..eb88ecb4`, carried onto this branch as 152ca98f and recorded at 47edb229), the fix round (`47edb229..59c019ec`) and the final fix round (`a4256c45..8a604aef`) — and the merge resolution, which moves the assessment-call slice onto main's contracts and database feature flags. Definitions and reading guide: the inspection workflow's metrics reference. Computed because the graph has sixteen components.

Change review: date 2026-09-20, commit 05e46534 (GEN-192, base f46b6f41: `f46b6f41..05e46534`), mode record update, scope "Let the coach set her assessment call hours and meeting room"; full scope. It recomputed C1, C2, C5, C6, C9 and C17 from a fresh cruise; volatility columns are untouched.

Change review: date 2026-09-18, commit 8ac6a613 (PR #229 head, squash-merged to main as 7d92dc22; base 79fa1e95), scope 46 changed implementation files in C1, C6, C7, C8 and C14 plus direct neighbors, mode partial change review (run 8 baseline e8690f45). It recomputed C1, C6, C7, C8 and C14 and added `+ 1` to their volatility.

Change review: date 2026-09-18, commit c2277ebb, baseline 7d92dc22, scope the persisted waitlist-mode change (`79fa1e95..f0eb1bf4`, merged with main in `242a0976`): the changed units and their direct graph neighborhood in apps/platform, packages/{config,content,db,domain,infrastructure,ui}, tests, migrations and package deployment; partial scope, recorded under "Persisted waitlist-mode change review" below.

Change review: date 2026-09-18, commits 6c043f97, 63100724, eced8490 and 3836745e (PR #230, base 843d8261), merged with main at 74950bd7; scope the C5 mobile-navigation modules (`layout/navigation-dialog.tsx`, `layout/use-close-mobile-navigation-on-desktop.ts`, `layout/portal-shell.tsx`, the removed `lib/focus-trap.ts`) and their public-site and portal consumers; partial scope. It recomputes C5 and C11 and adds the "Mobile navigation" volatility column.

Counting: fan-in is the number of modules outside the component that import at least one module inside it; fan-out is the number of modules inside that import at least one module in another in-scope component (externals excluded). Abstract types are port interfaces that exist only to be implemented; total types are exported classes, interfaces, and type aliases. Previous distance is the run-8 value at e8690f45 for every row. Generated from a cold cruise of the import graph of the working tree that merges main (e2d86a3a) into the GEN-191 branch at ea81d98f, before GEN-192: 378 modules, 1016 dependencies, 0 violations, 0 circular, over 355 in-scope production modules; GEN-192's own fresh cruise reads 399 modules, 377 in-scope production modules, 1084 dependencies, 0 violations, 0 circular; the per-module edge table is in `dependencies.md`. Volatility keeps run 8's count of the 84 commits in `148d594f..e8690f45` that changed a non-test file in the component, with the 16 commits of the domain-model restructure (`2173cbfb..e8690f45`) in the "of which run 8" column, and adds PR #229 (`7d92dc22`) as `+ 1` to each component it changed. The "Waitlist mode" column counts the waitlist-mode commits that changed a non-test file or package manifest in the component: the seven in `79fa1e95..f0eb1bf4` plus the main merge `242a0976`. The "Mobile navigation" column counts PR #230's commits. The "GEN-191 commits" column counts the seventeen commits in `843d8261..6fc3157f`. No column counts PR #232 (`e2d86a3a`), the later GEN-191 rounds or the merge of main into GEN-191; the next full architecture review owns the recount.

**What the merge recounted.** Fan-in, fan-out, instability, abstractness and distance are recomputed from the merged cruise above, so every number in those five columns is current; the volatility columns are carried from both sides unchanged. Against the branch's record at 8a604aef, the merge moved five components. **C1's fan-in fell 62 → 55**: main moved `BotVerifier`, `ProductEmail` and `ManagementAuthenticator` into C6, so eight C6 modules stopped importing `/shared`, while PR #232 (`e2d86a3a`) added `data/schema.server.ts` (C8) as an importer; against the 41 main recorded for PR #231 (`74950bd7`), the twelve C17 importers, `PostgresCoachCalendar` and that schema module account for the rise. Its abstractness holds at 0.23 (24 / 104): the four shared ports that left (`Logger`, `BotVerifier`, `ProductEmail`, `ManagementAuthenticator`) are offset by the three incident interfaces (`AcquisitionIncidents`, `WaitlistIncidents`, `AssessmentCallIncidents`) and the five GEN-191 ports; `AssessmentCallBookingWindow` is a concrete class, and `WAITLIST_MODE_FEATURE_FLAG` a constant. D 0.77, where main read 0.78 (0.775 rounded up) and the branch 0.77. **C2's fan-in fell 22 → 21** because main's `platform-composition.server.ts` no longer imports `DatabaseClient`. **C5's published types rose 4 → 5** with main's `NavigationMenu`; its fan-in holds at 40 and it stays at D 1.00. **C6 moved 0.73 → 0.71** (main read 0.70, run 8 0.72), by less than the 0.10 threshold: its fan-in rose 44 → 51 as the modules that named the three contracts through C1 `/shared` now name them in C6 (main's 39 plus C17's twelve importers), its fan-out fell 16 → 11 (main's 8 plus the three coach-calendar modules), and its abstractness rose 0.00 → 0.12 (3 / 26) with the three concern contracts; I = 11 / 62 = 0.18. **C14's total types fell 14 → 13** as main made `PlatformFeatureHandles` module-private, and C8's 12 → 11 with `WaitlistFeatureHandles`; neither moves A off zero. No component crossed the 0.10 threshold and none was reclassified.

**What the branch's record updates recounted.** On the branch, the GEN-191 column counts the seventeen commits in `843d8261..6fc3157f` rather than extending the window, and does not count the twelve commits in `6fc3157f..bcf6555e`, the four of the booking-card rebuild and its record (`bcf6555e..7a61673f`), or the second parity round (`7a61673f..d3513898`). Six of the seventeen touch no component: `54a4a29b`, `a54d0ece` and `c6122c63` change only the prototype, and `422c7797`, `eb1fb8a3` and `186b77c4` only this record. The migration files under `apps/platform/db/drizzle/` are the shared migration journal, not C15, so `d26c0925` counts for C17 alone. A single feature's window is too short to read as volatility; the next full architecture review owns the recount. The GEN-191 fix round's six commits (`47edb229..59c019ec`, two of them prototype-only) are not counted either. That round moved no fan-in, fan-out, instability or distance: its three new modules (`packages/db/src/database-error.ts`, `surfaces/public-site/shell/header-appearance.ts`, `ui/public/book/book-page.css`) are imported only from inside their own components, and every module that gained or lost a C5 subpath (`auth-nav-actions.tsx` and `catalog-view.tsx` now also import `./primitives`; `access-denied-page.tsx`, `booking-confirmation.tsx`, `root-error-page.tsx` and `pricing.tsx` no longer import `./lib`) still imports C5. It added two published types, `DatabaseTransaction` to C2 (0 / 1 → 0 / 2) and `PublicHeaderAppearance` to C11 (0 / 12 → 0 / 13), neither abstract, so neither A moved; `BusyInterval` became `TimeInterval` without changing C1's count. The final fix round (`a4256c45..8a604aef`, two of its four commits prototype-only) is not counted in the volatility columns either. It moved one fan-in: C5 39 → 40, because `call-overview.tsx` (C17) now builds its card from C5's `cardVariants`; C17 already imported other components from that module, so its fan-out held at 26, and C5 stays at I 0.00 and D 1.00. `cardVariants` is a function, so C5's published type count holds at 4.

Change review: date 2026-09-20, commits `f46b6f41..HEAD` on `eduardvalentin1996/gen-193-coach-sees-her-assessment-calls-on-the-dashboard`; scope the GEN-193 coach assessment-calls slice; partial scope. The C5, C9 and C17 cells below carried its figures, read from a cold cruise at that branch's HEAD; superseded by the rebase recount below, which recomputes every cell this section and the GEN-192 recut touched from one fresh cruise of the merged tree.

**GEN-192 recut (`f46b6f41..05e46534`, 2026-09-19).** A fresh cold cruise: `pnpm exec depcruise --config tools/dependency-cruiser.config.cjs --output-type json apps/platform/src packages/config/src packages/content/src packages/db/src packages/domain/src packages/infrastructure/src packages/test-support/src packages/ui/src` reads 399 modules and 1084 dependencies, 0 violations, over the same in-scope production filter `dependencies.md` documents (test files excluded from fan-in/fan-out by the same convention `units.md` uses for the outermost ring). Against the pre-GEN-192 cruise (378 modules, 1016 dependencies), the branch added 21 modules and 68 dependencies. Six components move: **C1**'s fan-in rises 55 → 57 (`api/settings/assessment-call-settings-controller.server.ts` reads `/assessment-call`; `contracts/assessment-call-settings.ts` replaces the deleted `StaticCoachAvailability` as `/coach-availability`'s sole C17 reader, a net wash on module count but a different importer), and its abstract/total types rise 24/104 → 26/115: `CoachAvailabilityChanges`, `CoachMeetingRoomSource` and `CoachMeetingRoomChanges` arrive abstract (+3), `MeetingRoomLink` leaves (-1), and nine further concrete types arrive (`CoachAvailabilityResult`, `CoachAvailabilityProblem`, `CoachMeetingRoom`, `CoachMeetingRoomResult`, `AssessmentCallSettingsSnapshot`, `AssessmentCallSettingsProblem`, `GetAssessmentCallSettingsUseCase`, `UpdateAssessmentCallSettingsUseCase`, `UpdateAssessmentCallSettingsResult`); A holds at 0.23 rounded, I stays 0.00 (C1 still has no in-scope fan-out), D holds at 0.77. **C2**'s fan-in rises 21 → 24: `PostgresCoachAvailability`, `PostgresCoachMeetingRoom` and the new `coach-meeting-room/schema.server.ts` each name `DatabaseClient`/`appSchema` for the first time; C2 has no in-scope fan-out and no published type, so I, A and D hold at 0.00/0.00/1.00. **C5**'s fan-in rises 40 → 43 (three new C17 consumers: `assessment-call-settings-section.tsx`, `settings-page.tsx` and `join-page.tsx`, the last a first-time importer since the remediation round removed the join page's own render); no new type or interface is published (`CheckboxChip`, `Select` and `Toaster` are components and a function, not types), so A and D hold at 0.00 and 1.00. **C6** moves 0.72 (run 8) → 0.71 (the merge) → **0.68**: fan-in rises 51 → 52 (`api/settings/settings.ts` joins as a new route-module consumer of `./http/server`), fan-out rises 11 → 14 (`postgres-coach-availability.server.ts` and `postgres-coach-meeting-room.server.ts` each cross into C1 and C2; `coach-meeting-room/schema.server.ts` crosses into C2), and two new concrete published types (`PostgresCoachAvailability`, `PostgresCoachMeetingRoom`) join the three existing concern contracts, so A falls 0.12 (3/26) → 0.11 (3/28) while I rises 0.18 → 0.21; the combined move is 0.03, under the 0.10 review threshold. **C9** moves 0.50 → **0.53**, by 0.03: two new C17 modules read it for the first time (`api/settings/assessment-call-settings-controller.server.ts` reads `require-account.server`; `contracts/paths.ts` reads `contracts/paths.ts`), raising fan-in 15 → 17 with fan-out and total types unchanged, so I falls 0.50 → 0.47 and D rises the complementary amount. **C17** moves 0.21 → **0.23** (recorded to two decimals as 0.212 → 0.225 unrounded): fan-in rises 7 → 9, a 29% rise (the coach portal's `routes.ts` and `navigation-links.tsx`, its first imports of this feature), and fan-out rises 26 → 31, a 19% rise: seven modules become newly outward — the five new files under `api/settings/` and `ui/coach/settings/` plus two pre-existing modules gaining their first outward edge (`contracts/paths.ts`, into the accounts feature's `contracts/paths.ts`; `join-page.tsx`, into C5) — against two that stop being outward when GEN-192 deletes them (`data/static-coach-availability.server.ts`, `data/configured-meeting-room-link.server.ts`): 26 + 7 − 2 = 31. Because fan-in (the denominator's other term) grew proportionally faster than fan-out (the numerator), I = fanOut/(fanIn+fanOut) *falls*, 0.788 → 0.775, and D rises by the same amount since A holds at 0.00 — the inward growth this round outweighs the outward growth, moving C17 fractionally toward the main sequence on the instability axis even as D itself rises. Two new published contract types (`AssessmentCallSettings`, `AssessmentCallSettingsErrorCode`) join the existing 23, plus one more outside `contracts/` (`FieldValidationIssue`, exported from the new `api/resolve-field-error-code.ts`); `StaticCoachAvailability` and `ConfiguredMeetingRoomLink`, the two classes GEN-192 deletes, lived in the feature's private `data/` folder and were never part of this published-type count, so their removal does not offset the addition — the count simply gains three, 23 → 26, and A holds at 0.00. C13 is not recut: both its C17-importing modules (`routes.ts`, `navigation-links.tsx`) already imported C9, so by this file's own counting rule (distinct importing modules, not edges) its fan-out holds at 5, the established precedent for a module gaining a second outward subpath without gaining a first. No component crosses the 0.10 threshold on its own from this recut beyond C6's near-miss noted above, and none is reclassified; C6 remains the one watched screen, now marginally closer to the main sequence than the merge left it.

Change review: date 2026-09-20, commits `a5615696..HEAD` on `claude/coach-portal-followups`; scope the ad-hoc coach portal follow-up set; partial scope. The C5 and C17 cells below carry its figures, read from a cold cruise at HEAD; every other cell is carried forward, and the volatility columns count none of its commits.

Change review: date 2026-09-20, commits `a5615696..HEAD` on `claude/coach-portal-followups`; scope the ad-hoc coach portal follow-up set; partial scope. The C5 and C17 cells below carry its figures, read from a cold cruise at HEAD; every other cell is carried forward, and the volatility columns count none of its commits.

| Component | Fan-in | Fan-out | Instability | Abstractness | Distance | Previous distance | Volatility (run-8 window) | of which run 8 | Waitlist mode | Mobile navigation | GEN-191 commits |
|---|---|---|---|---|---|---|---|---|---|---|---|
| C1 packages/domain | 58 | 0 | 0.00 | 0.23 (26 / 115) | 0.77 | 0.77 | 25 + 1 | 12 | 6 | 0 | 4 |
| C2 packages/db | 24 | 0 | 0.00 | 0.00 (0 / 2) | 1.00 | 1.00 | 0 | 0 | 0 | 0 | 0 |
| C3 packages/config | 29 | 0 | 0.00 | 0.00 (0 / 13) | 1.00 | 1.00 | 5 | 0 | 2 | 0 | 2 |
| C4 packages/content | 11 | 0 | 0.00 | 0.00 (0 / 7) | 1.00 | 1.00 | 2 | 0 | 1 | 0 | 0 |
| C5 packages/ui | 49 | 0 | 0.00 | 0.00 (0 / 5) | 1.00 | 1.00 | 3 | 0 | 1 | 3 | 6 |
| C6 packages/infrastructure | 52 | 14 | 0.21 | 0.11 (3 / 28) | 0.68 | 0.72 | 10 + 1 | 1 | 1 | 0 | 2 |
| C7 features/store | 10 | 37 | 0.79 | 0.00 (0 / 37) | 0.21 | 0.21 | 29 + 1 | 6 | 0 | 0 | 0 |
| C8 features/waitlist | 10 | 14 | 0.58 | 0.00 (0 / 11) | 0.42 | 0.42 | 16 + 1 | 1 | 4 | 0 | 0 |
| C9 features/accounts | 17 | 15 | 0.47 | 0.00 (0 / 10) | 0.53 | 0.50 | 11 | 1 | 0 | 0 | 0 |
| C11 surfaces/public-site | 1 | 22 | 0.96 | 0.00 (0 / 13) | 0.04 | 0.04 | 12 | 1 | 0 | 3 | 2 |
| C12 surfaces/client-portal | 1 | 6 | 0.86 | undefined (0 types) | 0.14 | 0.14 | 3 | 0 | 0 | 0 | 0 |
| C13 surfaces/coach-portal | 1 | 5 | 0.83 | undefined (0 types) | 0.17 | 0.17 | 3 | 0 | 0 | 0 | 0 |
| C14 apps/platform/src/server | 3 | 11 | 0.79 | 0.00 (0 / 13) | 0.21 | 0.21 | 11 + 1 | 1 | 3 | 0 | 2 |
| C15 app root | 0 | 4 | 1.00 | undefined (0 types) | 0.00 | 0.00 | 7 | 0 | 2 | 0 | 1 |
| C16 packages/test-support | 0 | 0 | undefined | undefined (0 types) | undefined | undefined | 3 | 0 | 0 | 0 | 0 |
| C17 features/assessment-calls | 10 | 39 | 0.80 | 0.00 (0 / 26) | 0.20 | 0.21 | n/a | n/a | n/a | n/a | 8 |

## GEN-193 coach assessment calls

A cold cruise of the working tree at GEN-193's HEAD (before this rebase) read 399 modules and 1088 dependencies, 0 violations and 0 circular, over 373 in-scope production modules. Three components moved, none by the 0.10 review threshold and none reclassified.

**C5 fan-in rose 40 to 44** and its fan-out stayed 0, so instability stayed 0.00 and D stayed 1.00: `coach-portal/pages/home.tsx` stopped importing `./layout` when the placeholder shell went, and five C17 modules arrived (the coach section, the pager, the widget, the join link and `use-coach-clock.ts`). The two new subpaths publish values only — `Badge`, the seven pagination parts, the four tabs parts and the two appointment composites — and `appointment.ts`'s four types stayed package-private, so the published type count and abstractness were unchanged. C5 is concrete by design; every consumer that arrives pushes it no further, because it already sits at the far end.

**C17 moved 0.21 to 0.23**, closer to the main sequence. Its fan-in rose 7 to 10 (the coach surface's page, route registry and navigation links), and its fan-out 26 to 33 (the coach controller, `contracts/paths.ts` now naming C9's portal path, and the five coach UI modules that reach C5). Abstractness stayed 0.00: the coach contract and the listing module publish inferred wire types and view types, not ports.

**C13 held at 0.17.** Its fan-out was still five distinct modules — the page, the route registry, the two shell modules and the navigation links — because the page swapped one outward import for another, and its fan-in was still one. **C9 fan-in rose 15 to 16**, because C17's `contracts/paths.ts` built the coach page's path on `COACH_PORTAL_PATH`; nothing else about C9 moved.

No component entered the zone of uselessness: the slice published no new abstract type, and every C5 export it added had a named consumer, which `knip` proves on every `pnpm typecheck`. Mean D and its standard deviation were unchanged to two places, and the one-standard-deviation set was the same.

Superseded by the rebase recount below, which folds this slice's figures into the same fresh cruise that recomputes GEN-192's own recut.

**Rebase recount (`origin/main` at `a5615696`, 2026-09-20).** A fresh cold cruise of the rebased tree (`pnpm exec depcruise --config tools/dependency-cruiser.config.cjs --output-type json apps/platform/src packages/config/src packages/content/src packages/db/src packages/domain/src packages/infrastructure/src packages/test-support/src packages/ui/src`) reads 423 modules and 1156 dependencies, 0 violations, over the same in-scope production filter. It supersedes both the GEN-192 recut and the GEN-193 section above for every cell either one touched, and also folds in the remediation round's toaster move and the coach-controller's `api/coach/` regrouping (neither moves a fan-in/fan-out count, since both are renames within C5 and C17 respectively). Six components carry updated cells: **C1** fan-in 57 → 58 (GEN-193's `ListAssessmentCallsUseCase`/`AssessmentCallListing`/`AssessmentCall.hasEnded`/`AssessmentCallReservations.listAll` add one net importer beyond GEN-192's own recut), A and D unchanged. **C5** fan-in 43/44 (GEN-192's and GEN-193's separately recorded figures) settle at 48 in the merged tree: the two slices' new C17 consumers do not overlap, so both additions land; fan-out, A and D hold at 0/0.00/1.00. **C6** holds exactly at GEN-192's own recut (52/14/0.21/0.11 (3/28)/0.68), unmoved by GEN-193, which never touches infrastructure. **C9** settles at 17 fan-in (GEN-192's 17 and GEN-193's 16 both describe real edges into C9 that coexist in the merged tree, and 17 is the fan-in GEN-192's own recut already counted, so no further move), fan-out 15, I 0.47, D 0.53. **C13** holds at 1/5/0.17, unmoved by either slice once merged (its two C17-importing modules already imported C9, so gaining a further C17 fragment moves no distinct-module count). **C17** settles at fan-in 10, fan-out 38 (fan-out is the sum of GEN-192's 31 and GEN-193's coach-listing outward edges once both slices' new files are counted together, since they touch disjoint files), I 0.79, A 0.00 (0 / 26), D 0.21 — back near its run-8 baseline rather than the 0.225 either slice reached alone, because the two slices' fan-in growth (GEN-192's settings routes, GEN-193's coach-listing routes) compounds faster than their fan-out growth once both are present together. No component crosses the 0.10 threshold from this recount and none is reclassified. Mean D holds at 0.49 and its population standard deviation at 0.37 over the same fifteen components with a defined D, the same band (0.12 to 0.86) and the same outlier set (C2, C3, C4, C5 above it; C11, C15 below it; C12 at 0.14 the nearest edge, still inside).

**C10 features/coaching-bundles is gone.** The feature folder and the `packages/domain/src/coaching-bundles` slice were both deleted; the three bundle literals, the benefits list and the presenter are one C11-private module in `surfaces/public-site/sections/pricing/` (decision D5). Its three edges (`C11 → C10`, `C10 → C1`, `C10 → C5`) left the graph with it.

Mean D 0.49, standard deviation 0.37 (population), over the fifteen components with a defined D; the band is 0.12 to 0.86. Main read 0.51 and 0.38 over fourteen, the branch 0.49 and 0.37 over fifteen; GEN-192's recut of C1, C2, C5, C6, C9 and C17 leaves both figures unchanged to two decimals. More than one standard deviation from the mean: C2, C3, C4, C5 (D = 1.00, concrete and depended on) and C11, C15 (near zero, the intended delivery position) — the same set on both sides, in run 8 and after GEN-192. C17 sits at D 0.225 (I = 31 / 40 = 0.775, A = 0), inside the band. Its fan-in rose 7 → 9 (`coach-portal/routes.ts` and `coach-portal/shell/navigation-links.tsx`, its first imports of this feature) and its fan-out rose 26 → 31, net: seven modules became newly outward — the five new files (`api/settings/assessment-call-settings-controller.server.ts`, `api/settings/settings.ts`, `contracts/assessment-call-settings.ts`, `ui/coach/settings/assessment-call-settings-section.tsx`, `ui/coach/settings/settings-page.tsx`) plus two pre-existing ones that gained their first outward edge (`contracts/paths.ts`, into the accounts feature's `contracts/paths.ts`; `join-page.tsx`, into C5's `./layout` and `./primitives`) — against two that stopped being outward when GEN-192 deleted them (`data/static-coach-availability.server.ts`, `data/configured-meeting-room-link.server.ts`). Fan-in grew proportionally faster than fan-out (29% against 19%), so instability *fell*, 0.788 → 0.775, and D *rose* by the complementary amount, 0.212 → 0.225 (A holds at 0): C17 is now marginally *farther* from the main sequence than C7 (D = 0.213) and C14 (D = 0.214), the two components it most resembles, inverting last round's reading of "closer." It read 0.206 (I = 27/34) after the GEN-191 first fix round; the remediation round's join page, which no longer imported C5, took one outward-importing module away; the booking-card rebuild left it at 26/33 (0.212); GEN-192 is the first change to move it since. The nearest component to an edge is still C12 at D 0.14, and it stays inside. **No component was reclassified by C17's growth, or by C1, C2, C5, C6 or C9's recut.**

The paragraphs below record how each side moved before the merge.

**The coach-calendar refactor moved C6 back 0.77 → 0.73**, by less than the 0.10 threshold: its new `./coach-calendar/server` subpath adds three modules that import C1 or C2, raising fan-out 13 → 16, and the assessment-call repository becomes a new C6 dependent (the composition already was one), raising fan-in 43 → 44. C1's fan-in rose 61 → 62 with `PostgresCoachCalendar`, and its abstractness held at 0.23 as the `CoachCalendar` port arrived with `SlotPolicy`, `BusyInterval` (now `TimeInterval`) and `CoachTimeOutcome` (24 / 106 → 25 / 110); C2's fan-in rose 19 → 22 with the calendar, the reservation writers and the table. No component was reclassified.

One component's own D moved, by less than the 0.10 threshold, in the record update before this one. **C6 moved 0.72 → 0.77: its instability fell 0.28 → 0.23 because nine C17 modules joined its dependents (fan-in 34 → 43) while its own fan-out held at 13.** The review fix round left C6 untouched. C6 is concrete by design and owns no port, so every consumer that arrives pushes it further from the balanced line; the record already carries it as a watched screen and this update does not change that reading, but it is now as far out as C1 is on the other side. Nothing about C6 changed in this window beyond one Turnstile constant and the Resend attachment mapping — the movement is entirely the new dependents.

**C1 held at 0.77.** Its abstractness held at 0.23 because the four new ports arrived alongside nineteen new published types (20 / 87 → 24 / 106), and the fix round removed members and union payloads but no published type. Its fan-in rose 49 → 62 (61 at 422c7797, plus `call-facts.tsx`) and settled at 61 with the booking-card rebuild, whose `call-overview.tsx` replaced both `call-facts.tsx` and `slot-grouping.ts` as the browser half's one reader, without touching instability, which is zero either way. C5's fan-in rose 37 → 38 with the join page at f4f3dc16, fell back to 37 when the remediation round removed the join page's own page, and rose to 38 again with the rebuild's `slot-picker.tsx`, to the same (nil) effect on its D. C11 gained one published type, `PublicContentFrame`, and stays at A = 0. The second parity round moved no figure in the five recounted columns: the modules whose C5 imports changed subpath still import C5 (the hero keeps `./motion` after dropping `./lib` and `./primitives`), so C5's fan-in holds at 38, and the new `layout/dead-end-page.tsx` is an edge inside C5. C5's four published types are unchanged, since neither the dead-end page's props nor the input's are exported. The domain absorbed a whole feature's policy without moving on the main sequence.

Every other component's D is unchanged: C17's twelve, three, four, two, six and nine importing modules into C1, C2, C3, C4, C5 and C6 raised only fan-in, and the components it is imported by — C11, C14, C15 — already imported something else from outside themselves, so their fan-out counts of distinct modules did not move.

`8ac6a613` left C1 at D 0.77: four shared ports left C1 and two consumer-owned incident interfaces were added, taking abstract/total types from 20/87 to 18/79. C6 moves 0.72 → 0.70: it gains those three adapter-facing interfaces while eight of its former C1-dependent modules become concern-local, taking fan-out 13 → 8 and fan-in 34 → 39. C7, C8 and C14 keep their prior distances.

PR #230 replaces the C5 module `lib/focus-trap.ts` with `layout/navigation-dialog.tsx` and the package-private `layout/use-close-mobile-navigation-on-desktop.ts`. A cold cruise of the tree merging `74950bd7` and `3836745e` reads 308 modules (288 in-scope production modules), 793 dependencies, 0 violations and 0 circular; against main's 785 dependencies the navigation change adds eight edges and one module. C5 fan-in (32) and fan-out (0) are unchanged; the published `NavigationMenu` type raises its total types from three to four, so abstractness stays 0.00 and distance 1.00. C11's `public-navigation.tsx` swaps its `./primitives` import for `./layout` and `./motion`, which leaves its fan-in, fan-out and types unchanged.

In the run-8 window C1 carries the highest volatility, 12 of the 16 restructure commits and 25 of the branch's 84. Every one of the twelve is a move, a rename, or a behaviour-preserving internal refactor of the restructure itself: no domain entity, use case, port or boundary type changed in this window because a database, framework or vendor concern changed. That is one reason to change executed in twelve steps, not a component with many reasons. The position stays a watched screen for the next run, whose window is the first that can tell migration from volatility. GEN-191 is not that window: its seventeen commits are one feature delivered in slices, two review fix rounds and a remediation round. Four of them touched C1. `40f425e2` added the two new subpaths; `43bc46f8` edited the existing `/shared` subpath, widening `ProductEmailCommand` with an `attachments` list (`shared/product-email.ts`, `shared/index.ts`); `87facabb` revised the two subpaths GEN-191 had itself introduced, narrowing the reservation results and adding the time-zone check to `CoachAvailability.configure`; and `2acf65b9` widened `ProductEmailCommand` again with an optional per-send `replyTo`.

No component sits in the zone of uselessness: all 26 published abstract types in C1 have an implementer and a consumer, including the twenty-four declared `export interface` and the two structural `export type` contracts (`Accounts`, `Clock`). `AcquisitionIncidents`, `WaitlistIncidents` and `AssessmentCallIncidents` (B266, B267, B273) are each implemented by the one console adapter and consumed beside their use cases; the coach-calendar refactor's `CoachCalendar` is implemented by C6's `PostgresCoachCalendar` and consumed by `ListOpenSlotsUseCase` (B272); of the four ports GEN-191 added, `AssessmentCallReservations` (B268) and `AssessmentCallNotifications` (B270) are still each implemented once in C17; `CoachAvailabilitySource` (B269) is implemented in C6 since GEN-192, by `PostgresCoachAvailability`, and consumed by four use cases; and `MeetingRoomLink` (B271) is retired. GEN-192 adds three ports, `CoachAvailabilityChanges` (B274), `CoachMeetingRoomSource` (B275) and `CoachMeetingRoomChanges` (B276), each implemented once in C6 and consumed by one to two use cases. C6's three adapter-facing concern contracts (`BotVerifier`, `ProductEmail`, `ManagementAuthenticator`) each have at least one implementation and outside consumer. The unpublished `DownloadTokenGenerator` also has both (`AcquireProductsUseCase` consumes it; `RandomDownloadTokenGenerator` satisfies it structurally). One method is paid for and unused: `ProductAssets.assertReady` has no production caller, because the composition calls the adapter's own `assertReadyAtStartup()` instead; it predates this change review.

Component edge counts (distinct importing modules): see `dependencies.md`, section "Component graph". Cycles detected: none, at module, component, domain-folder and UI-subpath level, over 355 in-scope production modules of the merged tree before GEN-192 and 377 after. PR #229 is one coherent ownership refactor across C1, C6, C7, C8 and C14, not five independent requirement changes. In the run-8 window, two of the sixteen restructure commits touched three or more components — `2b6396bd` (C1, C6, C14: the feature-flag import repoint) and `9ef78f38` (C1, C10, C11: the bundle dissolution) — and both are uniform structural passes, not requirement changes crossing a boundary. Five of the seventeen GEN-191 commits touched three or more components: `43bc46f8` (C1, C3, C6: the attachment field on the shared email port, its Resend mapping, and the new config concern), `b2c711ef` (C5, C11, C14, C15, C17: registering the feature's routes and wiring its composition), `7c156e6f` (C5, C11, C17: the calendar's styling in `calendar/calendar.tsx`, the `BOOK_PATH` links in the hero, about and pricing sections, and the booking UI itself), `87facabb` (C1, C3, C14, C17: the reservation results narrowed in C1 with their C17 consumers, the config guard deleted from C3, and the composition handle renamed in C14's container), and `2acf65b9` (C1, C6, C17: the per-send reply-to on the shared email port, the Resend adapter honouring it, and the coach notification setting it). The first and fifth are port widenings that had to land on both sides at once, the second the wiring commit a new feature needs, the third one user-visible slice that spanned the design system, the surface and the feature, and the fourth a review fix round whose items were independent of each other.

## Persisted waitlist-mode change review

The change review covers `79fa1e95..f0eb1bf4` and its merge with main at `7d92dc22` in `242a0976`. A cold cruise at `c2277ebb` reads 307 modules, 785 dependencies, 0 violations and 0 circular dependencies; the 287 in-scope production modules are unchanged. Against the `7d92dc22` cruise, three edges left (`platform-composition.server.ts` → `packages/db`, → `packages/infrastructure` feature-flags, and its value import of `domain/feature-flag`) and six arrived (`container.server.ts` → `domain/feature-flag` and → `packages/infrastructure` feature-flags, `platform-composition.server.ts` and `waitlist-composition.server.ts` → `domain/feature-flag` as types, and `get-waitlist-use-case.ts` → `feature-flag/index.ts` and → `waitlist-incidents.ts`). The last column counts `63a570a7`, `f5d1889c`, `fc2fd208`, `010904d8`, `489f6293`, `48b8cafb` and `f0eb1bf4` per component, plus the merge `242a0976` in C1, C8 and C14, where it moved the failed-read report from the removed `Logger` port onto `WaitlistIncidents.waitlistModeReadFailed()`.

Two cells differ from the `8ac6a613` figures. C2's fan-in fell 16 → 15 because `platform-composition.server.ts` stopped importing `DatabaseClient`. C1's total type count rose 79 → 80 because `./feature-flag` now publishes `FeatureFlagSet`, which `GetWaitlistUseCase` names instead of deriving its own copy from the reader port; abstractness is 0.225 and distance 0.775, so the rounded D moves 0.77 → 0.78 without crossing the 0.10 review threshold. C1's fan-in stays 41 because every new importer of `feature-flag` already imported another C1 subpath. No instability changed; mean D stays 0.51 with standard deviation 0.38, the one-standard-deviation set is unchanged, and no component entered the zone of uselessness: `WaitlistIncidents` gained a method, not a type. The package packlists and handle visibility changes add no module edge.

C1 carries six of the eight counted commits, all serving one requirement: persisted waitlist mode read through the existing `FeatureFlagReader`. C3 remains a stable, concrete hub; its R31 finding is accepted because restructuring configuration was outside the approved behavior change. The waitlist-mode requirement changed C1, C3, C4, C5, C6, C8, C14 and C15, the C4, C5 and C6 touches being package-manifest packlists only; C8's R27 finding is likewise accepted rather than used to broaden this change. Component and domain-slice graphs remain acyclic.

## Coach portal follow-ups

A cold cruise of the working tree at HEAD reads 403 modules and 1102 dependencies, 0 violations and 0 circular. Two components move, neither by the 0.10 review threshold and neither reclassified; the range adds two modules to C17, one published function and one published constant to C5, one method to a C1 port and one edge inside C1.

**Three cells are corrected rather than carried, because this pass re-read every importer list from the cruise instead of adding a delta to the previous cell.** GEN-193 never recorded `ui/coach/assessment-calls/use-call-listing-params.ts` (now U1327), which imports C5's `./lib`, so its C5 fan-in and C17 fan-out were each one low before this range began, and C1's fan-in missed the coach controller. The corrected baselines are C5 45, C17 34 and C1 56.

**C5 fan-in rises 45 to 46** and its fan-out stays 0, so instability stays 0.00 and D stays 1.00. The one new importer this range adds is C17's `ui/coach/assessment-calls-error-boundary.tsx`, which takes `./layout`, `./lib` and `./primitives`. `DeadEndContent` is a function and `DEAD_END_BODY_CLASS_NAME` a constant, so the published type count holds at 5 and abstractness at 0.00.

**C17 fan-out rises 34 to 35** with that same module; its fan-in holds at 10, because the coach surface's `pages/home.tsx` already imported the feature and only added a re-export of the shared boundary. I moves 0.77 to 0.78 (35 / 45 = 0.7778 against the corrected 34 / 44 = 0.7727) and D 0.23 to 0.22, a tenth of the review threshold and no reclassification. Its abstractness holds at 0.00 (0 / 23): the range adds no exported type to C17 at all.

**C1, C13 and C14 hold.** C1 gains a method on an existing port and one intra-component edge (`list-assessment-calls-use-case.ts` to `assessment-call-incidents.ts`), neither of which crosses a component, and `AssessmentCallListingResult` renames `AssessmentCallListing` rather than adding a published type, so its 24 / 104 abstractness and D 0.77 are unchanged; only its fan-in cell is corrected, for the GEN-193 reason above. C13's fan-out is still five distinct modules: the re-exported boundary is a sixth edge out of `pages/home.tsx`, but that module was already counted. C14's logger implements one more method on a port it already implemented.

Volatility is not recounted here: a follow-up set of eleven commits is too short a window to read, and the next full architecture review owns the recount.
