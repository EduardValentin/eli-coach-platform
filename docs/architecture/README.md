# Architecture

Last update: GEN-192 ("Let the coach set her assessment call hours and meeting room", HEAD 05e46534, base f46b6f41, `f46b6f41..05e46534`; full scope, from a fresh cruise: 399 modules, 1084 dependencies, 0 violations), recorded on top of the merge of main (`843d8261..e2d86a3a`: PR #229 `7d92dc22`, PR #231 `74950bd7`, PR #230 `1979b1cc`, `984d54f2` and PR #232 `e2d86a3a`) into the GEN-191 branch at ea81d98f, recorded on top of the GEN-191 final fix round (`a4256c45..8a604aef`, HEAD 8a604aef: one `Card` with `cardVariants`, every theme role registered with `cn`, the slot policy's split checks), recorded on top of the GEN-191 fix round (`47edb229..59c019ec`), itself recorded on top of the GEN-191 coach-calendar refactor (`19169b9e..eb88ecb4`, carried onto this branch as 152ca98f and recorded at 47edb229), itself recorded on top of the GEN-191 design-system fix round on top of bff79348 (one field, alert and button system, the token mirror, one focus rule, the call-moment merge and the Eli portrait), after the record update for the owner's second parity round (`7a61673f..d3513898`), covering `843d8261..bcf6555e`, the booking-card rebuild (`bcf6555e..dde3b960`) and that working tree: the slices recorded at 422c7797, then corrected against the code after the first review fix round (`a54d0ece`, `87facabb`, `f4f3dc16`), the second (`7be7d1ad`), the remediation round (`c6122c63`, `2acf65b9`, `6fc3157f`), the booking-card rebuild and the second parity round, each from a fresh cruise. Last full inspection: architecture change review run 8 at commit e8690f45; baseline audit run 1 at 148d594f. Scope `apps/platform/src`, `apps/platform/db`, `packages/{config,content,db,domain,infrastructure,test-support,ui}/src`, and the enforcement layer (`tools/dependency-cruiser.config.cjs`, `tools/dependency-cruiser.tsconfig.json`, `tools/boundaries.test.mjs`, `tools/boundary-fixtures/`, `tools/domain-layout.mjs`, `tools/domain-layout.test.mjs`, `tools/domain-layout-fixtures/`, `knip.json`, `eslint.config.mjs`, workspace `package.json` export maps, tsconfigs, vite, react-router, and vitest configs). Maintained by the architecture inspection workflow; changes ship with the PR that causes them. Run 8 verdict: SHOULD_CHANGE (3 blocker, 3 major, 3 minor open; 312 targets OK; 6 changes named) recorded in the uncommitted ledger at `.architecture/ledger.md`; its three directions are projecting a snapshot where a use case hands an entity instance to a controller, fixturing the two domain-layout checks nothing yet exercises, and giving `WaitlistPresentation` the bundle-offer flag the pricing page currently derives for itself. The domain-layout direction has since been addressed on main: all six of the tool's checks now have a failing fixture. The rows those findings dispute are held in the ledger as pending record updates rather than written here. The GEN-191 update adds the assessment-calls feature (C17), the `assessment-call` and `coach-availability` domain subpaths, the `calendar` UI subpath and the four ports B268-B271; it does not re-run the evaluation workflows, and the run-8 findings above are still open. GEN-191's own findings were raised and dispositioned at plan-review time, the review fix round's three owner decisions (D-A, D-B, D-C) were taken on the code, and the remediation round added D-D (a per-send reply-to on the shared email command), D-E (an unknown join link answers the standard 404) and a set of accepted, deferred prototype-versus-production differences in the shared primitives; all are recorded in `decisions.md` under the 2026-09-18 owner decisions. The booking-card rebuild took most of those differences up in the primitives and the stylesheet, and gave the booking page its own wording of a call's date, time and zone, so the page and the emails no longer share one. The second parity round records the owner's decisions on the rest as current state (every button takes the 14px corner, replacing the pill rule for public controls; one disabled look; the prototype's single focus rule; a base layer and tokens that mirror the prototype's) and adds `DeadEndPage` to C5 `./layout`, shared by the root error, access-denied and sign-in-failed pages, the last now registered outside the public-site layout. The coach-calendar refactor moves the assessment call's slot policy into `/assessment-call` as a `SlotPolicy` that `CoachAvailability` takes as a parameter, reads busy time as intervals through the new `CoachCalendar` port (B272), and replaces the exact-start unique index with C6's `coach_time_reservations` table and its hand-written no-overlap exclusion constraint, which every appointment kind will reserve through; its owner decisions are CC1–CC4 in `decisions.md`. The fix round steps the coach's availability in minutes and offers a start only if the call ends inside the window, makes `SlotPolicy.of` reject an invalid policy, renames `BusyInterval` to `TimeInterval`, keeps one coach-time reservation per appointment with a unique index (migration `0023`), moves the transaction type and the cause-chain reader of a database error into C2 (`DatabaseTransaction`, `isCausedByDatabaseError`), names each call-moment formatter for its wording in one fixed locale, and rebuilds production's `Button` on role axes (`width`, `weight`, `textSize`, `lettering` in place of `label`) with the header's look passed as `PublicHeaderAppearance`; it was recorded from a fresh cruise and moved no fan-in, fan-out or distance.

The merge resolution moves the assessment-call slice onto main's contracts and persisted waiting-list mode. The booking gate is `AssessmentCallBookingWindow` in `/assessment-call`, which both assessment-call use cases take in place of the `bookingOpen` boolean the container used to compute from the environment: it reads the `WAITLIST_MODE` feature flag (`WAITLIST_MODE_FEATURE_FLAG`, the key `GetWaitlistUseCase` also reads) through the container's one `FeatureFlagReader` on every request, keeps booking open only while the flag is not true, and counts a failed read as closed. The slice's incidents go through the new `AssessmentCallIncidents` port (B273: `bookingModeReadFailed`, `notificationFailed`, `slotsReadFailed`) in place of the removed shared `Logger`, implemented by the same console adapter as `AcquisitionIncidents` and `WaitlistIncidents`; its controller and email adapters take `BotVerifier` and `ProductEmail` (with `ProductEmailCommand` and `EmailAttachment`) from C6's `./bot-detection/server` and `./email/server`. The GEN-191 migrations are renumbered `0020`–`0023` after main's `0018` and `0019`. The public site's navigation composes C5's `NavigationDialog`, which gains an optional `menuButtonVariant`, with the branch's header look (`PublicHeaderAppearance`). `lighthouserc.cjs` is main's: Lighthouse CI runs with no database, an unreadable flag closes booking, so `/book/` is not audited (D21 is superseded). Colliding identifiers were renumbered on the branch side: units `U1200`–`U1283` → `U1215`–`U1298`, edges from `E1014` up by 202, ports `B266`–`B270` → `B268`–`B272`, and the spec's owner decisions D1–D5 → D17–D21.

Partial change review of `8ac6a613` (PR #229 head, squash-merged to main as `7d92dc22`; base `79fa1e95`) on 2026-09-18: scope the 46 implementation files `79fa1e95..8ac6a613` changed in C1 domain, C6 infrastructure, C7 store, C8 waitlist and C14 server, plus their direct dependency neighborhood; architecture artifacts excluded. Verdict for that scope: OK (0 blocker, 0 major, 0 minor open; 55 assessment rows OK; 0 changes named), recorded in the same ledger. It did not re-adjudicate the Run 8 findings above, which stay open.

Partial change review of the persisted waitlist-mode change on 2026-09-18: commit `c2277ebb` (baseline `7d92dc22`), covering `79fa1e95..f0eb1bf4` and its merge with main in `242a0976`; scope the changed units and their direct graph neighborhood in `apps/platform`, `apps/platform/db`, the app and workspace package manifests, `packages/{config,content,db,domain,infrastructure,ui}`, the integration and E2E test architecture, Playwright, Lighthouse, CI and the Docker build. Its findings are resolved or explicitly accepted in the same ledger; it did not re-adjudicate the Run 8 findings, which stay open.

The C5 component, unit, dependency and metric rows describe the Radix-backed mobile navigation, whose `NavigationDialog` layout primitive owns the mobile menu for the public site and the portals. The rows come from PR #230 (6c043f97, 63100724, eced8490 and 3836745e). History is anchored to e8690f45 until the next full architecture review. A cold cruise of the merged working tree, before GEN-192, read 378 modules (355 in-scope production modules), 1016 dependencies, no violations and no cycles. GEN-192's own fresh cruise reads 399 modules, 1084 dependencies, 0 violations, no cycles, over the same in-scope production filter.

GEN-192 ("Let the coach set her assessment call hours and meeting room", `f46b6f41..05e46534`, base f46b6f41) replaces the coach's two hard-coded assessment-call adapters with her own saved settings. `StaticCoachAvailability` and `ConfiguredMeetingRoomLink` are deleted; the throwing `CoachAvailability.configure` is replaced by the result-returning `CoachAvailability.from` (`{ status: "configured" | "invalid" }`), called both by the new `PostgresCoachAvailability` on read and by the new `UpdateAssessmentCallSettingsUseCase` on save, and backed by the one-row `coach_availability` table (migration `0024`). `PostgresCoachAvailability` implements both `CoachAvailabilitySource` and the new `CoachAvailabilityChanges` port, and lives in C6's `./coach-calendar/server` subpath, now split into an `availability/` and a `reservations/` subfolder (the pre-existing calendar and reservation-writer modules moved into `reservations/` unchanged). A parallel new domain slice, `/coach-meeting-room` (`CoachMeetingRoom`, `CoachMeetingRoomSource`, `CoachMeetingRoomChanges`), replaces the retired `MeetingRoomLink` port; `PostgresCoachMeetingRoom` implements both new ports behind C6's new `./coach-meeting-room/server` subpath, reading and writing the one-row `coach_meeting_room` table the same migration creates. `ResolveJoinLinkUseCase` now takes `CoachMeetingRoomSource` directly in place of `MeetingRoomLink` and gains a third outcome, `link_not_set`, which `AssessmentCallJoinRoute`, the join route's own page component, renders as a "call link not ready" page built from C5's `DeadEndPage`, instead of throwing the standard 404; the join route (`assessmentCallsJoinRoutes`) is registered as a second fragment outside the public-site shell, alongside the accounts feature's `accountsDeadEndRoutes`. Two new use cases, `GetAssessmentCallSettingsUseCase` and `UpdateAssessmentCallSettingsUseCase`, sit behind a coach-only settings page at `/coach/settings` — the coach portal's (C13) first feature route — served by a new `AssessmentCallSettingsController` (`api/settings/`) answering `PUT /api/assessment-calls/settings`; the pre-existing booking controller, its routes and its integration tests move unchanged into a sibling `api/booking/` folder. `packages/ui` gains three design-system primitives the settings form consumes: `CheckboxChip` and `Select` in `./primitives`, and `Toaster`/`toast` in `./overlays`, wrapping the package's second vendor dependency, `sonner`, the same way `./calendar` wraps `react-day-picker`. `ASSESSMENT_CALL_MEETING_LINK` leaves the environment for good (`docs/SECRET_MANAGEMENT.md`); `ASSESSMENT_CALL_COACH_EMAIL` remains the one assessment-call runtime variable (D-A).

## Shape

The business rules live in `packages/domain` (C1), published as twelve subpaths with no root barrel: one folder per entity (`account`, `acquisition`, `assessment-call`, `cart`, `coach-availability`, `coach-meeting-room`, `download-grant`, `email-address`, `feature-flag`, `product`, `waitlist`) plus the interface-only `shared`. Rules are methods on the entity classes that carry the decisions — account lifecycle and portal access (`Account#isActive`), waitlist availability and reduced-pricing allocation, delivery limits and their windows, download-grant validity and delivery shape, publication slug, taxonomy, size-cap, digest and target rules, cart reconciliation, purchasability (`Product.evaluatePurchasability`), file-format resolution, email normalization (`EmailAddress`), and the assessment-call rules — which starts in the coach's own zone are open (`CoachAvailability.openSlotStarts`, `isOpenStart`, stepping in minutes from the window's opening, where a start is open only when the call ends inside the window and the call and its buffer overlap none of the coach's busy intervals), whether a slot may be reserved (`AssessmentCall.decideReservation`, where a taken slot is refused whoever holds it), and the assessment call's slot policy (`ASSESSMENT_CALL_RULES`, a `SlotPolicy` the `/assessment-call` slice owns and hands to `CoachAvailability`: the duration, read once to derive `endsAt` on the snapshot so no adapter re-applies it, the buffer, which with the duration sizes the coach's time a call reserves, and the step, horizon and lead time; `SlotPolicy.of` rejects a policy whose step is shorter than the call or whose values are not whole minutes) — and `<Verb><Noun>UseCase` classes, each with one `execute` method, sequence them. Twenty-six published ports and one unpublished one are named for their consumers' capabilities: eleven gateways (`Accounts`, `WaitlistEntries`, `StoreCatalog`, `StoreAcquisitions`, `StoreProductPublications`, `DownloadGrants`, `FeatureFlags`, `ProductAssets`, `AssessmentCallReservations`, `CoachAvailabilitySource`, `CoachCalendar`), the three notification ports (`WaitlistConfirmation`, `ProductDelivery`, `AssessmentCallNotifications`), the writer, digest and hasher ports (`ProductAssetWriter`, `ProductAssetDigest`, `PayloadDigestGenerator`, `DownloadTokenHasher`), the two `/coach-availability` and `/coach-meeting-room` change ports the coach's own settings save through (`CoachAvailabilityChanges`, `CoachMeetingRoomChanges`) and the read port the retired `MeetingRoomLink` left behind (`CoachMeetingRoomSource`), the input boundary `FeatureFlagReader`, `Clock` as the sole `shared` port, and `AcquisitionIncidents`, `WaitlistIncidents` and `AssessmentCallIncidents` beside the use cases that consume them. `DownloadTokenGenerator` remains deliberately unpublished because its only consumer is inside the package. Waitlist mode is persisted policy input, not deployment configuration: `Waitlist` is the configured waiting-list aggregate whose cap and active offer are shared by availability and registration, and `GetWaitlistUseCase` reads the existing `FeatureFlagReader` on every execution, reads `WAITLIST_MODE` from the published `FeatureFlagSet` (true enables, false or absent disables), and assembles the plain `WaitlistSnapshot` from that mode, the entity's offer and its computed availability. A failed flag read is reported through `WaitlistIncidents.waitlistModeReadFailed()` and safe-enables the waitlist with availability unknown. `AssessmentCallBookingWindow` reads the same flag, through the same key, for booking: open only while the flag is not true, and closed on a failed read, reported through `AssessmentCallIncidents.bookingModeReadFailed()`. The resulting `waitlist → feature-flag` and `assessment-call → feature-flag` edges enter the sibling's index, stay in the use-case ring and close no cycle. The offer object is readonly but not deeply immutable; that R17 cost is accepted until offer mutation or a second offer model is required. The package declares no dependencies and its tsconfig sets `"types": []`, so neither an import nor an ambient Node global reaches inside it — which is why the wall-clock arithmetic the assessment-call slots need is written against `Intl.DateTimeFormat` alone, in `coach-availability/zoned-time.ts`, rather than against a date library.

Details plug in through those ports. Each feature under `apps/platform/src/features/<feature>/` holds its Postgres repositories (`data/`), email adapters (`email/`), HTTP controllers and route modules (`api/`), route fragment (`routes.ts`), wire schemas, path literals and anything else both halves must word identically (`contracts/`, the one browser-safe folder the server half and the browser half may both import), request-context key and guards (`server/guards/`), its composition (`server/<feature>-composition.server.ts`) and its browser code (`ui/<slice>/`, with presenters and step machines in `ui/shared/` and `ui/public/`). `packages/infrastructure` (C6) holds adapters and factories — Turnstile bot detection, Resend and in-memory product email, bearer-secret management auth, the feature-flag repository, the coach calendar (the coach's own availability window, read and saved, alongside the Postgres read of her reserved time and the transaction-scoped writers every appointment kind reserves through), the coach's meeting room (read and saved), HTTP helpers and PWA definitions — and owns the three adapter-facing contracts `BotVerifier`, `ProductEmail` and `ManagementAuthenticator` inside their matching concerns. `packages/test-support` (C16) is a dev-only fixture package no production package declares. `packages/db`, `packages/config`, `packages/content` and `packages/ui` are leaf packages; `packages/ui` publishes seven concern subpaths and the stylesheet with no root barrel; `calendar` is where a composite over a third-party widget goes so that consumers of `primitives` do not carry the vendor, and since GEN-192 `overlays` carries a second vendored composite the same way (`Toaster` over `sonner`, beside `Sheet`'s own Radix wrap). The application composition root is `apps/platform/src/server/container.server.ts`, memoised behind `getPlatformContainer()` and imported only by `root.server.ts`: it builds the shared handles once, including one `createConsoleLogger()` that implements all three domain incident interfaces and one `PostgresFeatureFlagRepository` behind one stateless `GetFeatureFlagsUseCase`, and calls the four feature compositions (`accounts`, `assessment-calls`, `store`, `waitlist`), all four of them synchronous, handing the same feature-flag reader to `composePlatformFeature`, `composeWaitlistFeature` and `composeAssessmentCallsFeature`; `platform-composition.server.ts` constructs no persistence. The platform feature-flag API, the public-site shell and the booking window each execute that reader per request, with no result cache and no production write path. Routes and loaders read their feature's request-context key (`accountsContext`, `sessionContext`, `storeContext`, `waitlistContext`, `assessmentCallsContext`, `platformContext`, `runtimeConfigContext`) off `args.context` instead of resolving anything from a container. Surfaces (`public-site`, `client-portal`, `coach-portal`) assemble features into products and own only route modules, shells and sections; `public-site` reaches the booking feature through a route fragment and one path literal and imports nothing else; at runtime its layout also reads the route handles of the booking page and of its own pricing page, which both ask for a full-bleed content frame.

Boundaries are enforced by three tools. `tools/dependency-cruiser.config.cjs` declares 35 forbidden rules — package export maps and dependency-absence for the workspace packages, and named rules for every app-level boundary (feature internals, surface-to-feature and surface-to-surface, composition root, browser half, route thinness, guards, domain slices, infrastructure subpaths, UI layering, workspace-by-name, stability and five hygiene rules) — `knip.json` holds published surfaces at zero unused exports, files, dependencies and unlisted dependencies, and `tools/domain-layout.mjs` checks the domain package's per-entity folder layout. All three run in `pnpm check:boundaries`, which runs inside `pnpm typecheck`, `pnpm build`, every vitest run (through `vitest.global-setup.mts`) and the Docker builder stage, so a crossing fails the build and the image. `tools/boundaries.test.mjs` proves the dependency-cruiser rules against `tools/boundary-fixtures/`: 34 of the 35 rules have a fixture, and `stability` has none because `moreUnstable` needs dependent counts a fixture tree cannot express — the real-tree cruise is its only proof. `tools/domain-layout.test.mjs` exercises `tools/domain-layout.mjs` against `tools/domain-layout-fixtures/` and against the real domain package; all six of the tool's checks have a failing fixture. Two rules stay review-owned by decision: controller shape (no base controller, no per-request instance field) and failure mapping (an adapter never swallows an infrastructure failure into a result-union member; a named expected condition may be classified and everything else is rethrown). The classified conditions are ENOENT in the filesystem asset store; Postgres `22P02` in the assessment-call repository (a malformed booking id is a mistyped URL) and Postgres `23P01` on the `coach_time_reservations_no_overlap` exclusion constraint in C6's `reserveCoachTime` (losing a race for the coach's time is a taken slot), both read through C2's `isCausedByDatabaseError`; and the waitlist signup's unique and check violations on its three named constraints, which register at the regular price or refresh consent (D15). Each is recorded in `decisions.md`. The component graph has no cycles at module, component, domain-slice or UI-subpath level. Deployment adds artifact controls outside the dependency rules: the platform package's `files` allowlist emits only `build`, the config, content, domain, infrastructure and UI packlists keep production source and artifacts while excluding colocated test/spec files, and the Docker builder asserts that the deployed artifact contains `build/server/index.js`, has no top-level `src` or `e2e`, has no test/spec file under `node_modules/@eli-coach-platform`, and omits the dev-only test-support package. Environment, Docker, CI, integration and Lighthouse wiring no longer carry `WAITLIST_MODE`; migration 0018 seeds the persisted flag true, and E2E setup and teardown switch it through a test-only Postgres adapter that production imports cannot reach.

## Reading this folder

- `components.md`: the enforced units and what each publishes.
- `units.md`: classes, modules, functions, interfaces, and data structures, with their kind and ring.
- `dependencies.md`: every source dependency, the forbidden edges, the ports, entry points, and shared data shapes.
- `metrics.md`: stability and abstractness per component, recomputed from the merged cruise; volatility still counts the 84 commits in `148d594f..e8690f45`, with the 16 of the domain-model restructure shown separately, plus PR #229, and three further columns count the waitlist-mode commits of `79fa1e95..f0eb1bf4` plus their main merge, PR #230's mobile navigation, and the seventeen GEN-191 commits, without extending the window.
- `decisions.md`: deferred decisions, accepted trade-offs, intended exceptions, accepted findings.
- `conventions.md`: where a file goes and what it may import; the folder layout the dependency rules assume.

Terms: a *unit* is a class, module, function, interface, or data structure. A *component* is a group of units behind one published interface that tooling enforces. An *edge* from A to B means A's source code names B. *Policy* is business rules; *detail* is databases, frameworks, transports, vendors, UI, and the data shapes they produce. A *ring* is a band of units at one level of distance from inputs and outputs; dependencies point inward.
