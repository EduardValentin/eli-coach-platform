# Components

Header: date 2026-09-17, commit dbe88053, scope apps/platform/src, apps/platform/db, packages/{config,content,db,domain,infrastructure,test-support,ui}/src plus the enforcement layer (tools/dependency-cruiser.config.cjs, tools/dependency-cruiser.tsconfig.json, tools/boundaries.test.mjs, tools/boundary-fixtures/, knip.json, eslint.config.mjs, workspace package.json export maps, tsconfigs, vite/react-router/vitest configs), mode change review (run 7).

A component is a group of units behind one published surface that tooling enforces. Workspace packages are enforced by their `package.json` export maps (a deep import does not resolve). App-level folders are enforced by named rules in `tools/dependency-cruiser.config.cjs` and by `knip.json`, proven by `tools/boundaries.test.mjs` and run by `pnpm check:boundaries` inside `pnpm typecheck`, `pnpm build`, every vitest run and the Docker builder stage; a crossing fails the build, not only `pnpm lint`. Nothing is a release unit: every package is `private` and consumed as `workspace:*` by path. Tests are the outermost pseudo-ring, not a component.

| ID | Path | Published surface | Enforcement | Ring | Actors | Release unit |
|---|---|---|---|---|---|---|
| C1 | packages/domain | Seven subpaths, no root barrel. `./accounts` (Account, AccountRole, AccountSnapshot, canAccessClientPortal, canAccessCoachPortal, Accounts, AccountProvisioningService (+AccountProvisioningResult), AccountDeletionService); `./waitlist` (WaitlistService, WaitlistEntries, WaitlistConfirmationService (+WaitlistConfirmationResult), resolveWaitlistAvailability, decideReducedPricingRegistration, models); `./store` (StoreCatalogService/StoreCatalog, StoreAcquisitionService/StoreAcquisitions/StoreDeliveryService/StoreDeliveryResult, DownloadGrantService/DownloadGrants/DownloadTokenHasher/DownloadGrantResolution, StoreProductPublicationService/StoreProductPublications, ProductAssets/ProductAssetOpenResult, ProductAssetWriter/ProductAssetDigest/ProductAssetContent, reconcileCart, evaluateDeliveryLimit, evaluatePurchasability, resolvePublicationTarget, resolveCoverFormat, resolveDownloadFormat, isStoreCoverMimeType, MAX_PUBLICATION_BYTES, models, publication models); `./coaching-bundles` (coachingBundles, coachingBundleBenefits, resolveCoachingBundleDisplay, WAITLIST_BUNDLE_OFFERS, CoachingBundleWaitlistOfferPlan); `./feature-flags` (FeatureFlagService, FeatureFlagReader, FeatureFlags, PersistedFeatureFlag); `./email-address` (normalizeEmail); `./shared`, interface-only (`export type` only: Clock, Logger, BotVerifier, ProductEmail, ManagementAuthenticator and their boundary data) | export map (seven subpaths, no `.`) + dependency-absence (`package.json` declares no dependencies) + `"types": []` in its tsconfig (no ambient Node globals) + the `domain-slices` and `domain-no-externals` rules | use-cases (entities: account-model, waitlist-availability, coaching-bundles, delivery limits, download-grant, publication rules, cart, purchasability, format rules) | operator/platform, visitor, client, coach; vendor:clerk and vendor:resend only through ports | no |
| C2 | packages/db | createDatabaseClient, DatabaseClient (Drizzle `NodePgDatabase`), createManagedDatabasePool, appSchema | exports (`.`) | adapters/frameworks | operator/platform | no |
| C3 | packages/config | Two entries. `.`: the concern types (AppConfig, DatabaseConfig, DatabaseBootstrapEnvironment, DatabaseConnection, WaitlistConfig, BotDetectionSettings, ProductEmailConfig, ManagementApiConfig, RuntimeEnvironment) plus TURNSTILE_TEST_RESPONSE_TOKEN, joinBasePath, buildRedirectPath. `./runtime`: loadRuntimeEnvironment, loadDatabaseBootstrapEnvironment, the database user/connection helpers, hasCompleteDatabaseConfiguration, resolveRuntimeDatabaseConnection | exports (`.`, `./runtime`) + `config-runtime-readers` | frameworks | operator/platform | no |
| C4 | packages/content | Legal document types (LegalDocument carries a preformatted `effectiveDateLabel`), PRIVACY_POLICY(+_VERSION), WEBSITE_AND_STORE_TERMS_DOCUMENT, WAITLIST_MARKETING_CONSENT(+_VERSION), STORE_MARKETING_CONSENT(+_VERSION), ELI_COACH_CONTACT_EMAIL, EVOA_FITNESS_PRIVACY_EMAIL | exports (`.`) | entities (boundary-data) | operator/platform | no |
| C5 | packages/ui | No root barrel. Six concern subpaths plus the stylesheet: `./filters` (FilterChip, FilterChipGroup, FilterChipTone); `./layout` (AppShell, PhoneFrame, PortalShell, PortalNavigationLink, SidebarSurfaceLayout); `./lib` (cn, MAIN_CONTENT_ID, useSearchParamsWriter); `./motion` (createFadeUpVariants, publicEase, publicEaseOut, publicViewportOnce, useClientReducedMotionPreference); `./overlays` (Sheet, SheetContent, SheetDescription, SheetTitle); `./primitives` (Button, buttonVariants, Card, Checkbox, IconButton, Input, inputClasses, Link, linkVariants, SectionEyebrow); `./styles.css`. `resolveFocusTrapTarget` is package-private in `lib/focus-trap.ts` | exports (seven entries) + `ui-subpaths`, `ui-primitives-import-only-lib`, `ui-lib-is-the-base` | frameworks | operator/platform (design system); coach/client via PortalShell; visitor via FilterChipGroup | no |
| C6 | packages/infrastructure | No root barrel. Seven subpaths: `./pwa` (pwaSurfaceDefinitions, createPwaRegistration); `./bot-detection` (browser: BotDetectionWidget, BotDetectionConfig, useBotDetectionSubmission, Turnstile constants); `./bot-detection/server` (createBotDetectionConfig, createBotVerifier, resolveRequestRemoteIp); `./email/server` (createProductEmail, InMemoryProductEmail, the Email* React primitives); `./feature-flags/server` (PostgresFeatureFlagRepository); `./management-auth/server` (createManagementAuthenticator, createManagementAuthConfig, isSecureManagementTransport, ManagementAuthConfig); `./http/server` (HttpJsonError, handleHttpErrorResponse, throwMethodNotAllowedResponse, createBadRequestResponse, readFormDataRequestBody). TurnstileBotVerifier, StaticTokenBotVerifier and BearerSecretManagementAuthenticator are un-exported behind their factories. C6 owns no port | exports (seven subpaths) + `infrastructure-subpaths`, `infrastructure-browser-entries` | adapters | operator/platform, visitor, client (pwa); vendor:cloudflare-turnstile, vendor:resend | no |
| C7 | apps/platform/src/features/store | `contracts/store.ts`, `contracts/store-management.ts`, `contracts/paths.ts` (STORE_ROUTE_SEGMENT, STORE_PATH, STORE_API_PATHS); `routes.ts` (storePublicRoutes, storeApiRoutes); `server/guards/store-context.server.ts` (storeContext); routed modules (`api/*.ts` eight resource routes; `ui/public/{catalog,product,download}-page.tsx`); `ui/public/{cart-drawer,cart-provider}.tsx` consumed by the public-site shell; `data/schema.server.ts` discovered by drizzle-kit glob. Controllers, repositories and email services are imported only by `server/store-composition.server.ts`; `ui/public/catalog-presenter.ts` is feature-private | `feature-internals`, `feature-schema-foreign-key`, `feature-server-private`, `feature-api-to-data`, `features-never-reach-server`, `guards-construct-nothing`, `browser-half`, `browser-half-loaders`, `route-thinness`, `route-thinness-domain`, `surface-<s>-to-feature`, `surface-import` | adapters (ui/ is frameworks) | visitor, operator/platform (management API); vendor:resend, vendor:cloudflare-turnstile | no |
| C8 | apps/platform/src/features/waitlist | `contracts/waitlist.ts`, `contracts/paths.ts`; `routes.ts` (waitlistApiRoutes); `server/guards/waitlist-context.server.ts` (waitlistContext); `ui/shared/waitlist-presentation.ts` (presentWaitlist, WaitlistPresentation); `api/waitlist.ts` (route); `ui/public/email-form.tsx`, `ui/public/availability-status.tsx`; `data/schema.server.ts` (glob) | same rule set as C7 | adapters | visitor, operator/platform; vendor:resend | no |
| C9 | apps/platform/src/features/accounts | `contracts/account.ts`, `contracts/paths.ts` (SIGN_IN_FAILED_PATH, CLIENT_PORTAL_ROUTE_SEGMENT, COACH_PORTAL_ROUTE_SEGMENT and the portal recovery paths); `routes.ts` (accountsPublicRoutes, accountsApiRoutes); `server/guards/{accounts-context,session-context,require-account,require-portal-access}.server.ts`; `server/account-resolution-middleware.server.ts` (root.server.ts); `ui/public/auth-nav-actions.tsx` (C11); `ui/shared/access-denied-page.tsx` (C15 root.tsx); routes `api/account.ts`, `api/clerk-webhooks.ts`, `ui/public/sign-in-failed-page.tsx` | same rule set as C7 | adapters | client, coach, visitor, operator/platform; vendor:clerk | no |
| C10 | apps/platform/src/features/coaching-bundles | `ui/shared/coaching-bundles-presentation.ts` (presentCoachingBundles, CoachingBundleCard); `ui/public/bundle-selector.tsx`, which takes cards, benefits and `showsWaitlistPricing` and imports no domain module | `feature-internals`, `surface-<s>-to-feature`, `surface-import`, `route-thinness`, `route-thinness-domain` | adapters | visitor | no |
| C11 | apps/platform/src/surfaces/public-site | Route modules only: `routes.ts` (publicSiteRoutes), `paths.ts` (PRICING_PATH), `shell/layout.tsx` (+ `layout.server.ts` loader, shouldRevalidate), pages home, pricing, blog, privacy, terms | `surface-to-surface`, `surface-import`, `surface-public-site-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | visitor, operator/platform (waiting-list mode, marketing and legal copy) | no |
| C12 | apps/platform/src/surfaces/client-portal | Route modules only: `routes.ts` (clientPortalRoutes), `shell/layout.tsx` (middleware), `pages/home.tsx`, `api/{manifest,sw,readyz}.ts` | `surface-to-surface`, `surface-import`, `surface-client-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | client, operator/platform | no |
| C13 | apps/platform/src/surfaces/coach-portal | Route modules only: `routes.ts` (coachPortalRoutes), `shell/layout.tsx` (middleware), `pages/home.tsx`, `api/readyz.ts` | `surface-to-surface`, `surface-import`, `surface-coach-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | coach, operator/platform | no |
| C14 | apps/platform/src/server | `container.server.ts` (getPlatformContainer, createPlatformContainer, PlatformContainer), `feature-contexts.server.ts` (createFeatureContextMiddleware), `platform-composition.server.ts` (composePlatformFeature, PlatformFeature), `database.server.ts`, `runtime-environment.server.ts`, `logger.server.ts` (createConsoleLogger), `guards/platform-context.server.ts` (platformContext), `guards/runtime-config-context.server.ts` (runtimeConfigContext), `api/` (readyz, meta and feature-flags routes with their controllers and the relocated feature-flag controller and contract) and `api/routes.ts` (platformApiRoutes) | `composition-root`, `server-guards-consumers`, `root-registry-to-server`, `config-runtime-readers` | composition (container, feature contexts) with adapters (controllers) and frameworks (routes) | operator/platform | no |
| C15 | apps/platform app root: `src/root.tsx`, `src/root.server.ts`, `src/root-error-page.tsx`, `src/routes.ts`, `src/app.css`, `src/types/`, `db/drizzle.config.ts`, `vite.config.ts`, `react-router.config.ts` | Root route module (Root, Layout, ErrorBoundary, meta, links, middleware, loader); the route registry, a concatenation of seven fragments with no path literals; build and migration configuration | `root-registry`, `root-registry-to-server`, `composition-root` | frameworks (root.server.ts is a second composition point) | operator/platform, visitor, client, coach | no |
| C16 | packages/test-support | `.`: CLERK_TEST_ENVIRONMENT | export map + `no-production-import-of-tests` and `not-to-dev-dep` (declared only as a devDependency of `packages/config` and `apps/platform`, so a production import fails both the rule and, after `pnpm --prod deploy`, resolution) | frameworks (test fixture) | none in production | no |

Not components: `tools/dependency-cruiser.config.cjs`, `tools/boundaries.test.mjs`, `tools/boundary-fixtures/` and `knip.json` are the enforcement layer; test files are the tests pseudo-ring; `designs/react-reference-app` is a separate deployable outside scope.
