# Units

Header: date 2026-09-19, commit ea81d98f merged with origin/main at e2d86a3a (branch point 843d8261); on the branch side, the GEN-191 final fix round (`a4256c45..8a604aef`) on top of the GEN-191 fix round (`47edb229..59c019ec`) on top of the coach-calendar refactor (`19169b9e..eb88ecb4`, carried onto this branch as 152ca98f and recorded at 47edb229) on top of the design-system fix round on top of bff79348 (after the second parity round `7a61673f..d3513898`), scope apps/platform/src, apps/platform/db, packages/{config,content,db,domain,infrastructure,test-support,ui}/src plus the enforcement layer (tools/dependency-cruiser.config.cjs, tools/dependency-cruiser.tsconfig.json, tools/boundaries.test.mjs, tools/boundary-fixtures/, tools/domain-layout.mjs, tools/domain-layout.test.mjs, tools/domain-layout-fixtures/, knip.json, eslint.config.mjs, workspace package.json export maps, tsconfigs, vite/react-router/vitest configs), mode record update for GEN-191 over `843d8261..bcf6555e`, the booking-card rebuild (`bcf6555e..dde3b960`) and the second parity round on top of it — the slices recorded at 422c7797, corrected after the two review fix rounds (`422c7797..186b77c4`), the remediation round (`186b77c4..6fc3157f`), the booking-card rebuild and the second parity round — on top of change review run 8 at e8690f45. GEN-191 added rows U1215–U1250, U1255–U1267 and U1275–U1286; U1251–U1254 and U1268–U1274 were never assigned. U1278–U1280 came in with the review fix round (`422c7797..f4f3dc16`), which also retired U1233; every row GEN-191 touched was re-read against f4f3dc16, the rows the second fix round (`f4f3dc16..7be7d1ad`) touched against 7be7d1ad, and the rows the remediation round (`186b77c4..6fc3157f`) touched against 6fc3157f, which added U1281. The booking-card rebuild (`bcf6555e..dde3b960`) added U1282–U1286 and retired U1245 and U1279, whose IDs stay out of use; every row it touched was re-read against that working tree. The second parity round (`7a61673f..d3513898`) added U1287 and retired no unit; every row it touched was re-read against that working tree. The design-system fix round on top of bff79348 added U1288 and retired U1284 (merged into U1278) and U1285 (its classes became `Alert`, `Button` variants and layout classes), whose IDs stay out of use; every row it touched was re-read against that working tree. The coach-calendar refactor (`19169b9e..eb88ecb4`, carried onto this branch as 152ca98f) added U1289–U1295, moved U1264 into the `assessment-call` slice and retired no unit; every row it touched was re-read against 152ca98f. The GEN-191 fix round (`47edb229..59c019ec`) added U1296–U1298, renamed U1290's module and symbol, and retired no unit; every row it touched was re-read against 59c019ec. Every other row is carried from run 8. The merge with main renumbered every GEN-191 row by +15 (U1200–U1283 became U1215–U1298) so that main's U1193–U1214 keep their IDs; the ranges above are the renumbered ones. The merge resolution added U1299 (`AssessmentCallIncidents`) and U1300 (`AssessmentCallBookingWindow`), and re-read U519, U851, U852, U908, U1196, U1218, U1226, U1235, U1259 and U1260 against the merged working tree.

Change review: date 2026-09-20, commits `a5615696..HEAD` on `claude/coach-portal-followups`; scope the ad-hoc coach portal follow-up set across C1, C5, C13, C14, C17 and the tests pseudo-ring; partial scope. It adds U1321-U1327, changes U519, U710, U802, U830, U851, U1215, U1235, U1287, U1299, U1305, U1306, U1311 and U1315, and retires nothing. U1321, U1322 and U1327 entered with GEN-193 at a5615696 and had no row: that review recorded the coach listing's UI and its domain ports but not the use case, the controller or the listing-params hook behind them. This pass swept the whole of GEN-193 for the same gap and backfills all three, because the third is the module whose absence skewed the C5 and C17 figures this record publishes.

Change review: date 2026-09-18, commit 8ac6a613 (PR #229 head, squash-merged to main as 7d92dc22; base 79fa1e95), scope 46 changed implementation files in C1, C6, C7, C8 and C14 plus direct neighbors, mode partial change review (run 8 baseline e8690f45).

Change review: date 2026-09-18, commit b3eb2653 (PR #232, base 299f237f); scope the waitlist capacity enforcement: the C8 waitlist repository, table, constraint-violation classifier and migration 0019, the C1 cap constant, the C3 waitlist config and the integration rig; partial scope. Rows it changed or added carry the commit.

Change review: date 2026-09-18, commit c2277ebb, baseline 7d92dc22, scope the persisted waitlist-mode change (`79fa1e95..f0eb1bf4`, merged with main in `242a0976`): the changed units and their direct graph neighborhood in apps/platform, packages/{config,content,db,domain,infrastructure,ui}, tests, migrations and package deployment; partial scope. Rows it changed or added carry the commit that changed them.

Change review: date 2026-09-18, commits 6c043f97, 63100724, eced8490 and 3836745e (PR #230, base 843d8261), merged with main at 74950bd7; scope the C5 mobile-navigation modules (`layout/navigation-dialog.tsx`, `layout/use-close-mobile-navigation-on-desktop.ts`, `layout/portal-shell.tsx`, the removed `lib/focus-trap.ts`) and their public-site and portal consumers; partial scope.

Change review: date 2026-09-20, commits `f46b6f41..HEAD` on `eduardvalentin1996/gen-193-coach-sees-her-assessment-calls-on-the-dashboard`; scope the GEN-193 coach assessment-calls slice across C1, C5, C13 and C17; partial scope. It adds U1301-U1320, moves U1242 into C5 and retires nothing; every row it touched was re-read against HEAD.

One row per module by default; ports, entity/model sets, and separate implementations get their own rows. Test files (`*.test.*`) are the outermost ring and are not mapped as units; the tests that import each module are listed in the slice returns under `.architecture/slices/`. Component IDs refer to `components.md`. "Published" means the symbol is reachable from outside its component through an export map, a route registration or a rule-sanctioned folder (`contracts/`, `ui/shared/`, `server/guards/`, `routes.ts`).

| ID | Path:symbol | Component | Kind | Ring | Visibility | Actors | Status |
|---|---|---|---|---|---|---|---|
| U100 | store/api/acquisitions/acquisitions-controller.server.ts:StoreAcquisitionController | C7 | adapter | adapters | public | visitor, vendor:cloudflare-turnstile | present |
| U101 | store/api/acquisitions/acquisitions.ts:module | C7 | framework-glue | frameworks | published (routed) | visitor | present |
| U102 | store/api/catalog/catalog-controller.server.ts:StoreCatalogController | C7 | adapter | adapters | public | visitor | present |
| U103 | store/api/catalog/catalog.ts:module | C7 | framework-glue | frameworks | published (routed) | visitor | present |
| U104 | store/api/covers/covers-controller.server.ts:StoreCoverAssetController | C7 | adapter | adapters | public | visitor | present |
| U105 | store/api/covers/covers.ts:module | C7 | framework-glue | frameworks | published (routed) | visitor | present |
| U106 | store/api/downloads/download-recovery.html:module | C7 | boundary-data | adapters | private | visitor | present |
| U107 | store/api/downloads/downloads-controller.server.ts:StoreDownloadController | C7 | adapter | adapters | public | visitor | present |
| U108 | store/api/downloads/downloads.ts:module | C7 | framework-glue | frameworks | published (routed) | visitor | present |
| U109 | store/api/management/management-controller.server.ts:StoreProductManagementController | C7 | adapter | adapters | public | operator/platform, management API caller | present |
| U110 | store/api/management/management-product-validations.ts:module | C7 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U111 | store/api/management/management-product-versions.ts:module | C7 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U112 | store/api/management/management-product.ts:module | C7 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U113 | store/api/management/management-products.ts:module | C7 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U114 | store/api/downloads/zip-stream.server.ts:ZipDeliveryStream | C7 | adapter | adapters | public | visitor | present |
| U115 | store/contracts/store-management.ts:module | C7 | boundary-data | adapters | published (contracts) | operator/platform | present |
| U116 | store/contracts/store.ts:module | C7 | boundary-data | adapters | published (contracts) | visitor | present |
| U117 | store/data/acquisitions/acquisition-repository.server.ts:PostgresStoreAcquisitionRepository | C7 | adapter | adapters | public | visitor, operator/platform | present |
| U118 | store/data/assets/asset-digest.server.ts:ProductAssetSha256Digest | C7 | adapter | adapters | public | operator/platform | present |
| U119 | store/data/assets/asset-store.server.ts:FilesystemProductAssetStore | C7 | adapter | adapters | public | visitor, operator/platform | present |
| U120 | store/data/catalog/catalog-repository.server.ts:PostgresStoreCatalogRepository | C7 | adapter | adapters | public | visitor | present |
| U121 | store/data/download-grants/download-grant-repository.server.ts:PostgresDownloadGrantRepository | C7 | adapter | adapters | public | visitor | present |
| U122 | store/data/download-grants/download-token.server.ts:RandomDownloadTokenGenerator | C7 | adapter | adapters | public | visitor | present |
| U123 | store/data/download-grants/download-token.server.ts:DownloadTokenSha256 | C7 | adapter | adapters | public | visitor | present |
| U124 | store/data/download-grants/download-token.server.ts:PayloadSha256Digest | C7 | adapter | adapters | public | visitor | present |
| U125 | store/data/publications/publication-repository.server.ts:PostgresStoreProductPublicationRepository | C7 | adapter | adapters | public | operator/platform | changed (GEN-191 fix round): names C2's `DatabaseTransaction` instead of deriving the transaction type itself |
| U126 | store/data/schema.server.ts:module | C7 | framework-glue | frameworks | published (drizzle-kit glob) | operator/platform | present |
| U127 | store/email/create-product-delivery.server.ts:createProductDelivery | C7 | adapter factory | adapters | public | operator/platform, vendor:resend | renamed from createStoreDeliveryService (D3) |
| U129 | store/email/email-product-delivery.server.ts:EmailProductDelivery | C7 | adapter | adapters | public | visitor, vendor:resend | renamed from EmailStoreDeliveryService (D3) |
| U130 | store/email/store-delivery-email-template.server.tsx:StoreDeliveryEmailTemplate | C7 | view | adapters | public | visitor | present |
| U131 | store/email/store-delivery-email.server.ts:createStoreDeliveryEmailContent | C7 | adapter | adapters | public | visitor | present |
| U132 | store/contracts/paths.ts:module (STORE_ROUTE_SEGMENT, STORE_PATH, STORE_DOWNLOAD_PATH, storeProductPath, STORE_API_PATHS) | C7 | boundary-data | adapters | published (contracts) | visitor, operator/platform | present |
| U133 | store/routes.ts:storePublicRoutes, storeApiRoutes | C7 | framework-glue | frameworks | published (registry) | operator/platform | present |
| U134 | store/server/store-composition.server.ts:composeStoreFeature (+StoreFeature, StoreFeatureHandles) | C7 | composition | composition | published (container only) | operator/platform | present |
| U135 | store/server/guards/store-context.server.ts:storeContext | C7 | framework-glue | frameworks | published (routes and loaders) | operator/platform | present |
| U136 | store/data/assets/asset-confinement.server.ts:module (isPathWithinRoot, isConfinedAsset, matchesAssetIdentity, matchesAssetDigest) | C7 | entity rule (pure) | entities | public | operator/platform | present |
| U200 | store/ui/public/api-client.ts:STORE_CATALOG_API_URL | C7 | boundary-data | adapters | public | visitor | present |
| U201 | store/ui/public/api-client.ts:STORE_ACQUISITIONS_API_PATH/URL | C7 | boundary-data | adapters | public | visitor | present |
| U202 | store/ui/public/api-client.ts:useStoreCatalogFetcher | C7 | adapter | adapters | public | visitor | present |
| U203 | store/ui/public/api-client.ts:useStoreAcquisitionFetcher | C7 | adapter | adapters | public | visitor | present |
| U204 | store/ui/public/cart/cart.ts:StoreCartState | C7 | boundary-data | adapters | published | visitor | present |
| U205 | store/ui/public/cart/cart.ts:createStoreCartStore | C7 | adapter (calls reconcileCart from C1) | adapters | published | visitor | present |
| U206 | store/ui/public/cart/cart.ts:useHydrateStoreCart | C7 | adapter | adapters | public | visitor | present |
| U207 | store/ui/public/cart/cart.ts:useReconcileStoreCartCatalog | C7 | adapter | adapters | public | visitor | present |
| U208 | store/ui/public/cart/cart.ts:selectStoreCartProducts | C7 | adapter | adapters | public | visitor | present |
| U209 | store/ui/public/cart/cart-storage.ts:module (STORE_CART_STORAGE_KEY, PersistedStoreCart, STORE_CART_PERSIST_OPTIONS) | C7 | adapter | adapters | public | visitor | present |
| U210 | store/ui/public/cart/cart-provider.tsx:StoreCartProvider | C7 | framework-glue | frameworks | published | visitor | present |
| U211 | store/ui/public/cart/cart-provider.tsx:useStoreCart | C7 | framework-glue | frameworks | published | visitor | present |
| U212 | store/ui/public/cart/cart-drawer.tsx:StoreCartButton | C7 | view | frameworks | published | visitor | present |
| U213 | store/ui/public/cart/cart-drawer.tsx:StoreCartDrawer | C7 | mixed | frameworks | published | visitor | changed (design-system fix round): its Continue, Back and send buttons are the `Button` primitive's `inverted`, `outline` and `primary` variants at the 56px `lg` size with no colour overrides, the email field is the default `Input` with no restyling, the empty-cart glyph takes `icon-muted`, a failed request shows the shared `Alert` (U1288), and it carries no focus classes of its own |
| U214 | store/ui/public/cart/cart-focus.ts:createFocusRestoreTracker | C7 | adapter (pure, tested) | adapters | public | visitor accessibility | present |
| U218 | store/ui/public/acquisition/acquisition-form.ts:useStoreAcquisition | C7 | hook over the step machine | adapters | public | visitor | present |
| U219 | store/ui/public/acquisition/acquisition-flow.ts:module (reduceAcquisitionFlow, resolveAcquisitionError, flow types) | C7 | step machine (pure, tested) | adapters | public | visitor | present |
| U221 | store/ui/public/catalog/catalog-filters.ts:STORE_FILTER_DIMENSIONS and the filter functions (collectFilterDimensions, offersAnyFilter, resolveFilterSelection, canonicalizeFilterSearchParams, resolveCanonicalFilterTarget, filterProducts, removeFilterParams, haveOnlyFilterParamsChanged) | C7 | mixed | adapters | public | visitor | present |
| U229 | store/ui/public/catalog/catalog-filter-controls.tsx:useStoreCatalogFilterFocus, StoreCatalogFilters | C7 | view | frameworks | public | visitor | present |
| U231 | store/ui/public/catalog/catalog-page.tsx:loader | C7 | adapter | adapters | published (routed) | visitor | present |
| U233 | store/ui/public/catalog/catalog-page.tsx:module (CatalogRoute, meta, shouldRevalidate, ErrorBoundary) | C7 | view | frameworks | published (routed) | visitor | present |
| U237 | store/ui/public/catalog/catalog-view.tsx:CatalogView, CatalogUnavailableView | C7 | view | frameworks | public | visitor | changed (GEN-191 fix round): the add-to-cart action is `buttonVariants` at `lg`, full width, `secondary` once the product is in the cart and `outline` before; the cards take the `card` corner and the type tags the `tile` corner. the header reads "Find the right plan" over the prototype's copy in `copy-muted`, the empty catalog is the prototype's centred column with an `icon-muted` glyph, and the catalog's bottom padding sums with the layout frame's to the prototype's; the product cards carry no focus classes of their own |
| U240 | store/ui/public/catalog/catalog-presenter.ts:presentCatalog (+CatalogPresentation) | C7 | presenter | adapters | private to the feature | visitor | present |
| U241 | store/ui/public/download/download-page.tsx:module (DownloadRoute, meta) | C7 | view | frameworks | published (routed) | visitor | changed (GEN-191 fix round): the download action is a full-width `Button` at `xl` with `lg` text and `raised` elevation, and the expired-link action is the `inverted` variant through `buttonVariants` |
| U244 | store/ui/public/download/download-state.ts:module (DOWNLOAD_API_URL, resolvePrivateDownloadToken, usePrivateDownloadToken) | C7 | adapter | adapters | public | visitor | present |
| U247 | store/ui/public/product/product-page.tsx:loader | C7 | adapter | adapters | published (routed) | visitor | present |
| U248 | store/ui/public/product/product-page.tsx:module (ProductDetailsRoute, meta) | C7 | view | frameworks | published (routed) | visitor | changed (GEN-191 fix round): the download action is a full-width `Button` at `xl` with `lg` text and `raised` elevation and no colour or shadow overrides; the image and the details card take the `card` corner and the tags the `tile` corner |
| U300 | waitlist/api/waitlist.ts:action | C8 | framework-glue | frameworks | published (routed) | visitor, operator/platform | present |
| U301 | waitlist/api/waitlist-controller.server.ts:WaitlistController | C8 | adapter | adapters | public | visitor, operator/platform | present |
| U302 | waitlist/contracts/waitlist.ts:module | C8 | boundary-data | adapters | published (contracts) | visitor | present |
| U303 | waitlist/data/repository.server.ts:PostgresWaitlistRepository | C8 | adapter | adapters | public | operator/platform | changed (b3eb2653; one READ COMMITTED transaction per signup under the per-offer advisory lock, lowest free `reduced_slot` in 1..`WAITLIST_REDUCED_PRICING_CAP`, insert inside a savepoint, and a U1211-classified constraint rejection mapped to capacity reached or already registered; no retry loop) |
| U304 | waitlist/data/schema.server.ts:waitlistEntriesTable, waitlistEntryConstraints | C8 | boundary-data | adapters | public | operator/platform | changed (b3eb2653; nullable `reduced_slot`, unique `(offer_slug, reduced_slot)`, `reduced_slot between 1 and WAITLIST_REDUCED_PRICING_CAP`, slot present exactly for reduced entries; constraint names published for U1211) |
| U305 | waitlist/email/create-waitlist-confirmation.server.ts:createWaitlistConfirmation | C8 | adapter factory | adapters | public | operator/platform, vendor:resend | renamed from createWaitlistConfirmationService (D3) |
| U307 | waitlist/email/email-waitlist-confirmation.server.ts:EmailWaitlistConfirmation | C8 | adapter | adapters | public | operator/platform, vendor:resend | renamed from EmailWaitlistConfirmationService (D3) |
| U308 | waitlist/email/waitlist-confirmation-email-template.server.tsx:WaitlistConfirmationEmailTemplate | C8 | view | adapters | public | operator/platform | present |
| U309 | waitlist/email/waitlist-confirmation-email.server.ts:createWaitlistConfirmationEmailContent | C8 | adapter | adapters | public | operator/platform | present |
| U310 | waitlist/ui/public/api-client.ts:useJoinWaitlistFetcher (+WAITLIST_API_URL) | C8 | adapter | adapters | public | visitor | present |
| U311 | waitlist/ui/public/availability-status.tsx:WaitlistAvailabilityStatus | C8 | view | adapters | published | visitor | changed (second parity round): the tone's colour sits on the paragraph itself, with an on-inverted danger colour for the closed state on the dark variant |
| U312 | waitlist/ui/public/confetti.ts:launchWaitlistConfetti | C8 | adapter | adapters | public | visitor | present |
| U313 | waitlist/ui/public/email-form.tsx:WaitlistEmailForm | C8 | view over WaitlistPresentation | adapters | published | visitor | changed (GEN-191 fix round): `WaitlistJoinedMessage` and `WaitlistConsentNotice` sit beside the form, and the submit button's text is `waitlistSubmitLabel(mode)` or, while submitting, `WaitlistSubmittingLabel`, the one part that still reads `useClientReducedMotionPreference`, to swap the spinner for text. The form wraps its motion in `MotionConfig reducedMotion="user"`; the error alert reads no reduced-motion prop. The submit button is `Button` at `lg`, semibold, with `scale` press and no colour override; the pill email field stays its own and carries no focus ring classes |
| U314 | waitlist/ui/public/errors.ts:module | C8 | adapter | adapters | public | visitor | present |
| U315 | waitlist/ui/public/submission.ts:useWaitlistSubmission | C8 | hook over the step machine | adapters | public | visitor | present |
| U317 | waitlist/ui/public/submission-flow.ts:resolveSubmissionState | C8 | step machine (pure, tested) | adapters | public | visitor | present |
| U318 | waitlist/ui/shared/waitlist-presentation.ts:presentWaitlist (+WaitlistPresentation) | C8 | presenter | adapters | published (ui/shared) | visitor | present |
| U319 | waitlist/contracts/paths.ts:WAITLIST_API_PATH | C8 | boundary-data | adapters | published (contracts) | visitor | present |
| U320 | waitlist/routes.ts:waitlistApiRoutes | C8 | framework-glue | frameworks | published (registry) | operator/platform | present |
| U321 | waitlist/server/waitlist-composition.server.ts:composeWaitlistFeature (+WaitlistFeature; module-private WaitlistFeatureHandles accepts `FeatureFlagReader`) | C8 | composition | composition | function and output published (container only); handles module-private | operator/platform | changed (48b8cafb) |
| U322 | waitlist/server/guards/waitlist-context.server.ts:waitlistContext | C8 | framework-glue | frameworks | published (route and layout loader) | operator/platform | present |
| U316 | public-site/sections/pricing/bundle-selector.tsx:BundleSelector | C11 | view | frameworks | section-private | visitor | changed (GEN-191 fix round): every price is worded by one `formatEuros`, and the secondary text takes `text-link-muted` (the `bundle-secondary` colour is gone). the "Most popular" card stacks above its neighbours (`z-10`), its folder-tab badge keeps the `compact` corner, its shadow is drawn by a Tailwind `shadow-(…)` utility over the `--ui-public-bundle-featured-shadow` property the featured-card class sets, so its shadow paints over them, the benefits heading is an `h4` under the cards' `h3`s as the prototype's is, the card composes `Save {n}%`, `€{amount}` and `Billed as €{total}` from the presenter's numbers as the prototype's markup does (since the GEN-191 final fix round every euro amount goes through the module-private `EuroAmount` and `formatEuros`, which share one sign, and the benefits panel is a `cardVariants` card) (the `PriceText` splitter is gone) and the non-featured cards take `shadow-card` |
| U323 | public-site/sections/pricing/coaching-bundles.ts:presentCoachingBundles (+CoachingBundleCard, the module-private BUNDLES and BENEFITS literals, toCard, savingsPercentOf) | C11 | presenter over a literal | adapters | section-private | visitor, operator/platform (pricing) | changed (design-system fix round): a card carries its amounts as numbers (`pricePerMonth`, `total`, the waitlist tier's `originalPricePerMonth` and `originalTotal`, `savingsPercent`) and `billedMonthly`; the wording and the euro sign are the view's (U316), so `formatPrice` and the preformatted labels are gone |
| U400 | accounts/api/account-controller.server.ts:AccountController | C9 | adapter | adapters | published | client, coach | present |
| U401 | accounts/api/account.ts:loader | C9 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U402 | accounts/api/clerk-webhooks.ts:action | C9 | framework-glue | frameworks | published (routed) | vendor:clerk, operator/platform | present |
| U403 | accounts/api/webhook-controller.server.ts:AccountWebhookController | C9 | adapter | adapters | public | vendor:clerk, operator/platform | present |
| U404 | accounts/contracts/account.ts:accountResponseSchema | C9 | boundary-data | adapters | published (contracts) | client, coach | present |
| U405 | accounts/contracts/account.ts:PublicSessionState | C9 | boundary-data | adapters | published (contracts) | visitor, client, coach | present |
| U406 | accounts/data/account-repository.server.ts:PostgresAccountRepository | C9 | adapter | adapters | public | operator/platform | present |
| U407 | accounts/data/schema.server.ts:module (accountsTable, accountRoleEnum) | C9 | adapter | adapters | public | operator/platform | present |
| U408 | accounts/server/guards/session-context.server.ts:sessionContext | C9 | framework-glue | frameworks | published (guards) | operator/platform | present |
| U409 | accounts/server/guards/session-context.server.ts:ResolvedSession (account: AccountSnapshot; default `{ kind: "anonymous" }`) | C9 | boundary-data | adapters | published (guards) | operator/platform | present |
| U410 | accounts/server/account-resolution-middleware.server.ts:createAccountResolutionMiddleware | C9 | adapter | adapters | published (root.server.ts) | operator/platform, vendor:clerk | present |
| U411 | accounts/server/guards/require-portal-access.server.ts:requirePortalAccess | C9 | adapter | adapters | published (portal layouts) | client, coach | present |
| U412 | accounts/server/guards/require-account.server.ts:requireApiAccount | C9 | adapter | adapters | published (U400) | client, coach | present |
| U413 | accounts/ui/public/auth-nav-actions.tsx:AuthNavActions | C9 | view | frameworks | published (C11) | visitor, client, coach | changed (GEN-191 fix round): `placement` is required: `header-solid`, `header-transparent` or `mobile-menu`. In the header the portal pill is `buttonVariants` at `xs` with `sm` text and `nav` lettering, `primary` on the solid header and `glass` on the transparent one, rather than classes keyed to the header's `data-appearance` |
| U414 | accounts/ui/public/sign-in-failed-page.tsx:module (SignInFailedRoute, meta) | C9 | view | frameworks | published (routed) | visitor | changed (design-system fix round): its action is `Button` in the `inverted` variant at `lg`; `DeadEndPage` takes `landmarkLabel` |
| U415 | accounts/ui/public/sign-in-failed-page.tsx:loader | C9 | adapter | adapters | published (routed) | visitor | present |
| U416 | accounts/ui/shared/access-denied-page.tsx:AccessDeniedPage | C9 | view | frameworks | published (root.tsx) | client, coach | changed (design-system fix round): its action is a router `Link` styled by `buttonVariants` in the `inverted` variant; `DeadEndPage` takes `landmarkLabel` |
| U417 | accounts/ui/shared/access-denied-page.tsx:resolveAccessDeniedRecovery | C9 | adapter | adapters | published (root.tsx) | client, coach | present |
| U418 | accounts/contracts/paths.ts:module (SIGN_IN_FAILED_PATH, CLIENT_PORTAL_ROUTE_SEGMENT, COACH_PORTAL_ROUTE_SEGMENT, the portal paths, PORTAL_PATH_BY_ROLE) | C9 | boundary-data | adapters | published (contracts) | client, coach, visitor | present |
| U419 | accounts/routes.ts:accountsDeadEndRoutes, accountsApiRoutes | C9 | framework-glue | frameworks | published (registry) | operator/platform | changed (second parity round): `accountsPublicRoutes` is renamed `accountsDeadEndRoutes`: the public site registers it outside its layout (U633) |
| U420 | accounts/server/accounts-composition.server.ts:composeAccountsFeature (+AccountsFeature, AccountsFeatureHandles) | C9 | composition | composition | published (container only) | operator/platform | present |
| U421 | accounts/server/guards/accounts-context.server.ts:accountsContext | C9 | framework-glue | frameworks | published (guards) | operator/platform | present |
| U1215 | assessment-calls/contracts/assessment-calls.ts:module (openSlotsResponseSchema, bookAssessmentCallRequestSchema, bookingSchema, the success/error/response schemas and their inferred types) | C17 | boundary-data | adapters | published (contracts) | visitor | changed (design-system fix round): `bookingSchema` no longer carries `joinPath`; the join path is built only by the confirmation emails. The error envelope is a code and a message only (D-B); a visitor time zone is at most 64 characters, is accepted when `Intl.DateTimeFormat` constructs with it, and is stored in its canonical spelling; changed (coach follow-ups): adds `COACH_ASSESSMENT_CALLS_UNAVAILABLE_STATUS` (503) and `COACH_ASSESSMENT_CALLS_UNAVAILABLE_MESSAGE`, the status and copy U1322 throws and U1323 renders, held here because both halves must produce them identically |
| U1216 | assessment-calls/contracts/paths.ts:module (BOOK_ROUTE_SEGMENT, BOOK_PATH, ASSESSMENT_CALL_API_PATHS, assessmentCallJoinPath, COACH_ASSESSMENT_CALLS_ROUTE_SEGMENT, COACH_ASSESSMENT_CALLS_PATH) | C17 | boundary-data | adapters | published (contracts) | visitor, coach | changed (GEN-193): carries the coach page's segment and path, built on C9's `COACH_PORTAL_PATH`, which it names relatively because the route-config loader resolves this module without the app alias |
| U1217 | assessment-calls/routes.ts:assessmentCallsPublicRoutes, assessmentCallsCoachRoutes, assessmentCallsApiRoutes | C17 | framework-glue | frameworks | published (registry) | operator/platform | changed (GEN-193): adds the coach fragment the coach surface spreads |
| U1218 | assessment-calls/api/assessment-calls-controller.server.ts:AssessmentCallsController (+BookingPageData; loadBookingPage, listSlots, book, resolveJoin) | C17 | adapter | adapters | public | visitor, vendor:cloudflare-turnstile | changed (design-system fix round): the booking response no longer carries `joinPath`. Verifies the Turnstile token before any domain call, serialises the domain `Date`s once into the wire shapes, turns a `closed` use-case status into a 404 and `email_already_booked` into a detail-free 409 `booking_refused` (D-B); `book` answers 500 `server_error` after logging an unexpected failure. Since the main merge it types its verifier with C6's `BotVerifier` contract (U1195) |
| U1322 | assessment-calls/api/coach-assessment-calls-controller.server.ts:CoachAssessmentCallsController (`loadCalls`) | C17 | adapter | adapters | published (container only) | coach | new (GEN-193, at a5615696; row backfilled by the coach follow-ups review, which found no row for it); changed (coach follow-ups): it maps U1321's unavailable result to a thrown 503 `Response` carrying U1215's copy, so both coach loaders stay a single forwarding line and the route boundary owns the presentation. A loaded result is still parsed through `coachAssessmentCallsSchema` with the clock's instant |
| U1219 | assessment-calls/api/slots.ts:module | C17 | framework-glue | frameworks | published (routed) | visitor | new |
| U1220 | assessment-calls/api/bookings.ts:module | C17 | framework-glue | frameworks | published (routed) | visitor | new |
| U1221 | assessment-calls/data/repository.server.ts:PostgresAssessmentCallRepository (implements `AssessmentCallReservations`) | C17 | adapter | adapters | public | visitor | changed (GEN-191 fix round): names C2's `DatabaseTransaction` and classifies `22P02` through C2's `isCausedByDatabaseError` (U1296), so its own cause-chain walk is gone; `insertCall` takes `{ id, command }`. `reserve` runs one transaction: it takes a per-email advisory lock, generates the call's id, reserves the coach's time through `reserveCoachTime` (C6 `./coach-calendar/server`, U1294) for `ASSESSMENT_CALL_RULES.coachTimeFrom(startsAt)` — the call plus its buffer — as appointment kind `assessment_call`, then hands the outcome and the address's upcoming call to `AssessmentCall.decideReservation`. `reserved` inserts the call under the pre-generated id; an email refusal after a successful reservation releases it with `releaseCoachTime` in the same transaction; `taken` answers `slot_taken`. `findById` classifies Postgres `22P02` as "no such booking", recorded in `decisions.md`; every other error is rethrown |
| U1222 | assessment-calls/data/schema.server.ts:assessmentCallsTable | C17 | framework-glue | frameworks | published (drizzle-kit glob) | operator/platform | new. Index on `(visitor_email, starts_at)`; migration `db/drizzle/0020_create_assessment_calls.sql`. The unique index on `starts_at` was dropped by `0021_create_coach_time_reservations.sql`: the coach-time reservation's exclusion constraint (U1295) is now the only double-booking guard |
| U1223 | assessment-calls/data/static-coach-availability.server.ts:StaticCoachAvailability (implements `CoachAvailabilitySource`) | C17 | adapter | adapters | public | operator/platform | new. Holds one `CoachAvailability.configure(...)` literal, built at module load, and exposes it only through `current()` |
| U1224 | assessment-calls/data/configured-meeting-room-link.server.ts:ConfiguredMeetingRoomLink (implements `MeetingRoomLink`) | C17 | adapter | adapters | public | visitor | new. Returns the one configured room for every call |
| U1225 | assessment-calls/email/create-assessment-call-notifications.server.ts:createAssessmentCallNotifications | C17 | adapter factory | adapters | public | operator/platform | new |
| U1226 | assessment-calls/email/email-assessment-call-notifications.server.ts:EmailAssessmentCallNotifications (+EmailAssessmentCallNotificationsOptions; implements `AssessmentCallNotifications`) | C17 | adapter | adapters | public | visitor, vendor:resend | new. Sends the visitor and coach mails concurrently with `Promise.allSettled` and reports `sent`/`failed` per recipient, a rejected send counting as `failed`; the `.ics` names the product contact address (`contactEmail`) as organizer. The coach notification is sent with the visitor's address as its per-send `replyTo`, so the coach's reply reaches the visitor as the email says; the visitor confirmation sets none and keeps the configured `PRODUCT_EMAIL_REPLY_TO`. The API integration rig's `SentEmail` reads it back from the wire field `reply_to`. Since the main merge it and its factory take `ProductEmail`, `ProductEmailCommand` and `EmailAttachment` from C6's `./email/server` contract (U1196) |
| U1227 | assessment-calls/email/visitor-confirmation-email.server.ts:createVisitorConfirmationEmailContent (+VisitorConfirmationEmailContent) | C17 | adapter | adapters | public | visitor | new |
| U1228 | assessment-calls/email/visitor-confirmation-email-template.server.tsx:VisitorConfirmationEmailTemplate (+VisitorConfirmationEmailViewModel) | C17 | view | adapters | public | visitor | new |
| U1229 | assessment-calls/email/coach-notification-email.server.ts:createCoachNotificationEmailContent (+CoachNotificationEmailContent) | C17 | adapter | adapters | public | operator/platform | new |
| U1230 | assessment-calls/email/coach-notification-email-template.server.tsx:CoachNotificationEmailTemplate (+CoachNotificationEmailViewModel) | C17 | view | adapters | public | operator/platform | new |
| U1231 | assessment-calls/email/assessment-call-email-actions.server.tsx:AssessmentCallEmailActions (+ASSESSMENT_CALL_ACTION_COPY) | C17 | view | adapters | public | visitor, operator/platform | new |
| U1232 | assessment-calls/email/assessment-call-email-styles.server.ts:module (the email stylesheet as `CSSProperties` constants) | C17 | boundary-data | adapters | public | visitor | new |
| U1233 | assessment-calls/email/assessment-call-schedule.server.ts:formatCallMoment | C17 | utility | adapters | — | — | removed (87facabb; the one call-moment formatter is U1278 in `contracts/`, shared with the browser half) |
| U1234 | assessment-calls/email/calendar-invite.server.ts:buildIcs, buildGoogleCalendarUrl | C17 | adapter | adapters | public | visitor, operator/platform | new. Both read `endsAt` off the snapshot rather than re-applying the duration rule. The `.ics` keeps `METHOD:PUBLISH`, carries an `ORGANIZER` taken from its options, and escapes `\r\n`, bare `\r` and bare `\n` in text values |
| U1235 | assessment-calls/server/assessment-calls-composition.server.ts:composeAssessmentCallsFeature (+AssessmentCallsFeature, AssessmentCallsFeatureHandles) | C17 | composition | composition | published (container only) | operator/platform | changed (main merge): takes the container's `featureFlags: FeatureFlagReader` and `incidents: AssessmentCallIncidents` in place of `bookingOpen` and `logger`, builds one `AssessmentCallBookingWindow` (U1300) for both use cases, and types `botVerifier` and `productEmail` with C6's `BotVerifier` and `ProductEmail` contracts. Synchronous: no handle it builds needs awaiting. The config handle is `assessmentCallsConfig`. Builds `PostgresCoachCalendar` (U1293) from the database handle for `ListOpenSlotsUseCase`; changed (coach follow-ups): passes the incidents handle to U1321 as well, so the coach listing reports its own read failures |
| U1236 | assessment-calls/server/guards/assessment-calls-context.server.ts:assessmentCallsContext | C17 | framework-glue | frameworks | published (routes and loaders) | operator/platform | new |
| U1237 | assessment-calls/ui/public/book/book-page.tsx:loader (+shouldRevalidate, meta, handle) | C17 | adapter | adapters | published (routed) | visitor | new. Reads only `assessmentCallsContext`; a slot-loading failure renders the unavailable state rather than a 500 (D21). `handle.publicContentFrame` is `"full-bleed"`, which the public-site layout (U606) reads through `useMatches`; that route handle crosses no import |
| U1238 | assessment-calls/ui/public/book/book-page.tsx:AssessmentCallBookingRoute (+the module-private BookingFlow and SlotSelectionStep) | C17 | view | frameworks | published (routed) | visitor | changed (GEN-191 fix round): imports `book-page.css` (U1298), whose entrance animates the first step's panel, and its buttons are full-width semibold `Button`s. A two-panel card under `MotionConfig reducedMotion="user"`: `CallOverview` (U1282), which holds the page's `h1`, beside the step panel. `BookingFlow` owns the step machine through `useReducer` and swaps the steps inside `AnimatePresence mode="wait"`; `SlotSelectionStep` groups the open slots by display-zone day and renders `SlotPicker` (U1283) or the unavailable state under a visually hidden `h2`. `BookingFlow` takes the heading callback ref from `useStepHeadingFocus` (U1281) and hands it to whichever step it renders; each step's `h2` is programmatically focusable (`tabIndex={-1}`) |
| U1298 | assessment-calls/ui/public/book/book-page.css:module | C17 | framework-glue | frameworks | feature-private | visitor | new (GEN-191 fix round). `ui-booking-first-step-entrance`: the first step panel's 20px slide and fade when the page loads, as keyframes in the `components` layer |
| U1239 | assessment-calls/ui/public/book/api-client.ts:module (SLOTS_API_URL, BOOKINGS_API_URL, useBookAssessmentCallFetcher, useRefreshSlotsFetcher) | C17 | adapter | adapters | public | visitor | new |
| U1240 | assessment-calls/ui/public/book/booking-flow.ts:module (reduceBookingFlow, INITIAL_BOOKING_FLOW, BookingFlowState, BookingFlowEvent, BookingClientError, BookingDetails) | C17 | step machine (pure, tested) | adapters | public | visitor | new. The state carries the selected day and a `details` object (full name, email, notes) as well as the step, the selected slot, the booking and the error. Both `submit` and `show-slots` set `details`, so what the visitor typed survives her going back to the times and a slot taken while she filled the form. `select-day` clears the selected slot, so a time chosen on one day never survives a move to another. A `slot_unavailable` response returns to the slot step and clears the selected slot and the selected day |
| U1241 | assessment-calls/ui/public/book/slot-grouping.ts:groupSlotsByDay | C17 | adapter (pure, tested) | adapters | public | visitor | changed (GEN-193): `dayKeyOf` moved to `ui/shared/day-key.ts` (U1301) with the `@date-fns/tz` import, and the calendar reads it from there; this module only regroups instants by that key. It words nothing |
| U1242 | packages/ui/src/lib/use-display-time-zone.ts:useDisplayTimeZone | C5 | adapter | adapters | published (`./lib`) | visitor, coach | changed (GEN-193): moved out of `assessment-calls/ui/public/book/` into the design system, because it is actor-agnostic and names nothing but React. Server-renders in the zone its caller passes — the coach's for both coach surfaces — and switches to the browser's resolved zone after hydration |
| U1243 | assessment-calls/ui/public/book/submission.ts:useBookAssessmentCallSubmission (+BookAssessmentCallSubmission) | C17 | hook over the step machine | adapters | public | visitor | new |
| U1244 | assessment-calls/ui/public/book/slot-calendar.tsx:SlotCalendar | C17 | view | frameworks | feature-private | visitor | changed (GEN-191 fix round): a day's accessible name leads with "Today" on the current day, as the vendor's default name does, then the day (`formatDayFirstDate`, U1278), then "Past day" or "No open slots", then "selected". Renders the calendar alone; navigation is open-ended and every day without an open slot is disabled, and the calendar opens on the selected day's month, else on the first open slot's |
| U1245 | assessment-calls/ui/public/book/slot-list.tsx:SlotList | C17 | view | frameworks | — | — | removed (booking-card rebuild; the day's times are U1283) |
| U1246 | assessment-calls/ui/public/book/booking-details-form.tsx:BookingDetailsForm | C17 | mixed | frameworks | feature-private | visitor, vendor:cloudflare-turnstile | changed (GEN-191 fix round): the back control is `IconButton` in the `soft` variant, the fields are the default `Input` and `Textarea` with only an icon inset or height at the call site, a failed booking shows the shared `Alert` (U1288), and the submit button is a full-width semibold `Button`. A rejected submission moves focus to the first rejected field through react-hook-form's default error focus |
| U1247 | assessment-calls/ui/public/book/booking-confirmation.tsx:BookingConfirmation | C17 | view | frameworks | feature-private | visitor | changed (GEN-191 fix round): words the call through U1278 (`formatMonthFirstDate`, `formatClockTime`, `nameTimeZone`), and its one action is a router `Link` styled by `buttonVariants` in the `outline` variant with semibold `sm` text and no call-site classes. Shows no join link; the booking response carries none |
| U1248 | assessment-calls/ui/public/book/unavailable-slots.tsx:UnavailableSlots | C17 | view | frameworks | feature-private | visitor | changed (GEN-191 fix round): the notice is the shared `Alert` (U1288) and the retry is a full-width semibold `Button` |
| U1249 | assessment-calls/ui/public/join/join-page.tsx:loader | C17 | adapter | adapters | published (routed) | visitor | new. Throws a 302 redirect to the room for a known id and a 404 `Response` for a missing, unknown or malformed one; the root `ErrorBoundary` renders the 404 as the standard "Page not found" page |
| U1250 | assessment-calls/ui/public/join/join-page.tsx:AssessmentCallJoinRoute | C17 | framework-glue | frameworks | published (routed) | visitor | new. Returns `null`: the loader always throws, so it never renders; it exists because a route module without a default export would be registered as a resource route. The feature's own unavailable page and its copy were deleted in the remediation round |
| U1278 | assessment-calls/contracts/call-moment.ts:formatCallMoment, formatDayFirstDate, formatMonthFirstDate, formatMonthFirstDay, formatClockTime, nameTimeZone | C17 | utility (pure, tested) | adapters | published (contracts) | visitor, operator/platform | changed (GEN-191 fix round): each formatter is named for the wording it produces, and every wording names its locale, so the server and the browser render a call identically: `formatCallMoment` (the email line), `formatDayFirstDate` (en-GB, "Monday, 2 March 2026": the email's day and the calendar's accessible day names), `formatMonthFirstDate` (en-US, the booking page's date), `formatMonthFirstDay` (en-US, the slot picker's day heading), `formatClockTime` (en-US, "5:00 PM", shared by the emails and the booking page) and `nameTimeZone`. Every function takes `(instant, timeZone)`; one `Intl.DateTimeFormat` per wording and zone is cached for the life of the process. It sits in `contracts/` because that is the one browser-safe folder both halves may import |
| U1279 | assessment-calls/ui/public/book/call-facts.tsx:CallFacts | C17 | view | frameworks | — | — | removed (booking-card rebuild; the call's facts are U1282) |
| U1281 | assessment-calls/ui/public/book/step-heading-focus.ts:useStepHeadingFocus | C17 | adapter | adapters | feature-private | visitor | changed (design-system fix round): effect-based: the returned callback ref stores the mounted heading in state, and an effect focuses any heading that mounts after the first, so no ref is written during render. The next step's heading still takes focus when it mounts after the leaving step's exit under `AnimatePresence mode="wait"`, never on first load. The step headings carry `scroll-mt-24` at the call site |
| U1282 | assessment-calls/ui/public/book/call-overview.tsx:CallOverview | C17 | view | frameworks | feature-private | visitor | changed (GEN-191 final fix round: the chosen call's card is a `cardVariants` card; design-system fix round): the coach's photo is the owner's portrait served from the app's own `public/media/eli/` (`ELI_PORTRAIT_PATHS` in `@eli-coach-platform/content`) at 96px with width and height set; the page's `h1`, the call's facts and, on the details step, the chosen call's date and time (U1278) |
| U1283 | assessment-calls/ui/public/book/slot-picker.tsx:SlotPicker | C17 | view | frameworks | feature-private | visitor | changed (GEN-191 fix round): words the day heading, times and zone through U1278 (`formatMonthFirstDay`, `formatClockTime`, `nameTimeZone`); `useScrollDaySlotsIntoView` reveals the chosen day's times, and the small-screen "Change date" button takes `linkVariants` in the `inline` variant, semibold. The times are `aria-pressed` buttons on the `control` corner, the chosen one on `surface-strong` with `shadow-card` |
| U500 | server/container.server.ts:createPlatformContainer (constructs and shares the feature-flag repository and reader) | C14 | composition-root | composition | published (root.server.ts) | operator/platform | changed (f5d1889c) |
| U501 | server/container.server.ts:getPlatformContainer | C14 | composition-root | composition | published (root.server.ts) | operator/platform | present |
| U502 | server/container.server.ts:PlatformContainer (`{ accounts, closeDatabase, platform, store, waitlist }`) | C14 | boundary-data | composition | published | operator/platform | present |
| U503 | server/database.server.ts:createPlatformDatabase | C14 | adapter | frameworks | published | operator/platform | present |
| U504 | server/database.server.ts:DatabaseClosedError | C14 | adapter | frameworks | published | operator/platform | present |
| U505 | server/database.server.ts:PlatformDatabase | C14 | boundary-data | frameworks | published | operator/platform | present |
| U510 | server/runtime-environment.server.ts:getRuntimeEnvironment | C14 | adapter | frameworks | published (container only) | operator/platform | present |
| U511 | server/api/meta/app-metadata-controller.server.ts:AppMetadataController | C14 | adapter | adapters | published | operator/platform | present |
| U512 | server/api/meta/service-metadata.ts:appMetadataSchema | C14 | boundary-data | adapters | published | operator/platform | present |
| U513 | server/api/feature-flags/feature-flags.ts:module (action, loader) | C14 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U514 | server/api/meta/meta.ts:loader | C14 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U515 | server/api/readyz/readyz.ts:loader | C14 | framework-glue | frameworks | published (routed) | operator/platform | present |
| U516 | server/api/readyz/readyz-controller.server.ts:ReadyzController | C14 | adapter | adapters | published | operator/platform | present |
| U517 | server/feature-contexts.server.ts:createFeatureContextMiddleware | C14 | composition | composition | published (root.server.ts) | operator/platform | present |
| U518 | server/platform-composition.server.ts:composePlatformFeature (+PlatformFeature, PlatformControllers, RuntimeConfig; module-private PlatformFeatureHandles accepts `FeatureFlagReader`) | C14 | composition | composition | function and output types published (container only); handles module-private | operator/platform | changed (48b8cafb; feature-flag construction remains in U500) |
| U519 | server/logger.server.ts:createConsoleLogger (+private ConsoleLogger) | C14 | adapter | adapters | container-only | operator/platform | altered in a79f507d; structurally implements U1193 and U1194; changed (242a0976; adds `waitlistModeReadFailed`); changed (main merge into GEN-191; also implements U1299 with `bookingModeReadFailed`, `notificationFailed` and `slotsReadFailed`, logged under `assessment_call_booking_mode_read_failure`, `assessment_call_notification_failure` and `assessment_call_slots_failure`); changed (coach follow-ups; adds `callsReadFailed` under `assessment_call_listing_failure`, so every incident the assessment-call use cases raise now has a category) |
| U530 | server/guards/platform-context.server.ts:platformContext | C14 | framework-glue | frameworks | published (server/api only) | operator/platform | present |
| U531 | server/guards/runtime-config-context.server.ts:runtimeConfigContext | C14 | framework-glue | frameworks | published (public-site layout loader) | operator/platform | present |
| U532 | server/api/routes.ts:platformApiRoutes | C14 | framework-glue | frameworks | published (registry) | operator/platform | present |
| U533 | server/api/feature-flags/feature-flags-contract.ts:module (featureFlagContextSchema, featureFlagSnapshotSchema, FeatureFlagContext, FeatureFlagSnapshot) | C14 | boundary-data | adapters | published | operator/platform | present |
| U534 | server/api/feature-flags/feature-flags-controller.server.ts:FeatureFlagController | C14 | adapter | adapters | published | operator/platform | present |
| U535 | server/test-support/request-args.ts:createRequestArgs (+contextEntry, ContextEntry) | C14 | test helper | tests | test-only (no production importer; excluded from `no-orphans` by exact path) | operator/platform | present |
| U520 | root.tsx:module (Root, Layout, ErrorBoundary, meta, links) | C15 | mixed | frameworks | published (routed) | visitor, client, coach | present |
| U521 | root.server.ts:module (middleware, loader) — Clerk, `createFeatureContextMiddleware(getPlatformContainer)`, `createAccountResolutionMiddleware()`; the container's only importer | C15 | composition-root | composition | published | operator/platform | present |
| U522 | root-error-page.tsx:RootErrorPage | C15 | view | frameworks | published | visitor, client, coach | changed (design-system fix round): its action is a router `Link` styled by `buttonVariants` in the `inverted` variant; `DeadEndPage` takes `landmarkLabel` |
| U523 | routes.ts:module — a concatenation of seven route fragments, no path literals | C15 | framework-glue | frameworks | published | operator/platform | present |
| U524 | app.css:module | C15 | framework-glue | frameworks | published | operator/platform | present |
| U525 | types/canvas-confetti.d.ts:module | C15 | framework-glue | frameworks | private | operator/platform | present |
| U526 | apps/platform/db/drizzle.config.ts:module | C15 | framework-glue | frameworks | published (CLI) | operator/platform | present |
| U527 | apps/platform/vite.config.ts:module | C15 | framework-glue | frameworks | published (build) | operator/platform | present |
| U528 | apps/platform/react-router.config.ts:module | C15 | framework-glue | frameworks | published (build) | operator/platform | present |
| U600 | public-site/shell/logo.tsx:Logo | C11 | view | frameworks | public | operator/platform | changed (GEN-191 fix round): takes the header's `appearance` (U1297) in place of `isSolid`; the mark and the focus outline take the `tile` corner |
| U601 | public-site/shell/public-navigation.tsx:PublicNavigation | C11 | view | frameworks | public | visitor | changed (GEN-191 fix round): derives one `PublicHeaderAppearance` (U1297) from the scroll behaviour, the scroll position and the menu state (and, since the final fix round, marks the header `data-surface="inverted"` while it is transparent, so its controls take the inverted focus ring), and hands it to `Logo` and to `actions`, now a render prop `(appearance) => ReactNode`; the header no longer scopes its descendants with `group`. The small-screen menu's links are motion links and a divider separates them from the actions. the small-screen menu toggle is `IconButton` in the `plain` variant (8px padding, no border, corner or hover colour), as the prototype's toggle is; the scrolled bar takes `shadow-card`. Since the main merge it composes C5's `NavigationDialog` (U852), passing `menuButtonVariant="plain"` for that toggle, and keeps the branch's visuals: the appearance, the `actions(appearance)` render function, the inverted surface while transparent, the divider and actions group and the spring motion |
| U602 | public-site/shell/public-footer.tsx:PublicFooter | C11 | view | frameworks | public | visitor | changed (design-system fix round): the footer paints no background of its own, so the layout's shows through as it does in the prototype |
| U603 | public-site/shell/public-layout.tsx:PublicLayout (takes `WaitlistPresentation`; +PublicContentFrame) | C11 | view | frameworks | public | visitor, operator/platform | changed (GEN-191 fix round): renders `AuthNavActions` through the navigation's `actions` render prop, mapping the header's appearance to the `header-solid` or `header-transparent` placement. the root paints `surface-subtle` under the homepage's hero-overlay frame and `surface-page` under the solid frame, as the prototype's root and pages do, and `main` is at least one screen tall. Takes a `contentFrame`, `padded` or `full-bleed`; a full-bleed page drops the `main` element's centred, padded frame |
| U1297 | public-site/shell/header-appearance.ts:PublicHeaderAppearance | C11 | boundary-data | frameworks | public | visitor | new (GEN-191 fix round). `solid \| transparent`, the public header's look: `PublicNavigation` derives it, `Logo` reads it, and `PublicLayout` maps it to `AuthNavActions`' placement |
| U604 | public-site/shell/layout.server.ts:loader — `presentWaitlist(...)`; bot detection and appBasePath from `runtimeConfigContext`, session from `sessionContext`, waitlist from `waitlistContext` | C11 | adapter | adapters | published (routed via layout.tsx) | visitor, operator/platform | present |
| U605 | public-site/shell/layout.server.ts:PublicLayoutLoaderData | C11 | boundary-data | adapters | public | visitor | present |
| U606 | public-site/shell/layout.tsx:module (PublicLayoutRoute, shouldRevalidate, PublicOutletContext) | C11 | mixed | frameworks | published (routed) | visitor | present; picks the content frame from any matched route's `handle.publicContentFrame` through `useMatches`, `padded` unless a route asks for `full-bleed`; the booking page (U1237) and the pricing page (U626) ask |
| U609 | public-site/sections/about/about-content.ts:module | C11 | boundary-data | adapters | public | operator/platform | changed (design-system fix round): `ABOUT_MEDIA` adds Eli's large and small portraits, built from `ELI_PORTRAIT_PATHS` under the app's base path |
| U610 | public-site/sections/about/instagram-story-widget.tsx:InstagramStoryWidget | C11 | view | frameworks | public | visitor | changed (design-system fix round): the profile avatar is Eli's small portrait at 32px, with width and height set |
| U611 | public-site/sections/about/about.tsx:PublicAbout | C11 | view | frameworks | public | visitor, operator/platform | changed (design-system fix round): the portrait is Eli's large portrait (no longer the hero video's poster) with width and height set, the credentials take `text-label`, and the call to action is `buttonVariants` at the default size with `raised` elevation and `scale` press (GEN-191 fix round) |
| U612 | public-site/sections/cycle-nutrition/cycle-nutrition-content.ts:module | C11 | mixed | adapters | public | operator/platform | changed (GEN-191 fix round): `getCycleNutritionViewState` is split into `getScrollingCycleViewState(progress)` and `getAnchoredCycleViewState(progress)`, the reduced-motion reading that holds the phase's anchor day; the section picks one by the reduced-motion preference |
| U613 | public-site/sections/cycle-nutrition/cycle-nutrition.css:module | C11 | framework-glue | frameworks | private | operator/platform | changed (design-system fix round): owns the section's one-caller values: the pill and centre-disc shadows as section custom properties that the elements' Tailwind `shadow-(…)` utilities read (so the computed shadow list matches the prototype's arbitrary shadow utilities), and the cue size; the phase size left it for the `public-cycle-phase` text token (GEN-191 fix round); the wheel no longer sets `will-change` |
| U614 | public-site/sections/cycle-nutrition/cycle-nutrition.tsx:PublicCycleNutrition | C11 | view | frameworks | public | visitor | changed (GEN-191 fix round): picks `getScrollingCycleViewState` or `getAnchoredCycleViewState` (U612) by the reduced-motion preference, and the phase heading is `text-4xl`, `public-cycle-phase` from `md`. the section is exactly 250vh tall, the centre disc ignores the pointer as the prototype's does, the class names no stylesheet read are gone, and the cue size and the shadows come from U613; the "Days 17–28" range stays as visually hidden text |
| U615 | public-site/sections/footer-cta/footer-cta.tsx:PublicFooterCta | C11 | view (re-derives the closed/unavailable/open copy branch) | frameworks | public | visitor, operator/platform | changed (design-system fix round): `FooterCtaContent` owns the in-view reveal and propagates `hidden`/`visible` variant labels to the actions, under `MotionConfig reducedMotion="user"`; no reveal or reduced-motion flag is passed down. The sheet's scroll parallax is still off for reduced motion. The two links are `buttonVariants` at the default size with `regular` weight, `primary` with `raised` elevation and `outline-brand`, both with `scale` press (GEN-191 fix round) |
| U617 | public-site/sections/hero/hero.tsx:PublicHero | C11 | view (re-derives the copy branch) | frameworks | public | visitor, operator/platform | changed (GEN-191 fix round): each entrance is `heroEntranceMotion({ transition, riseDistance })` and `useShouldLoadHeroVideo()` reads the reduced-motion preference itself; the supporting copy takes `text-inverted-secondary`. the entrance motion runs under `MotionConfig reducedMotion="user"` with plain motion props (`getHeroPanelMotionProps` and the reduced-motion flag are gone; the flag still gates the video). The section declares `data-surface="inverted"` for the lighter focus ring, the heading sizes are `display-md` and `4xl`, and the call to action is `buttonVariants` with `caps` lettering, semibold `sm` text, `raised` elevation and `scale` press |
| U619 | public-site/sections/legal/legal-document-view.tsx:LegalDocumentView (renders `effectiveDateLabel` inside a `<time dateTime=…>`) | C11 | view | frameworks | public | visitor | present |
| U620 | public-site/sections/legal/legal-nav.tsx:LegalNav | C11 | view | frameworks | public | visitor | present |
| U621 | public-site/sections/my-method/my-method.tsx:PublicMyMethod | C11 | view | frameworks | public | visitor | changed (design-system fix round): reveals on the shared `publicSectionRevealViewport` (U804); the heading takes `leading-tight` and the axis labels `text-caption leading-normal` |
| U622 | public-site/sections/platform/platform-content.ts:module | C11 | boundary-data | adapters | public | operator/platform | changed (second parity round): gained `MACRO_SPLIT`, and a capability's icon is typed as lucide's `LucideIcon` rather than a local React component type |
| U623 | public-site/sections/platform/platform.tsx:PublicPlatform | C11 | view | frameworks | public | visitor | changed (design-system fix round): reveals on the shared `publicSectionRevealViewport` (U804); the heading takes `leading-tight`, the phone body copy `text-caption`, the cards `shadow-card`, and the cloud icon the `control` corner |
| U624 | public-site/sections/workouts/workouts.tsx:PublicWorkouts (calls `resolveSwipeIntent`) | C11 | view | frameworks | public | visitor | changed (design-system fix round): reveals on the shared `publicSectionRevealViewport` (U804); the day tiles take the `tile` corner |
| U625 | public-site/pages/home.tsx:HomeRoute (+meta) | C11 | view | frameworks | published (routed) | visitor | present |
| U626 | public-site/pages/pricing.tsx:PricingRoute (+meta, handle) — calls `presentCoachingBundles` and passes cards, benefits and `showsWaitlistPricing` to `BundleSelector` | C11 | view | frameworks | published (routed) | visitor, operator/platform | changed (design-system fix round): the intro paragraph has no max width of its own, as the prototype's has not, and the assessment-call link is `buttonVariants` at `lg` with `raised` elevation. `handle.publicContentFrame` is `"full-bleed"` |
| U629 | public-site/pages/blog.tsx:BlogRoute (+meta) | C11 | view | frameworks | published (routed) | visitor | present |
| U630 | public-site/pages/privacy.tsx:PrivacyRoute (+meta) | C11 | view | frameworks | published (routed) | visitor | present |
| U631 | public-site/pages/terms.tsx:TermsRoute (+meta) | C11 | view | frameworks | published (routed) | visitor | present |
| U632 | public-site/paths.ts:PRICING_PATH | C11 | boundary-data | adapters | public | visitor | present |
| U633 | public-site/routes.ts:publicSiteRoutes | C11 | framework-glue | frameworks | published (registry) | operator/platform | changed (second parity round): spreads the accounts feature's `accountsDeadEndRoutes` after the layout route instead of inside it |
| U634 | public-site/sections/workouts/swipe-intent.ts:resolveSwipeIntent | C11 | entity rule (pure, tested) | entities | public | visitor | present |
| U700 | client-portal/api/manifest.ts:loader | C12 | adapter | adapters | published (routed) | client, operator/platform | present |
| U701 | client-portal/api/readyz.ts:loader | C12 | adapter | adapters | published (routed) | operator/platform | present |
| U702 | client-portal/api/sw.ts:loader | C12 | adapter | adapters | published (routed) | client, operator/platform | present |
| U703 | client-portal/api/service-worker.js:module | C12 | framework-glue | frameworks | private (loaded via `?raw` import) | client, operator/platform | present |
| U704 | client-portal/pages/home.tsx:ClientHomeRoute | C12 | view | frameworks | published (routed) | client | present |
| U705 | client-portal/shell/layout.server.ts:middleware | C12 | framework-glue | frameworks | published (re-exported by `layout.tsx`, routed) | client, operator/platform | present |
| U706 | client-portal/shell/layout.tsx:ClientLayoutRoute (+middleware re-export) | C12 | mixed | frameworks | published (routed) | client | present |
| U708 | client-portal/shell/navigation-links.ts:clientSurfaceLinks | C12 | boundary-data | adapters | public | client | present |
| U717 | client-portal/routes.ts:clientPortalRoutes | C12 | framework-glue | frameworks | published (registry) | operator/platform | present |
| U1301 | assessment-calls/ui/shared/day-key.ts:dayKeyOf | C17 | adapter (pure, tested) | adapters | published (`ui/shared/`) | visitor, coach | new (GEN-193). The feature's one calendar-day key, moved out of `ui/public/book/slot-grouping.ts` so the coach listing and the booking calendar read the same rule; the one module that names `@date-fns/tz` |
| U1302 | assessment-calls/ui/coach/assessment-call-listing.ts:module (classifyCalls, filterCalls, orderCalls, upcomingCalls, countCallsLeftToday, parseStatusParam, parsePageParam, pageOfCalls, paginationSteps, haveOnlyListingParamsChanged, STATUS_PARAM, QUERY_PARAM, PAGE_PARAM, PAGE_SIZE) | C17 | adapter (pure, tested) | adapters | feature-private | coach | new (GEN-193). The browser's mirror of `AssessmentCall.hasEnded`: a call is past from the instant it ends. Today is the start's calendar day in the active zone; upcoming runs soonest first and past most recent first, All being the two groups in that order. The pipeline is classify, order, filter, page, ten to a page, a page past the end clamped to the last. The count the greeting reads covers today's calls that have not ended |
| U1303 | assessment-calls/ui/coach/use-coach-clock.ts:useCoachClock (+CoachClock) | C17 | adapter | adapters | feature-private | coach | new (GEN-193). The first render reads the loader's instant in the coach's zone, the browser takes over with its own on mount, and nothing ticks afterwards; composes U1242 |
| U1304 | assessment-calls/ui/coach/join-call-link.tsx:JoinCallLink | C17 | view | frameworks | feature-private | coach | new (GEN-193). The one "Join call" affordance both coach surfaces render, an inverted `Button` recipe on a router link |
| U1323 | assessment-calls/ui/coach/assessment-calls-error-boundary.tsx:AssessmentCallsErrorBoundary (+the module-private AssessmentCallsUnavailable) | C17 | view | frameworks | published (re-exported as each coach route's `ErrorBoundary`) | coach | new (coach follow-ups). Renders U1215's copy on a 503 and rethrows anything else, so an unrelated error still reaches the root boundary. The view is U1325's body inside a `portal-panel` card inside U1324's frame, and carries `data-parity-root="AssessmentCallsUnavailable"`; it owns the page's one `h1`, because the boundary replaces the route's own heading. The view stays module-private: the boundary is the only published way to reach it |
| U1324 | assessment-calls/ui/coach/coach-calls-page-frame.ts:COACH_CALLS_PAGE_FRAME_CLASS_NAME | C17 | style constant | frameworks | feature-private | coach | new (coach follow-ups). The coach calls page's container, read by U1306 and U1323 so the dead end fills exactly the frame the page it replaces fills |
| U1305 | assessment-calls/ui/coach/assessment-calls/assessment-calls-page.tsx:loader (+shouldRevalidate, meta) | C17 | adapter | adapters | published (routed) | coach | new (GEN-193). Reads only `assessmentCallsContext`; declines revalidation when only `status`, `q` or `page` changed, because the browser answers those from the list the loader already carried; changed (coach follow-ups): unchanged in body, but the module now also re-exports U1323 as its `ErrorBoundary`, which is what turns the controller's 503 into the dead end |
| U1306 | assessment-calls/ui/coach/assessment-calls/assessment-calls-page.tsx:CoachAssessmentCallsRoute | C17 | view | frameworks | published (routed) | coach | new (GEN-193). The page's `h1` alone in the `data-parity-root="CoachAssessmentCallsHeader"` header, then the section; changed (coach follow-ups): its container is U1324 rather than a literal, so the page and its dead end share one frame |
| U1307 | assessment-calls/ui/coach/assessment-calls/assessment-calls-section.tsx:AssessmentCallsSection (+the module-private CallList and CallCard) | C17 | view | frameworks | feature-private | coach | new (GEN-193). `data-parity-root="AssessmentCallsSection"`. A `portal-panel` card holding the search field, the four segmented tabs and one `AppointmentCard` per call; `status`, `q` and `page` are written with replace, the default status and an empty search are kept out of the URL, and a filter or search change drops the page |
| U1308 | assessment-calls/ui/coach/assessment-calls/call-list-pager.tsx:CallListPager | C17 | view | frameworks | feature-private | coach | new (GEN-193). The "Showing a-b of n" line and the `Pagination` primitive, rendered only when there is more than one page |
| U1327 | assessment-calls/ui/coach/assessment-calls/use-call-listing-params.ts:useCallListingParams (+CallListingParams) | C17 | adapter | adapters | feature-private | coach | new (GEN-193, at a5615696; row backfilled by the coach follow-ups review, which found no row for it). Reads the listing's status, query and page off the URL and writes them back through C5's `useSearchParamsWriter`, and builds the pager's hrefs; it is the module that makes the calls page answer a filter without the server |
| U1309 | assessment-calls/ui/coach/dashboard/coach-greeting.tsx:CoachGreeting | C17 | view | frameworks | feature-private | coach | new (GEN-193). `data-parity-root="CoachGreeting"`; the `h1` and the calls sentence, whose count span carries `data-parity="today-count"` |
| U1310 | assessment-calls/ui/coach/dashboard/upcoming-calls-widget.tsx:UpcomingCallsWidget | C17 | view | frameworks | feature-private | coach | new (GEN-193). `data-parity-root="UpcomingCallsWidget"`; up to three `DashboardAppointmentRow`s, an empty state centred in the list area, and the "View all calls" link pinned to the card's bottom edge |
| U709 | coach-portal/api/readyz.ts:loader | C13 | adapter | adapters | published (routed) | operator/platform | present |
| U710 | coach-portal/pages/home.tsx:CoachHomeRoute | C13 | view | frameworks | published (routed) | coach | changed (GEN-193): the placeholder `AppShell` is gone. The page classifies the loaded calls once against `useCoachClock` and composes C17's `CoachGreeting` and `UpcomingCallsWidget` in the prototype's grid slot; the dashboard stays in the surface under the recorded page-home exception; changed (coach follow-ups): the module re-exports U1323 as its `ErrorBoundary`, the same one the calls page re-exports, so a failed listing read renders one dead end on both coach routes |
| U1311 | coach-portal/pages/home.tsx:loader (+meta) | C13 | adapter | adapters | published (routed) | coach | new (GEN-193). Reads `coachAssessmentCalls.loadCalls()` off `assessmentCallsContext`, the same read the calls page makes; changed (coach follow-ups): the read can now throw a 503 `Response` from U1322, which the route's re-exported boundary catches; the loader itself still only forwards |
| U711 | coach-portal/shell/layout.server.ts:middleware | C13 | framework-glue | frameworks | published (re-exported by `layout.tsx`, routed) | coach, operator/platform | present |
| U712 | coach-portal/shell/layout.tsx:CoachLayoutRoute (+middleware re-export) | C13 | mixed | frameworks | published (routed) | coach | present |
| U716 | coach-portal/shell/navigation-links.tsx:coachSurfaceLinks | C13 | boundary-data | adapters | public | coach | changed (GEN-193): gains the "Assessment calls" entry, the Video glyph, after Dashboard; its href comes from C17's `contracts/paths.ts` |
| U718 | coach-portal/routes.ts:coachPortalRoutes | C13 | framework-glue | frameworks | published (registry) | operator/platform | changed (GEN-193): spreads C17's `assessmentCallsCoachRoutes` inside the shell after the index, so the coach page sits under the portal's access middleware |
| U801 | packages/ui/src/lib/constants.ts:MAIN_CONTENT_ID | C5 | utility | frameworks | published (`./lib`) | operator/platform | present |
| U802 | packages/ui/src/styles.css:module | C5 | boundary-data | frameworks | published (`./styles.css`) | operator/platform | changed (GEN-191 fix round): the corner roles are `checkbox` (4px), `tile` (6px), `field` (8px), `compact` (10px), `control` (14px), `card` (16px) and `panel`, replacing `--radius-xs`, `--radius-sm`, `--radius-md` and the logo-mark radius; `micro` (10px) replaces the count-badge, my-method overline and phone-action sizes, `public-cycle-phase` joins the sizes, and `size-control-xs` and `size-control-xl` join the control heights; `text-inverted-secondary` and `divider` are added and `text-strong`, `text-emphasis`, `bundle-secondary`, `waitlist-button-hover` and `tracking-wide` are gone. One scale per axis: sizes are the framework's steps plus `caption` (11px), `md` (15px), `label`, `micro` and the display steps (the `body-*` sizes are gone); line heights are the framework's plus `display-snug` and `heading`; fully rounded shapes are the framework's `rounded-full` (`--radius-pill` is gone). The 14px control corner is `--radius-control`, the 6px tile corner `--radius-tile` (the workouts day tiles, the savings badge and the phone preview's chat-bubble tails), `--radius-compact` (10px) is left to the pricing badge's folder tab, and `--radius-xl` and the footer CTA radius are gone. Added `shadow-card`, `shadow-action`, `shadow-action-hover`, `surface-strong` and `icon-muted`; removed `surface-input`, `surface-selected`, `text-accent`, `text-disabled`, `about-credential-text`, `brand-primary-pressed`, `shadow-brand-glow`, `shadow-public-nav` and the cycle-only tokens. The base layer no longer smooth-scrolls on focus and suppresses the outline of `tabindex="-1"` targets; the lighter focus ring applies inside any `data-surface="inverted"` container. The prototype's `theme.css` mirrors these tokens Changed (GEN-193): adds `--color-brand-secondary-surface`, the opaque teal fill the accent badge needs to clear AA on every surface it sits on, mirroring the token the prototype added.; changed (coach follow-ups): adds `--color-surface-muted-hover`, the hover step for `surface-muted`, mirroring the token the prototype added so the name means one value on both sides |
| U803 | packages/ui/src/lib/cn.ts:cn | C5 | utility | frameworks | published (`./lib`) | operator/platform | changed (GEN-191 fix round): registers the `micro`, `caption` and `md` sizes with the label, display, my-method figure, cycle-phase, phone and footer-CTA sizes, so `tailwind-merge` keeps them apart from colours; since the GEN-191 final fix round it registers every theme role — text sizes, radii, shadows, tracking, leading and containers — so a later class of the same kind replaces a role |
| U804 | packages/ui/src/motion/motion.ts:module (createFadeUpVariants, publicEase, publicEaseOut, useClientReducedMotionPreference) | C5 | mixed | frameworks | published (`./motion`) | operator/platform, visitor | changed (design-system fix round): adds `publicSectionRevealViewport`, the one `once`, -80px viewport the section reveals share |
| U805 | packages/ui/src/lib/use-search-params-writer.ts:useSearchParamsWriter | C5 | adapter | adapters | published (`./lib`) | operator/platform | present Changed (GEN-193): the writer returns `replaceSearchParams` beside `writeSearchParams`, the same revision path through a replacing navigation, for state a reader would rather step past than walk back through. |
| U806 | packages/ui/src/layout/app-shell.tsx:AppShell | C5 | view | frameworks | published (`./layout`) | operator/platform | present |
| U812 | packages/ui/src/primitives/button.tsx:Button (+buttonVariants) | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (GEN-191 fix round): `buttonVariants` is a function over the module-private `cva` definition, and its axes are roles rather than prototype families. `variant`: `primary`, `secondary`, `inverted` (dark fill, brand on hover), `outline`, `outline-brand` and `glass` (the translucent pill on the transparent header); `size`: `xs`, `md`, `lg` and `lg-tight` (the `lg` height with the narrow padding, since the GEN-191 final fix round removed `xl`), heights from the `size-control-*` tokens; `width`: `content`, `full` (width only) or `full-below-sm`; `weight`: `regular`, `medium` or `semibold`; `textSize`: `sm`, `base` or `lg`; `lettering`: `plain`, `wide` or `caps`; `elevation`: `flat` or `raised`; `press`: `none` or `scale`. The `label` axis and the `cta` and `cta-lg` sizes are gone. Every variant takes the `control` corner; callers add only layout |
| U813 | packages/ui/src/primitives/card.tsx:Card (+cardVariants) | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (GEN-191 final fix round): `variant` `card` (the 16px `card` corner, faint border, card shadow; the default), `quiet` (the same corner, flat, on the quiet surface) or `panel` (the 24px corner, subtle border, soft shadow); it sets no padding. `cardVariants` gives the same classes to a semantic element, as `buttonVariants` does for links, and the hand-built cards of the catalogue, booking, pricing and legal views use one or the other Changed (GEN-193): gains the `portal-panel` variant, the portal surfaces' panel — the panel radius and soft shadow over the faint hairline the coach and client cards share, distinct from the reading `panel`'s warmer edge. |
| U815 | packages/ui/src/primitives/checkbox.tsx:Checkbox | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (design-system fix round): carries no focus classes of its own; the stylesheet's focus rule applies |
| U823 | packages/ui/src/filters/filter-chip-group.tsx:FilterChipGroup, FilterChip (+FilterChipTone) | C5 | mixed | adapters | published (`./filters`) | visitor (store catalog filtering) | changed (design-system fix round): carries no focus classes of its own; the stylesheet's focus rule applies |
| U825 | packages/ui/src/primitives/icon-button.tsx:IconButton | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (design-system fix round): a `variant` axis replaces `size`: `ghost` (the 44px transparent round control the portal shell uses), `plain` (the public menu toggle) and `soft` (the booking back control, a quiet 40px disc) |
| U826 | packages/ui/src/primitives/input.tsx:Input | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (design-system fix round): the one field look is the booking field's: 48px tall, the `field` corner, a `control-border-soft` border on `surface-quiet` at half strength, focus shown by `border-focus`. Callers set only layout |
| U827 | packages/ui/src/primitives/link.tsx:Link (+linkVariants) | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (GEN-191 fix round): the `brand` variant is gone; the variants are `inline` (the default), `subtle` and `pill` |
| U1287 | packages/ui/src/layout/dead-end-page.tsx:DeadEndPage | C5 | view | frameworks | published (`./layout`) | visitor, client, coach | changed (GEN-191 fix round): wraps the caller's one action in a centred row 32px below the description, so callers set no margin or padding on it. `DEAD_END_ACTION_CLASS_NAME` is gone and the landmark prop is `landmarkLabel`; callers style their one action with the `inverted` `Button` variant. The description takes `leading-relaxed`; changed (coach follow-ups): its body is now U1325's `DeadEndContent`, composed inside the same `main`, and its own class run is `cn(DEAD_END_BODY_CLASS_NAME, ...)`. It renders the same class set it rendered before, and its `eyebrow` and one action stay required |
| U828 | packages/ui/src/layout/phone-frame.tsx:PhoneFrame | C5 | view | frameworks | published (`./layout`) | operator/platform | present |
| U829 | packages/ui/src/primitives/section-eyebrow.tsx:SectionEyebrow | C5 | view | frameworks | published (`./primitives`) | operator/platform | changed (second parity round): the `muted` variant is Tailwind's `text-sm` |
| U1280 | packages/ui/src/primitives/textarea.tsx:Textarea | C5 | view | frameworks | published (`./primitives`) | visitor | changed (design-system fix round): shares the `Input` look (U826); callers set only its height |
| U1325 | packages/ui/src/layout/dead-end-page.tsx:DeadEndContent (+DEAD_END_BODY_CLASS_NAME) | C5 | view | frameworks | published (`./layout`) | visitor, client, coach | new (coach follow-ups). The dead-end body every dead end shares: the icon medallion, an optional eyebrow, one `h1`, the lead, and an optional action row. It returns a fragment and names the centring its parent must apply as `DEAD_END_BODY_CLASS_NAME`, because its two parents differ in their landmark, not in their presentation: U1287 wraps it in the full-page `main`, U1323 in a portal panel |
| U1288 | packages/ui/src/primitives/alert.tsx:Alert | C5 | view | frameworks | published (`./primitives`) | visitor | new (design-system fix round). The one form-level error message: a `role="alert"` box on the `control` corner with a `feedback-danger` border at 30% over the same colour at 5%, in small medium danger text. Its consumers are the booking flow (U1238, U1246, U1248) and the cart drawer (U213); callers add only layout |
| U1286 | packages/ui/src/primitives/label.tsx:Label | C5 | view | frameworks | published (`./primitives`) | visitor | new (booking-card rebuild). A native `label` whose `htmlFor` is required, so every use names its control; its one consumer is the booking details form |
| U830 | packages/ui/src/layout/portal-shell.tsx:PortalShell (+PortalNavigationLink) — composes the Radix-backed NavigationDialog for mobile navigation | C5 | view | frameworks | published (`./layout`) | coach, client | changed (design-system fix round): carries no focus classes of its own; its menu toggle is `IconButton`'s `ghost` variant; changed (coach follow-ups): the open drawer starts at the top bar's height (`top-16`) instead of the viewport top and renders navigation only, so the bar keeps the one brand and the close button and nothing focusable sits under the bar. `PortalSidebarContent` is split into the module-private `PortalSidebarSurface` (the bordered panel both the aside and the drawer fill) and `PortalSidebarNavigation` (the links and the longest-match active rule); the aside composes the brand row itself. The drawer carries `data-parity-root="PortalMobileDrawer"`, named for this unit rather than for the one portal that renders it today |
| U832 | packages/ui/src/layout/sidebar-surface-layout.tsx:SidebarSurfaceLayout | C5 | view | frameworks | published (`./layout`) | operator/platform | present |
| U833 | packages/ui/src/overlays/sheet.tsx:Sheet, SheetTitle, SheetDescription, SheetContent | C5 | view | frameworks | published (`./overlays`) | operator/platform | changed (design-system fix round): carries no focus classes of its own |
| U850 | packages/ui/src/lib/focus-trap.ts:resolveFocusTrapTarget | C5 | entity rule (pure, tested) | entities | package-private | coach, client | removed (Radix NavigationDialog owns focus containment) |
| U1275 | packages/ui/src/calendar/calendar.tsx:Calendar (+CalendarProps; the package-private CalendarChevron and CalendarDayButton) | C5 | view | frameworks | published (`./calendar`) | visitor | changed (design-system fix round): the month buttons take the 14px `control` corner, a disabled day is only the 50% disabled look (no grey recolour), and each disabled day's button carries `aria-disabled` |
| U1276 | packages/ui/src/calendar/index.ts:module | C5 | framework-glue | frameworks | published (`./calendar`) | operator/platform | new |
| U1312 | packages/ui/src/primitives/badge.tsx:Badge | C5 | view | frameworks | published (`./primitives`) | coach | new (GEN-193). A pill in two tones: `accent` on the secondary-brand surface with a hairline of the same hue, `neutral` outlined and muted |
| U1313 | packages/ui/src/primitives/avatar.tsx:Avatar | C5 | view | frameworks | package-private | coach | new (GEN-193). The attendee mark on an appointment card: the picture when there is one, otherwise the first letter of the name, hidden from assistive technology because the name is beside it. Two tones, `quiet` and `muted`, for a moment still ahead and one already past |
| U1314 | packages/ui/src/primitives/pagination.tsx:Pagination, PaginationList, PaginationLink, PaginationCurrentPage, PaginationPrevious, PaginationNext, PaginationEllipsis | C5 | view | frameworks | published (`./primitives`) | coach | new (GEN-193). A labelled navigation landmark over a list of router links. The page being read is a separate component rather than a flag, and a step with nowhere to go (`to` of `null`) renders a disabled button rather than a link |
| U1315 | packages/ui/src/tabs/tabs.tsx:Tabs, TabsList, TabsTrigger, TabsContent | C5 | view | frameworks | published (`./tabs`) | coach | new (GEN-193). The segmented filter bar over `radix-ui` Tabs: the bar wraps rather than overflowing a narrow panel, every trigger shares the width, and the panel's focus ring follows the card's corners; changed (coach follow-ups): below `sm`, a list that has a fourth trigger lays out as two columns (`PAIRS_A_FOURTH_TRIGGER_BELOW_SM`, a `:has()` child query on the list), so a fourth trigger is never orphaned on a row of its own; a three-trigger list and the bar from `sm` up are unchanged |
| U1316 | packages/ui/src/tabs/index.ts:module | C5 | framework-glue | frameworks | published (`./tabs`) | operator/platform | new (GEN-193) |
| U1317 | packages/ui/src/appointments/appointment-card.tsx:AppointmentCard (+the module-private AttendeeAvatar wiring) | C5 | view | frameworks | published (`./appointments`) | coach | new (GEN-193). One appointment as a card: attendee, day and time, optional mail link, badges, a superseded moment struck through, a quoted note keeping its line breaks, a footnote and actions that stack below the content under `md`. A past card is muted and carries no shadow |
| U1318 | packages/ui/src/appointments/dashboard-appointment-row.tsx:DashboardAppointmentRow | C5 | view | frameworks | published (`./appointments`) | coach | new (GEN-193). The compact row a dashboard panel lists: name, badges, one muted line reading "<day> at <time>", and the caller's action at the end |
| U1319 | packages/ui/src/appointments/appointment.ts:module (AppointmentStatus, AppointmentAttendee, AppointmentTime, AppointmentTitleElement) | C5 | boundary-data | adapters | package-private | coach | new (GEN-193) |
| U1320 | packages/ui/src/appointments/index.ts:module | C5 | framework-glue | frameworks | published (`./appointments`) | operator/platform | new (GEN-193) |
| U851 | packages/ui/src/{appointments,filters,layout,lib,motion,overlays,primitives,tabs}/index.ts:module | C5 | framework-glue | frameworks | published (six of the seven subpath entries; `calendar/index.ts` is U1276) | operator/platform | changed (second parity round): `layout/index.ts` publishes `DeadEndPage` and `DEAD_END_ACTION_CLASS_NAME`, `motion/index.ts` no longer publishes `publicViewportOnce`, and `primitives/index.ts` no longer publishes `inputClasses`; changed (main merge): `layout/index.ts` publishes main's `NavigationDialog` and `NavigationMenu` beside `DeadEndPage`; changed (coach follow-ups): `layout/index.ts` also publishes `DeadEndContent` and `DEAD_END_BODY_CLASS_NAME` (U1325) |
| U852 | packages/ui/src/layout/navigation-dialog.tsx:NavigationDialog (+NavigationMenu) — owns the mobile navigation pattern: persistent top bar whose page trigger turns invisible and hidden from assistive technology while the in-dialog menu button, which stays mounted across open, closing and reopen, takes over, top-bar actions that close the menu, first-link focus, the animated panel whose `open`/`closed` variants end the closing state, and the desktop-breakpoint close | C5 | view | frameworks | published (`./layout`) | visitor, client, coach | present; changed (main merge): an optional `menuButtonVariant` picks the menu button's `IconButton` variant, and the public site passes `plain` |
| U853 | packages/ui/src/layout/use-close-mobile-navigation-on-desktop.ts:useCloseMobileNavigationOnDesktop | C5 | adapter | adapters | package-private | visitor, client, coach | present |
| U900 | domain/account/account.ts:Account (+AccountRole, AccountSnapshot; `isActive()`, `toSnapshot()`, statics `canAccessClientPortal(snapshot)` / `canAccessCoachPortal(snapshot)`) | C1 | entity | entities | published | operator/platform, coach, client | moved from domain/accounts/account-model.ts, rules became methods |
| U901 | domain/account/accounts.ts:Accounts | C1 | port | use-cases | published | operator/platform | present (moved) |
| U902 | domain/account/provision-account-use-case.ts:ProvisionAccountUseCase (+ProvisionAccountResult carrying `AccountSnapshot`) | C1 | use-case | use-cases | published | operator/platform, vendor:clerk | renamed from AccountProvisioningService |
| U903 | domain/account/index.ts:module | C1 | framework-glue | adapters | published (`./account`) | operator/platform | moved from domain/accounts/index.ts |
| U948 | domain/account/delete-account-use-case.ts:DeleteAccountUseCase | C1 | use-case | use-cases | published | operator/platform, vendor:clerk | renamed from AccountDeletionService |
| U904 | domain/coaching-bundles/coaching-bundles.ts:module | C1 | entity | entities | — | — | removed (e8690f45; the literal is now C11 `sections/pricing/coaching-bundles.ts`, decision D5) |
| U949 | domain/coaching-bundles/bundle-offers.ts:module | C1 | entity | entities | — | — | removed (e8690f45; `WaitlistOfferPlan` moved to U911) |
| U905 | domain/coaching-bundles/index.ts:module | C1 | framework-glue | adapters | — | — | removed (e8690f45; the `./coaching-bundles` subpath is gone) |
| U906 | domain/feature-flag/feature-flags.ts:FeatureFlags | C1 | port | use-cases | published | operator/platform | present (moved) |
| U907 | domain/feature-flag/get-feature-flags-use-case.ts:FeatureFlagReader | C1 | port (input boundary) | use-cases | published | operator/platform | moved from the entity module to the use-case module that implements it |
| U908 | domain/feature-flag/feature-flag.ts:FeatureFlag (+FeatureFlagSet, WAITLIST_MODE_FEATURE_FLAG, static `toSet(flags)`) | C1 | entity + boundary-data | entities | published through `./feature-flag` | operator/platform | changed (f0eb1bf4; `FeatureFlagSet` published for `GetWaitlistUseCase`); changed (main merge into GEN-191; publishes `WAITLIST_MODE_FEATURE_FLAG`, the one key U963 and U1300 read) |
| U909 | domain/feature-flag/get-feature-flags-use-case.ts:GetFeatureFlagsUseCase (implements U907) | C1 | use-case | use-cases | published | operator/platform | renamed from FeatureFlagService |
| U910 | domain/feature-flag/index.ts:module | C1 | framework-glue | adapters | published (`./feature-flag`, including `FeatureFlagSet`) | operator/platform | changed (f0eb1bf4) |
| U911 | domain/waitlist/waitlist.ts:Waitlist (+WaitlistOffer with a private `"all-bundles"` plan literal, WaitlistAvailability, WaitlistSnapshot, WaitlistConsentVersions, WaitlistSignupPricing; `availability(count)`, static `availabilityBucketStart(now)`; `WAITLIST_REDUCED_PRICING_CAP`) | C1 | entity | entities | published | operator/platform, visitor | configured waiting-list aggregate; the active offer is shared aggregate configuration read directly by U963 and U914, and availability is its rule, computed against `WAITLIST_REDUCED_PRICING_CAP`; registration is decided by U303 and the U304 constraints; the direct readonly offer access and shallow immutability are accepted under R17; system response projection belongs to U963; changed (b3eb2653; `decideReducedPricingRegistration` removed because the database allocates reduced slots, and the cap is the published constant `WAITLIST_REDUCED_PRICING_CAP`; the `cap` prop and the signup command's `cap` field are removed, so the constant is the only source of N) |
| U912 | domain/waitlist/waitlist-entries.ts:WaitlistEntries (+ReducedPricingSignupResult, RegularPricingSignupResult) | C1 | port | use-cases | published | operator/platform | moved out of waitlist-service.ts |
| U913 | domain/waitlist/waitlist-confirmation.ts:WaitlistConfirmation (+SendWaitlistConfirmationCommand, WaitlistConfirmationResult = sent \| failed) | C1 | port | use-cases | published | operator/platform, vendor:resend | renamed from WaitlistConfirmationService (D3) |
| U914 | domain/waitlist/join-waitlist-use-case.ts:JoinWaitlistUseCase (+JoinWaitlistResult; takes `incidents: WaitlistIncidents`, uses `EmailAddress`) | C1 | use-case | use-cases | published | visitor, operator/platform | altered in a79f507d; generic logging replaced by waitlist-owned incident operations |
| U963 | domain/waitlist/get-waitlist-use-case.ts:GetWaitlistUseCase (takes `clock: Clock`, `featureFlags: FeatureFlagReader` and `incidents: WaitlistIncidents`; interprets `WAITLIST_MODE`, reports a failed flag read through `waitlistModeReadFailed()`, and assembles `WaitlistSnapshot`) | C1 | use-case | use-cases | published | visitor, operator/platform | changed (63a570a7, 489f6293, 010904d8, f0eb1bf4, 242a0976; owns system projection and names the published `FeatureFlagSet`) |
| U915 | domain/waitlist/waitlist-service.ts:module | C1 | boundary-data | use-cases | — | — | removed (e8690f45; the types moved onto U911, U912, U913) |
| U916 | domain/waitlist/index.ts:module | C1 | framework-glue | adapters | published (`./waitlist`) | operator/platform | altered in a79f507d; publishes U1194 |
| U950 | domain/waitlist/waitlist-registration.ts:decideReducedPricingRegistration | C1 | entity rule | entities | — | — | removed (e8690f45 moved it onto `Waitlist`; b3eb2653 removed the static, since reduced-slot allocation and its limit are enforced by U303 and the U304 constraints) |
| U956 | domain/email-address/email-address.ts:EmailAddress (`static normalize(raw)`, `value`, `deliveryLimitKey`) | C1 | entity (value object) | entities | published (`./email-address`) — no consumer outside the package, see the ledger's run-8 F242 | operator/platform, visitor | function `normalizeEmail` became a value object; absorbs the store's plus-tag stripping (old U928) |
| U964 | domain/email-address/index.ts:module | C1 | framework-glue | adapters | published (`./email-address`) | operator/platform | new |
| U953 | domain/cart/cart.ts:Cart (`static of(slugs)`, `reconcile(availableSlugs)`, `slugs`) | C1 | entity (value object) | entities | published (`./cart`) | visitor | function `reconcileCart` became a value object; runs in the browser, no ports |
| U965 | domain/cart/index.ts:module | C1 | framework-glue | adapters | published (`./cart`) | visitor | new |
| U966 | domain/product/product.ts:Product (id, slug, displayOrder, lifecycleStatus, latestVersionSequence; `canBeRevised()`, `nextVersionSequence()`, `revisionPlacement()`, static `evaluatePurchasability(selections, locked)`) | C1 | entity | entities | published | operator/platform, coach, visitor | new: the publication lifecycle record, absorbing old U952's purchasability rule |
| U917 | domain/product/product.ts:PublishedProduct (id, slug, displayOrder, version; `deliveryResource()`) plus ProductAsset, PublishedProductCover, StoreTaxonomyValue, PublicationOperation, PublicationPlacement | C1 | entity (catalog read model) + boundary-data | entities | published | operator/platform, visitor | boundary-data `PublishedStoreProduct` became a class; moved from domain/store/models.ts |
| U918 | domain/product/product.ts:isStoreCoverMimeType (+STORE_COVER_MIME_TYPES) | C1 | entity (allowlist rule) | entities | published | operator/platform | present (moved) |
| U919 | domain/product/store-catalog.ts:StoreCatalog | C1 | port | use-cases | published | operator/platform | present (moved); returns `PublishedProduct` instances (D1) |
| U920 | domain/store/catalog/store-catalog-service.ts:StoreCatalogService | C1 | use-case | use-cases | — | — | removed (e8690f45; split into U967, U968, U969) |
| U967 | domain/product/list-published-products-use-case.ts:ListPublishedProductsUseCase | C1 | use-case | use-cases | published | visitor, operator/platform | new |
| U968 | domain/product/find-published-product-use-case.ts:FindPublishedProductUseCase | C1 | use-case | use-cases | published | visitor, operator/platform | new |
| U969 | domain/product/find-published-cover-use-case.ts:FindPublishedCoverUseCase | C1 | use-case | use-cases | published | visitor, operator/platform | new |
| U921 | domain/product/{list-published-products,find-published-product,find-published-cover}-use-case.ts:module (PublishedCatalogResult, PublishedProductResult, PublishedCoverResult) | C1 | boundary-data | use-cases | published | visitor | split out of store-catalog-service.ts. Two of the three carry `PublishedProduct` instances rather than plain data; the ledger's run-8 F239 and F240 dispute that, so this row records the shapes without endorsing the crossing |
| U935 | domain/product/product-assets.ts:ProductAssets (+ProductAssetOpenResult = opened(bytes: `AsyncIterable<Uint8Array>`) \| unavailable) | C1 | port | use-cases | published | operator/platform | present (moved) |
| U937 | domain/product/product-assets.ts:ProductAssetWriter (+ProductAssetContent) | C1 | port | use-cases | published | operator/platform | present (moved) |
| U938 | domain/product/product-assets.ts:ProductAssetDigest | C1 | port | use-cases | published | operator/platform | present (moved) |
| U939 | domain/product/product-asset-keys.ts:buildCoverAssetKey / buildDownloadAssetKey | C1 | entity (content-addressing rule) | entities | folder-private | operator/platform | present (moved) |
| U940 | domain/product/product-file-formats.ts:resolveDownloadFormat / resolveCoverFormat | C1 | entity (format-by-signature rule) | entities | folder-private | operator/platform | present (moved) |
| U941 | domain/product/product-file-formats.ts:module (STORE_DOWNLOAD_EXTENSIONS, STORE_COVER_EXTENSIONS, resolution types) | C1 | boundary-data | entities | folder-private | operator/platform | present (moved) |
| U970 | domain/product/product-publication.ts:ProductPublicationDraft (`static from`; `requestDigest`, `settle`, `planCreation`, `planRevision`, `plan`, `persistCommand`, `plannedAssetContents`) | C1 | mixed (value object holding the submitted draft + the publication orchestration the five publication use cases share) | use-cases | folder-private (not exported from `./product`) | operator/platform, coach | new. Ring is use-cases, not entities: it holds the request's uploaded bytes and names the `ProductAssetDigest` port. `settle` and `persistCommand` read no instance state |
| U942 | domain/product/product-publication.ts:module (MAX_PUBLICATION_BYTES, validateSlugFormat, resolvePublicationTarget, and the publication command/plan/issue/result types) | C1 | boundary-data + policy constant + pure rules | use-cases | published (except `validateSlugFormat`) | operator/platform | merges old U942 and U955 |
| U955 | domain/store/publication/product-publication-rules.ts:module | C1 | entity rule | entities | — | — | removed (e8690f45; the rules are methods on U970 and the two functions in U942) |
| U943 | domain/product/store-product-publications.ts:StoreProductPublications | C1 | port | use-cases | published | operator/platform | present (moved); returns `Product` instances (D1) |
| U944 | domain/store/publication/store-product-publication-service.ts:StoreProductPublicationService | C1 | use-case | use-cases | — | — | removed (e8690f45; split into U971-U975) |
| U971 | domain/product/plan-new-product-use-case.ts:PlanNewProductUseCase | C1 | use-case | use-cases | published | operator/platform, coach | new |
| U972 | domain/product/plan-product-revision-use-case.ts:PlanProductRevisionUseCase | C1 | use-case | use-cases | published | operator/platform, coach | new |
| U973 | domain/product/publish-new-product-use-case.ts:PublishNewProductUseCase | C1 | use-case | use-cases | published | operator/platform, coach | new; keeps a private `commit` shared in shape with U974 (owner ruling, T7) |
| U974 | domain/product/publish-product-version-use-case.ts:PublishProductVersionUseCase | C1 | use-case | use-cases | published | operator/platform, coach | new; same private `commit` |
| U975 | domain/product/retire-product-use-case.ts:RetireProductUseCase (+RetireProductResult) | C1 | use-case | use-cases | published | operator/platform, coach | new |
| U945 | domain/store/publication/store-product-publication-service.ts:module | C1 | boundary-data | use-cases | — | — | removed (e8690f45; the types are in U942) |
| U946 | domain/store/index.ts:module | C1 | framework-glue | adapters | — | — | removed (e8690f45; the `./store` subpath is gone) |
| U976 | domain/product/index.ts:module | C1 | framework-glue | adapters | published (`./product`) | operator/platform | new |
| U977 | domain/acquisition/acquisition.ts:AcquisitionRequest (`static from(command, requestedAt)`; normalised email, deduplicated sorted slugs, canonical payload, delivery windows, delivery-limit key; `selectProducts(catalog)`) | C1 | entity (value object) | entities | published | visitor | new; absorbs the request-shaping the old service held |
| U951 | domain/acquisition/acquisition.ts:module (STORE_DELIVERY_LIMIT_POLICY, DeliveryLimitPolicy, evaluateDeliveryLimit, StoreDeliveryLimitWindow) | C1 | entity rule (pure, tested; called inside the repository transaction) | entities | published | visitor, operator/platform | present (moved from domain/store/delivery/) |
| U952 | domain/store/acquisition/purchasability.ts:evaluatePurchasability | C1 | entity rule | entities | — | — | removed (e8690f45; now the static `Product.evaluatePurchasability` on U966) |
| U928 | domain/store/delivery/delivery-limit-key.ts:resolveDeliveryLimitKey | C1 | entity rule | entities | — | — | removed (e8690f45; now `EmailAddress.deliveryLimitKey` on U956) |
| U922 | domain/acquisition/store-acquisitions.ts:StoreAcquisitions (+PrepareAcquisitionCommand, AcquisitionPreparation, ResolvedPriorAcquisition) | C1 | port | use-cases | published | operator/platform | present (moved). `PrepareAcquisitionCommand.products` is typed `readonly PublishedProduct[]`; the ledger's run-8 F245 disputes whether the record's D6 wording covers an entity entering a port, so this row records the port without endorsing that shape |
| U923 | domain/acquisition/product-delivery.ts:ProductDelivery (+ProductDeliveryResult = delivered \| rejected \| unconfirmed) | C1 | port | use-cases | published | vendor:resend | renamed from StoreDeliveryService (D3) |
| U978 | domain/acquisition/product-delivery.ts:DownloadTokenGenerator (+CreateDownloadTokenResult) | C1 | port | use-cases | declared, deliberately unpublished; the adapter satisfies it structurally | operator/platform | new as a named port |
| U925 | domain/acquisition/product-delivery.ts:PayloadDigestGenerator | C1 | port | use-cases | published | operator/platform | present (moved) |
| U927 | domain/store/acquisition/store-acquisition-service.ts:StoreAcquisitionService | C1 | use-case | use-cases | — | — | removed (e8690f45; now U979) |
| U979 | domain/acquisition/acquire-products-use-case.ts:AcquireProductsUseCase (+AcquireProductsResult; takes `clock: Clock`, `incidents: AcquisitionIncidents`) | C1 | use-case | use-cases | published | visitor, operator/platform | altered in a79f507d; generic logging replaced by acquisition-owned incident operations |
| U929 | domain/acquisition/acquire-products-use-case.ts:module (AcquireProductsCommand, ProductSelection) | C1 | boundary-data | use-cases | published | visitor, operator/platform | present (moved) |
| U980 | domain/acquisition/index.ts:module | C1 | framework-glue | adapters | published (`./acquisition`) | operator/platform | altered in a79f507d; publishes U1193 |
| U954 | domain/download-grant/download-grant.ts:DownloadGrant (+DownloadGrantItem, GrantDelivery; `isActive(now)`, `delivery()`) | C1 | entity | entities | published | visitor | boundary-data plus free rules became a class |
| U931 | domain/download-grant/download-grants.ts:DownloadTokenHasher | C1 | port | use-cases | published | operator/platform | present (moved) |
| U932 | domain/download-grant/download-grants.ts:DownloadGrants | C1 | port | use-cases | published | operator/platform | present (moved); returns a `DownloadGrant` instance (D1) |
| U933 | domain/store/download-grants/download-grant-service.ts:DownloadGrantService | C1 | use-case | use-cases | — | — | removed (e8690f45; now U981) |
| U981 | domain/download-grant/resolve-download-grant-use-case.ts:ResolveDownloadGrantUseCase (+DownloadGrantResolution; takes `clock: Clock`) | C1 | use-case | use-cases | published | visitor | new. `DownloadGrantResolution.grant` carries a `DownloadGrant` instance out to C7 adapters; the ledger's run-8 F241 disputes that crossing, so this row records the type without endorsing it |
| U982 | domain/download-grant/index.ts:module | C1 | framework-glue | adapters | published (`./download-grant`) | operator/platform | new |
| U957 | domain/shared/clock.ts:Clock | C1 | port | use-cases | published (`./shared`) | operator/platform | present |
| U958 | domain/shared/logger.ts:Logger | C1 | port | use-cases | — | operator/platform | removed in a79f507d (replaced by the slice-owned U1193 and U1194 incident interfaces) |
| U959 | domain/shared/bot-verifier.ts:BotVerifier (+BotVerificationRequest, BotVerificationResult) | C1 | port | use-cases | — | visitor, vendor:cloudflare-turnstile | removed in a79f507d (contract relocated to U1195) |
| U960 | domain/shared/product-email.ts:ProductEmail (+ProductEmailCommand, ProductEmailResult) | C1 | port | use-cases | — | operator/platform, vendor:resend | removed in a79f507d (contract relocated to U1196) |
| U961 | domain/shared/management-authenticator.ts:ManagementAuthenticator (+ManagementCredentials, ManagementAuthenticationResult) | C1 | port | use-cases | — | operator/platform, coach (future) | removed in a79f507d (contract relocated to U1197) |
| U1255 | domain/assessment-call/assessment-call.ts:AssessmentCall (+AssessmentCallProps, AssessmentCallSnapshot carrying the derived `endsAt`, CoachTimeOutcome = reserved \| taken, ReservationDecision = reserved \| slot_taken \| email_has_upcoming_call; `endsAt()`, `toSnapshot()`, statics `reconstitute(props)` / `decideReservation({coachTime, upcomingCallForEmail})`) | C1 | entity | entities | published | visitor, operator/platform | new. `decideReservation` is the slot-taken / email-has-upcoming ladder the repository calls inside its transaction, the same shape as `Waitlist.decideReducedPricingRegistration`. Since the coach-calendar refactor its slot input is the outcome of reserving the coach's time rather than a row held at the same start, so the no-overlap constraint decides whether the slot is taken and the method keeps only the order: a taken slot is `slot_taken` whoever holds it, the submitter included (D-C); neither refusal carries the call that caused it |
| U1256 | domain/assessment-call/assessment-call-reservations.ts:AssessmentCallReservations (+ReserveAssessmentCallCommand, ReservationResult = reserved(call) \| slot_taken \| email_has_upcoming_call) | C1 | port | use-cases | published | operator/platform | new. `reserve` and `findById`; `reservedStartsFrom` left the port with the coach-calendar refactor, because busy time is read through `CoachCalendar` (U1291) |
| U1257 | domain/assessment-call/assessment-call-notifications.ts:AssessmentCallNotifications (+AssessmentCallRecipient, AssessmentCallNotificationResult = per-recipient sent \| failed) | C1 | port | use-cases | published | visitor, operator/platform, vendor:resend | new |
| U1258 | domain/assessment-call/meeting-room-link.ts:MeetingRoomLink | C1 | port | use-cases | published | visitor | new |
| U1259 | domain/assessment-call/book-assessment-call-use-case.ts:BookAssessmentCallUseCase (+BookAssessmentCallCommand, BookAssessmentCallResult = booked(call) \| slot_unavailable \| email_already_booked \| closed; takes `availability`, `bookingWindow: AssessmentCallBookingWindow`, `clock: Clock`, `incidents: AssessmentCallIncidents`, `notifications`, `reservations`, uses `EmailAddress`) | C1 | use-case | use-cases | published | visitor, operator/platform | changed (main merge): asks `bookingWindow.isOpen()` per request in place of the `bookingOpen` boolean, and reports a failed recipient through `incidents.notificationFailed({ recipient })` in place of the removed shared `Logger`. Reads the coach's zone off `availability.current()` inside `execute()` and writes it onto the reservation; asks `isOpenStart` with `ASSESSMENT_CALL_RULES` as the policy and leaves busy time to the reservation. Only `booked` carries an `AssessmentCall` instance out to the controller (D9); `slot_taken` becomes `slot_unavailable` whoever holds the slot (D-C), and `email_already_booked` carries nothing (D-B). A failed or throwing notification is reported as an incident, never surfaced to the visitor |
| U1260 | domain/assessment-call/list-open-slots-use-case.ts:ListOpenSlotsUseCase (+OpenSlotsResult = open \| unavailable \| closed; takes `availability`, `bookingWindow: AssessmentCallBookingWindow`, `calendar: CoachCalendar`, `clock`, `incidents: AssessmentCallIncidents`) | C1 | use-case | use-cases | published | visitor | changed (main merge): asks `bookingWindow.isOpen()` per request in place of the `bookingOpen` boolean, and reports a failed port read through `incidents.slotsReadFailed()` in place of the removed shared `Logger`. Reads the coach's busy time through `calendar.busyFrom(now)` and passes it with `ASSESSMENT_CALL_RULES` to `openSlotStarts`. The `unavailable` member is the degraded read the booking page renders instead of a 500 (D21, CH-E). Only the two port reads sit inside the `try`; the slot arithmetic runs after it, so a fault there propagates |
| U1261 | domain/assessment-call/resolve-join-link-use-case.ts:ResolveJoinLinkUseCase (+JoinLinkResult = found \| unknown) | C1 | use-case | use-cases | published | visitor | new |
| U1299 | domain/assessment-call/assessment-call-incidents.ts:AssessmentCallIncidents (`bookingModeReadFailed()`, `notificationFailed({ recipient })`, `slotsReadFailed()`) | C1 | port | use-cases | published (`./assessment-call`) | operator/platform | new (main merge): the slice-owned incident interface that replaces the removed shared `Logger` for the assessment-call use cases, beside U1193 and U1194; implemented by U519; changed (coach follow-ups): adds `callsReadFailed()`, raised by U1321 when the coach's listing cannot be read. Like its three siblings it takes no error argument |
| U1300 | domain/assessment-call/assessment-call-booking-window.ts:AssessmentCallBookingWindow (`isOpen()`) | C1 | policy service | use-cases | published (`./assessment-call`) | visitor, operator/platform | new (main merge): reads the `WAITLIST_MODE` feature flag (`WAITLIST_MODE_FEATURE_FLAG`, U908) through `FeatureFlagReader` (U907) on every call; booking is open only while the flag is not true, and a failed read counts as closed and is reported through `bookingModeReadFailed()`. Replaces the `bookingOpen` boolean the container used to compute from the environment |
| U1321 | domain/assessment-call/list-assessment-calls-use-case.ts:ListAssessmentCallsUseCase (+AssessmentCallListingResult = ok \| unavailable; takes `availability`, `incidents` and `reservations`) | C1 | use-case | use-cases | published (`./assessment-call`) | coach, operator/platform | new (GEN-193, at a5615696; row backfilled by the coach follow-ups review, which found no row for it); changed (coach follow-ups): it reads the coach's availability and every reservation inside a private `readSources`, and a throw from either is caught, reported through U1299's `callsReadFailed` and answered as `{ status: "unavailable" }` rather than propagating. The shape mirrors U1260, which answers a failed slot read the same way. The published result type replaces the former `AssessmentCallListing` |
| U1262 | domain/assessment-call/index.ts:module | C1 | framework-glue | adapters | published (`./assessment-call`) | operator/platform | new |
| U1263 | domain/coach-availability/coach-availability.ts:CoachAvailability (+CoachAvailabilityProps, Weekday; `openSlotStarts({now, policy, busy})`, `isOpenStart({start, now, policy})`, static `configure(props)`) | C1 | entity | entities | published | visitor, operator/platform | changed (GEN-191 fix round): stepping is in minutes of the day: `startsOn` walks from the window's opening minute every `stepMinutes` while the call still ends by the window's close, `isStepStart` counts the minutes into the window against the step, and `fitsTheWindow`, which replaced `isWindowHour`, requires the start inside the window and the call to end inside it. `configure` is the first throwing factory in the package: it asserts a time zone `Intl.DateTimeFormat` can construct with, whole-hour windows and known weekdays. Since the coach-calendar refactor the step, horizon and lead time come from the `SlotPolicy` the caller passes, and a start is open only when `policy.coachTimeFrom(start)` overlaps no `TimeInterval`, half-open; the slice imports no appointment kind |
| U1264 | domain/assessment-call/assessment-call-rules.ts:ASSESSMENT_CALL_RULES | C1 | policy constant | entities | published (`./assessment-call`) | operator/platform | moved from `coach-availability/coach-availability.ts` by the coach-calendar refactor, and now a `SlotPolicy` (U1289). The single home of the 30-minute duration, 30-minute buffer, 30-day horizon and 120-minute lead time (CH-H); the 60-minute step is derived from the duration and buffer constants. The buffer is read: `coachTimeFrom` reserves the call plus its buffer, so it blocks every appointment kind, not only the next assessment start |
| U1265 | domain/coach-availability/coach-availability-source.ts:CoachAvailabilitySource | C1 | port | use-cases | published | operator/platform | new |
| U1266 | domain/coach-availability/zoned-time.ts:module (instantToWallClock, wallClockToInstant, addDays, weekdayIndexOf, compareCalendarDates, CalendarDate, WallClock, ZonedWallClockMinute) | C1 | entity rule (pure, tested) | entities | folder-private | operator/platform | changed (GEN-191 fix round): `WallClockHour` is `ZonedWallClockMinute`, and `wallClockToInstant` takes a minute as well as an hour. Wall-clock arithmetic in a named zone using `Intl.DateTimeFormat` only, with no import of any kind; the domain package's no-dependency boundary is why it exists |
| U1267 | domain/coach-availability/index.ts:module | C1 | framework-glue | adapters | published (`./coach-availability`) | operator/platform | new |
| U1289 | domain/coach-availability/slot-policy.ts:SlotPolicy (`coachTimeFrom(start)`, `leadMilliseconds()`, static `of(props)`) | C1 | value object | entities | published | operator/platform | changed (GEN-191 fix round): `of` validates and throws: every count must be a whole, non-negative number of minutes, the call must last at least a minute (a separate check since the final fix round), and the step at least as long as the call, so offered starts never overlap; `ASSESSMENT_CALL_RULES` therefore fails at module load if its literals are wrong. What an appointment kind brings to slot generation: duration, buffer, step, horizon and lead time. `coachTimeFrom` returns a `TimeInterval` and is the one place the coach's held interval — the call plus its buffer — is computed, read by the slot filter and by the repository that reserves it |
| U1290 | domain/coach-availability/time-interval.ts:TimeInterval | C1 | value (plain data) | entities | published | operator/platform | changed (GEN-191 fix round): renamed from `busy-interval.ts:BusyInterval`; `CoachCalendar.busyFrom` and `SlotPolicy.coachTimeFrom` both return it. `{ start, end }`, half-open; the read concept an internal reservation and, later, an external calendar both produce |
| U1291 | domain/coach-availability/coach-calendar.ts:CoachCalendar (`busyFrom(from)`) | C1 | port | use-cases | published | operator/platform | new (coach-calendar refactor). The coach's busy time as intervals ending after `from`; it says nothing about what occupies the time |
| U962 | domain/shared/index.ts:module | C1 | framework-glue | adapters | published (`./shared`, Clock only) | operator/platform | altered in a79f507d |
| U1000 | bot-detection/bot-detection-contract.ts:botDetectionConfigSchema (+BotDetectionConfig) | C6 | boundary-data | adapters | published | vendor:cloudflare-turnstile | present |
| U1001 | bot-detection/bot-detection-contract.ts:TURNSTILE_RESPONSE_FIELD, STORE_ACQUISITION_TURNSTILE_ACTION, WAITLIST_TURNSTILE_ACTION, ASSESSMENT_CALL_BOOKING_TURNSTILE_ACTION | C6 | boundary-data | adapters | published | visitor, vendor:cloudflare-turnstile | gained the assessment-call action; whether a feature's action constant belongs in its own `contracts/` is an open question in `decisions.md` |
| U1002 | bot-detection/bot-detection-config.server.ts:createBotDetectionConfig (takes `BotDetectionSettings`) | C6 | adapter | adapters | published | operator/platform | present |
| U1004 | bot-detection/submission/bot-detection-widget.tsx:BotDetectionWidget | C6 | view | adapters | published | visitor | present |
| U1007 | bot-detection/verifier/bot-verifier.server.ts:StaticTokenBotVerifier | C6 | adapter (un-exported; built by the factory) | adapters | package-private | operator/platform | present |
| U1008 | bot-detection/verifier/bot-verifier.server.ts:resolveRequestRemoteIp | C6 | utility | adapters | published | vendor:cloudflare | present |
| U1009 | bot-detection/verifier/create-bot-verifier.server.ts:createBotVerifier (selects on `BOT_DETECTION_PROVIDER`) | C6 | adapter factory | adapters | published | operator/platform | present |
| U1010 | bot-detection/index.server.ts:module | C6 | framework-glue | frameworks | published (`./bot-detection/server`, including BotVerifier) | - | altered in a79f507d |
| U1011 | bot-detection/index.ts:module | C6 | framework-glue | frameworks | published (`./bot-detection`) | - | present |
| U1012 | bot-detection/turnstile/turnstile-bot-verifier.server.ts:TurnstileBotVerifier | C6 | adapter (un-exported; built by the factory) | adapters | package-private | vendor:cloudflare-turnstile | present |
| U1013 | bot-detection/turnstile/turnstile-client.ts:useTurnstileWidget (+TurnstileChallengeHandle) | C6 | view | adapters | package-private | visitor | present |
| U1015 | bot-detection/turnstile/turnstile-widget.tsx:TurnstileWidget | C6 | view | adapters | package-private | visitor | present |
| U1016 | bot-detection/submission/use-bot-detection-submission.ts:useBotDetectionSubmission | C6 | hook over the step machine | adapters | published | visitor | present |
| U1037 | bot-detection/submission/bot-detection-flow.ts:module (reduceBotDetectionFlow, flow state and events) | C6 | step machine (pure, tested) | adapters | package-private | visitor | present |
| U1018 | email/email-primitives.server.tsx:Email* (10 components) | C6 | view | adapters | published | operator/platform | present |
| U1019 | email/index.server.ts:module | C6 | framework-glue | frameworks | published (`./email/server`, including ProductEmail) | - | altered in a79f507d |
| U1023 | email/resend-product-email.server.ts:ResendProductEmail (implements `ProductEmail`, returns `ProductEmailResult`) | C6 | adapter | adapters | package-private (built by the factory) | vendor:resend | maps `EmailAttachment.content` to a Resend `Attachment` with `Buffer.from`; the vendor's encoding stays on this side of the port. Sends the command's `replyTo` when present, otherwise the configured `PRODUCT_EMAIL_REPLY_TO` it was built with |
| U1038 | email/in-memory-product-email.server.ts:InMemoryProductEmail | C6 | adapter (recording double) | adapters | published | operator/platform | present |
| U1039 | email/create-product-email.server.ts:createProductEmail (selects on `PRODUCT_EMAIL_PROVIDER` = memory \| resend) | C6 | adapter factory | adapters | published | operator/platform | present |
| U1026 | feature-flags/index.server.ts:module | C6 | framework-glue | frameworks | published (`./feature-flags/server`) | - | present |
| U1027 | feature-flags/repository.server.ts:PostgresFeatureFlagRepository (implements the C1 `FeatureFlags` port) | C6 | adapter | adapters | published | operator/platform | present |
| U1028 | feature-flags/schema.server.ts:featureFlagsTable | C6 | boundary-data | frameworks | package-private | operator/platform | present |
| U1292 | coach-calendar/index.server.ts:module | C6 | framework-glue | frameworks | published (`./coach-calendar/server`) | - | new (coach-calendar refactor) |
| U1293 | coach-calendar/postgres-coach-calendar.server.ts:PostgresCoachCalendar (implements the C1 `CoachCalendar` port) | C6 | adapter | adapters | published | visitor | changed (GEN-191 fix round): returns the query's `{ start, end }` rows directly as `TimeInterval`s: every reservation ending after `from`, ordered by start; the rows' kinds and appointments never leave the adapter |
| U1294 | coach-calendar/coach-time-reservations.server.ts:reserveCoachTime, releaseCoachTime | C6 | adapter | adapters | published | visitor | changed (GEN-191 fix round): takes C2's `DatabaseTransaction` and classifies through C2's `isCausedByDatabaseError` (U1296), so its own cause-chain walk is gone. Transaction-scoped writers every appointment kind's repository calls inside its own transaction. `reserveCoachTime(transaction, { start, end, appointmentKind, appointmentId })` inserts in a savepoint and classifies Postgres `23P01` on `COACH_TIME_RESERVATIONS_NO_OVERLAP` as `{ status: "taken" }`, rethrowing everything else; `releaseCoachTime(transaction, { appointmentKind, appointmentId })` deletes the appointment's reservation |
| U1295 | coach-calendar/schema.server.ts:coachTimeReservationsTable, COACH_TIME_RESERVATIONS_NO_OVERLAP, AppointmentKind | C6 | boundary-data | frameworks | package-private (drizzle-kit glob) | operator/platform | changed (GEN-191 fix round): adds the unique index `coach_time_reservations_appointment_unique` on `(appointment_kind, appointment_id)`, declared here and created by `0023_coach_time_reservation_per_appointment.sql`, so an appointment holds at most one reservation and `releaseCoachTime` releases exactly that one. `coach_time_reservations` (`id`, `starts_at`, `ends_at`, `appointment_kind`, `appointment_id` with no foreign key because the owning table depends on the kind, `created_at`) with an `ends_at > starts_at` check. `AppointmentKind` is a one-member list today (`assessment_call`); `appointment_kind` is a plain `varchar(32)`, so adding a kind is one entry in that list and no migration. The `coach_time_reservations_no_overlap` exclusion constraint (`EXCLUDE USING gist (tstzrange(starts_at, ends_at, '[)') WITH &&)`) is hand-written in `0022_exclude_overlapping_coach_time_reservations.sql`, because drizzle-kit cannot declare it; the exported name is the literal the writer classifies against |
| U1029 | management-auth/bearer-secret-authenticator.server.ts:BearerSecretManagementAuthenticator (`authenticate({ authorizationHeader })`) | C6 | adapter (un-exported; built by the factory) | adapters | package-private | operator/platform | present |
| U1030 | management-auth/index.server.ts:module | C6 | framework-glue | frameworks | published (`./management-auth/server`, including ManagementAuthenticator and ManagementAuthenticationResult) | - | altered in a79f507d |
| U1031 | management-auth/management-auth-config.server.ts:createManagementAuthConfig, isSecureManagementTransport, MANAGEMENT_AGENT_PRINCIPAL_ID | C6 | adapter | adapters | published | operator/platform | present |
| U1033 | management-auth/management-authenticator-contract.server.ts:ManagementAuthConfig (+ManagementTransportPolicy) | C6 | boundary-data | adapters | published | operator/platform | present |
| U1040 | management-auth/create-management-authenticator.server.ts:createManagementAuthenticator (takes `ManagementApiConfig`) | C6 | adapter factory | adapters | published | operator/platform | present |
| U1041 | http/http.server.ts:module (HttpJsonError, handleHttpErrorResponse, throwMethodNotAllowedResponse, createBadRequestResponse, readFormDataRequestBody) + http/index.server.ts | C6 | adapter | adapters | published (`./http/server`) | operator/platform | present |
| U1034 | pwa/index.ts:module | C6 | framework-glue | frameworks | published (`./pwa`) | - | present |
| U1035 | pwa/pwa-registration.ts:createPwaRegistration | C6 | adapter | adapters | published | operator/platform | present |
| U1036 | pwa/pwa-surfaces.ts:pwaSurfaceDefinitions | C6 | boundary-data | adapters | published | operator/platform, client | present |
| U1100 | content/legal-document.ts:module (LegalDocument with `effectiveDateLabel`, LegalDocumentBlock, LegalDocumentSection, LegalLink, LegalText, formatEffectiveDate, ELI_COACH_CONTACT_EMAIL) | C4 | boundary-data | entities | published | operator/platform | present |
| U1101 | content/legal-document-hash.ts:legalDocumentSha256 (+canonicalizeLegalDocument) | C4 | utility | adapters | package-private | operator/platform | present |
| U1103 | content/privacy-policy.ts:PRIVACY_POLICY (+_VERSION, EVOA_FITNESS_PRIVACY_EMAIL, WAITLIST_MARKETING_CONSENT(+_VERSION)) | C4 | boundary-data | entities | published | operator/platform, visitor | present |
| U1108 | content/store-marketing-consent.ts:STORE_MARKETING_CONSENT (+_VERSION) | C4 | boundary-data | entities | published | operator/platform, visitor | present |
| U1110 | content/index.ts:module | C4 | framework-glue | frameworks | published | operator/platform | present |
| U1114 | content/website-and-store-terms/{current,index,types}.ts:module (WEBSITE_AND_STORE_TERMS_DOCUMENT, PAID_DIGITAL_DELIVERY_CONSENT, the PDF artifact helpers) | C4 | boundary-data | entities | published through `.` (the `./website-and-store-terms/publication` subpath is removed) | operator/platform | present |
| U1150 | db/database-client.ts:createDatabaseClient | C2 | adapter | adapters | published | operator/platform | present |
| U1151 | db/database-client.ts:DatabaseClient (NodePgDatabase<typeof schema>), DatabaseTransaction | C2 | port (detail-typed) | adapters | published | operator/platform | changed (GEN-191 fix round): publishes `DatabaseTransaction`, the handle a `transaction` callback receives, which C6's reservation writers, the assessment-call repository and the store publication repository name instead of deriving it |
| U1152 | db/database-pool.ts:createManagedDatabasePool | C2 | adapter | frameworks | published | operator/platform | present |
| U1153 | db/index.ts:module | C2 | framework-glue | frameworks | published | operator/platform | changed (GEN-191 fix round): also publishes `DatabaseTransaction` and `isCausedByDatabaseError` |
| U1154 | db/schema/app-schema.ts:appSchema (+schema/index.ts) | C2 | adapter | frameworks | published | operator/platform | present |
| U1296 | db/database-error.ts:isCausedByDatabaseError | C2 | utility (pure) | adapters | published | operator/platform | new (GEN-191 fix round). Walks an error's cause chain once, stopping at a repeated link, and hands each link's `code` and `constraint`, read when they are strings, to the caller's predicate. The one reader of a driver error's fields, shared by the `22P02` classification in `PostgresAssessmentCallRepository` and the `23P01` one in `reserveCoachTime` |
| U1160 | config/concerns/app.ts:appShape, AppConfig, isProductionRuntime | C3 | boundary-data + refinement | frameworks | published (type through `.`) | operator/platform | present |
| U1161 | config/concerns/bot-detection.ts:botDetectionShape, BotDetectionSettings, refineBotDetection, TURNSTILE_TEST_RESPONSE_TOKEN | C3 | boundary-data + refinement | frameworks | published (type through `.`) | operator/platform | present |
| U1162 | config/concerns/clerk.ts:clerkShape, ClerkConfig, refineClerk | C3 | boundary-data + refinement | frameworks | package-private | operator/platform, vendor:clerk | present |
| U1163 | config/concerns/database.ts:databaseShape, DatabaseConfig, databaseBootstrapEnvironmentSchema, DatabaseBootstrapEnvironment, DatabaseUserCredentials, DatabaseConnection | C3 | boundary-data | frameworks | published (types through `.`) | operator/platform | present |
| U1164 | config/concerns/management-api.ts:managementApiShape, ManagementApiConfig, refineManagementApi | C3 | boundary-data + refinement | frameworks | published (type through `.`) | operator/platform | present |
| U1165 | config/concerns/product-email.ts:productEmailShape, ProductEmailConfig, refineProductEmail | C3 | boundary-data + refinement | frameworks | published (type through `.`) | operator/platform, vendor:resend | present |
| U1166 | config/concerns/store-assets.ts:storeAssetsShape, StoreAssetsConfig, refineStoreAssets | C3 | boundary-data + refinement | frameworks | published (type through `.`) | operator/platform | present |
| U1277 | config/concerns/assessment-calls.ts:assessmentCallsShape, AssessmentCallsConfig | C3 | boundary-data | frameworks | published (type through `.`) | operator/platform | new. `ASSESSMENT_CALL_MEETING_LINK` and `ASSESSMENT_CALL_COACH_EMAIL`, both defaulting to opaque `example.invalid` placeholders; no refinement and no production guard (D-A) |
| U1167 | config/concerns/waitlist.ts:waitlistShape, WaitlistConfig (active offer plan and campaign; no mode, no cap) | C3 | boundary-data | frameworks | published (type through `.`) | operator/platform | changed (f5d1889c; `WAITLIST_MODE` removed); changed (b3eb2653; `WAITLIST_CAP` removed, the cap is U911's constant because the U304 check constraint fixes it) |
| U1170 | config/runtime-environment.ts:runtimeEnvironmentSchema, RuntimeEnvironment (the nine concern shapes composed without `WAITLIST_MODE`, five refinements) | C3 | boundary-data | frameworks | package-internal (the type is published through `.`) | operator/platform | gained the `assessment-calls` shape, with no refinement; changed transitively (f5d1889c) |
| U1171 | config/runtime.ts:loadRuntimeEnvironment (the only module that takes a process environment) | C3 | adapter | frameworks | published (`./runtime`) | operator/platform | present |
| U1174 | config/runtime.ts:loadDatabaseBootstrapEnvironment, the database user helpers, buildPostgresConnectionString, hasCompleteDatabaseConfiguration, resolveRuntimeDatabaseConnection | C3 | utility | frameworks | published (`./runtime`) | operator/platform | present |
| U1183 | config/base-path.ts:joinBasePath, buildRedirectPath | C3 | utility | frameworks | published (`.`) | operator/platform | present |
| U1192 | config/index.ts:module | C3 | framework-glue | frameworks | published (`.`) | operator/platform | present |
| U1193 | domain/acquisition/acquisition-incidents.ts:AcquisitionIncidents (+private AcquisitionIncident) | C1 | port + boundary-data | use-cases | published (`./acquisition`; incident shape private) | operator/platform | added in a79f507d |
| U1194 | domain/waitlist/waitlist-incidents.ts:WaitlistIncidents (`confirmationDeliveryFailed()`, `waitlistModeReadFailed()`) | C1 | port | use-cases | published (`./waitlist`) | operator/platform | added in a79f507d; changed (242a0976; adds `waitlistModeReadFailed` for U963) |
| U1195 | bot-detection/verifier/bot-verifier-contract.server.ts:BotVerifier (+BotVerificationRequest, BotVerificationResult) | C6 | port + boundary-data | adapters | published (`./bot-detection/server`) | visitor, operator/platform, vendor:cloudflare-turnstile | added in a79f507d; relocated contract, new unit ID |
| U1196 | email/product-email-contract.server.ts:ProductEmail (+ProductEmailCommand, ProductEmailResult = sent \| rejected \| unconfirmed, EmailAttachment) | C6 | port + boundary-data | adapters | published (`./email/server`) | operator/platform, visitor, vendor:resend | added in a79f507d; relocated contract, new unit ID; changed (main merge into GEN-191): `ProductEmailCommand` carries GEN-191's optional `attachments` list, whose `EmailAttachment.content` is a `Uint8Array` so no encoding crosses the contract (CH-B), and its optional per-send `replyTo` (2acf65b9); `EmailAttachment` and `ProductEmailCommand` are exported through `./email/server` for the assessment-call email adapters |
| U1197 | management-auth/management-authenticator-contract.server.ts:ManagementAuthenticator (+ManagementCredentials, ManagementAuthenticationResult, ManagementPrincipal) | C6 | port + boundary-data | adapters | published (`./management-auth/server`; credentials/principal signature-visible) | operator/platform, coach (future) | added in a79f507d; relocated contract, new unit ID |
| U1191 | packages/test-support/src/index.ts:CLERK_TEST_ENVIRONMENT | C16 | boundary-data | tests | published (`.`, devDependency only) | operator/platform | present |
| U1198 | apps/platform/db/drizzle/0018_restore_waitlist_mode_feature_flag.sql:migration | C15 | framework-glue | frameworks | migration runner | operator/platform | added (fc2fd208; seeds `WAITLIST_MODE=true`) |
| U1199 | apps/platform/e2e/support/database.ts:createE2eDatabasePool | tests | test adapter | tests | E2E-private | operator/platform | added (621cdc7e) |
| U1326 | apps/platform/e2e/support/assessment-calls.ts:clearBookedAssessmentCalls | tests | test adapter | tests | E2E-private | operator/platform | new (coach follow-ups). Deletes every booked call and releases its coach-time reservation before the booking journey runs, through the same pool U1199 builds. The journey always takes the soonest open slot, so calls left by an earlier run would push the one it books off the dashboard's next-three widget |
| U1200 | apps/platform/e2e/support/fixtures.ts:test | tests | testing API | tests | E2E journeys | operator/platform, client, coach | changed (621cdc7e) |
| U1201 | apps/platform/e2e/support/global-setup.ts:globalSetup | tests | test composition | tests | Playwright hook | operator/platform | changed (621cdc7e; disables persisted waitlist mode through U1203, which captures the prior value) |
| U1202 | apps/platform/e2e/support/global-teardown.ts:globalTeardown | tests | test composition | tests | Playwright hook | operator/platform | changed (621cdc7e; restores the captured persisted mode in `finally`) |
| U1203 | apps/platform/e2e/support/waitlist-mode.ts:disableWaitlistMode, restoreWaitlistMode | tests | test adapter | tests | E2E-private | operator/platform | added (621cdc7e); changed (f0eb1bf4; setup records the prior `WAITLIST_MODE` value in the runner environment and teardown writes that value back); changed (c2277ebb; the boolean-parameter update became separately named switch-on and switch-off updates) |
| U1204 | apps/platform/package.json:`files` | C15 | boundary-data | frameworks | package-manager entry | operator/platform | changed (010904d8; production deploy emits only `build`) |
| U1205 | docker/Dockerfile.react-router:post-deploy assertion | enforcement | build enforcement | frameworks | Docker builder | operator/platform | changed (48b8cafb; requires the server build, rejects top-level `src`/`e2e`, recursively rejects workspace test/spec files, and rejects test-support) |
| U1206 | packages/config/package.json:`files` | C3 | boundary-data | frameworks | package-manager entry | operator/platform | changed (48b8cafb; retains `src` except test/spec files) |
| U1207 | packages/content/package.json:`files` | C4 | boundary-data | frameworks | package-manager entry | operator/platform | changed (48b8cafb; retains `src` and `artifacts` except source test/spec files) |
| U1208 | packages/domain/package.json:`files` | C1 | boundary-data | frameworks | package-manager entry | operator/platform | changed (48b8cafb; retains `src` except test/spec files) |
| U1209 | packages/infrastructure/package.json:`files` | C6 | boundary-data | frameworks | package-manager entry | operator/platform | changed (48b8cafb; retains `src` except test/spec files) |
| U1210 | packages/ui/package.json:`files` | C5 | boundary-data | frameworks | package-manager entry | operator/platform | changed (48b8cafb; retains `src` including CSS except test/spec files) |
| U1211 | waitlist/data/signup-constraint-violations.server.ts:rejectsReducedSlot, rejectsDuplicateSignup | C8 | adapter utility | adapters | feature-private | operator/platform | added (b3eb2653; classifies a Postgres rejection by SQLSTATE and constraint name through the query error's `cause` chain; every other error stays a failure) |
| U1212 | apps/platform/db/drizzle/0019_waitlist_reduced_slots.sql:migration | C15 | framework-glue | frameworks | migration runner | operator/platform | added (b3eb2653; drops the `pricing_eligibility` default, adds `reduced_slot`, backfills reduced entries per offer by `row_number()` over `created_at, id`, then adds the unique index and both checks without `NOT VALID`, so more than N reduced entries fail the migration) |
| U1213 | apps/platform/integration-test-config/migration-test-database.ts:MigrationTestDatabase | tests | test adapter | tests | — | — | removed (#232 review; migrations get no tests, so the 0019 backfill test and its rig are deleted) |
| U1214 | apps/platform/integration-test-config/postgres-test-environment.ts:PostgresTestEnvironment.beginTransaction, OpenTransaction | tests | test adapter | tests | integration rig | operator/platform | added (b3eb2653; an uncommitted migration-user transaction a case commits later, rolled back on reset or stop; models a writer that skips the advisory lock) |
