# Components

Header: date 2026-09-19, commit ea81d98f merged with origin/main at e2d86a3a (branch point 843d8261), scope apps/platform/src, apps/platform/db, packages/{config,content,db,domain,infrastructure,test-support,ui}/src plus the enforcement layer (tools/dependency-cruiser.config.cjs, tools/dependency-cruiser.tsconfig.json, tools/boundaries.test.mjs, tools/boundary-fixtures/, tools/domain-layout.mjs, tools/domain-layout.test.mjs, tools/domain-layout-fixtures/, knip.json, eslint.config.mjs, workspace package.json export maps, tsconfigs, vite/react-router/vitest configs), mode record update merging two lines of history on top of change review run 8 at e8690f45: main's partial change review of 8ac6a613 (PR #229 head, squash-merged as 7d92dc22; base 79fa1e95; 46 changed implementation files in C1, C6, C7, C8 and C14 plus direct neighbors), the persisted waitlist-mode change (74950bd7), the Radix mobile navigation (1979b1cc) and the database-enforced waitlist capacity (e2d86a3a); and GEN-191 over `843d8261..ea81d98f` — the slices recorded at 422c7797, corrected after the two review fix rounds (`422c7797..186b77c4`), the remediation round (`186b77c4..6fc3157f`), the booking-card rebuild (`bcf6555e..dde3b960`), the second parity round (`7a61673f..d3513898`), the design-system fix round on top of bff79348, the coach-calendar refactor (`19169b9e..eb88ecb4`, carried onto this branch as 152ca98f and recorded at 47edb229), the fix round (`47edb229..59c019ec`) and the final fix round (`a4256c45..8a604aef`) — and the merge resolution, which moves the assessment-call slice onto main's contracts and database feature flags.

Change review: date 2026-09-18, commits 6c043f97, 63100724, eced8490 and 3836745e (PR #230, base 843d8261), merged with main at 74950bd7; scope the C5 mobile-navigation modules (`layout/navigation-dialog.tsx`, `layout/use-close-mobile-navigation-on-desktop.ts`, `layout/portal-shell.tsx`, the removed `lib/focus-trap.ts`) and their public-site and portal consumers; partial scope.

Change review: date 2026-09-18, commit c2277ebb, baseline 7d92dc22, scope the persisted waitlist-mode change (`79fa1e95..f0eb1bf4`, merged with main in `242a0976`): the changed units and their direct graph neighborhood in apps/platform, packages/{config,content,db,domain,infrastructure,ui}, tests, migrations and package deployment; partial scope. Rows it changed or added carry the commit that changed them.

A component is a group of units behind one published surface that tooling enforces. Workspace packages are enforced by their `package.json` export maps (a deep import does not resolve). The runtime config, content, domain, infrastructure, and UI packages also use package `files` allowlists that retain production source/artifacts and exclude `src/**/*.test.*` and `src/**/*.spec.*` from deployment. App-level folders are enforced by named rules in `tools/dependency-cruiser.config.cjs` and by `knip.json`, proven by `tools/boundaries.test.mjs` and run by `pnpm check:boundaries` inside `pnpm typecheck`, `pnpm build`, every vitest run and the Docker builder stage; a crossing fails the build, not only `pnpm lint`. Nothing is a release unit: every package is `private` and consumed as `workspace:*` by path. Tests are the outermost pseudo-ring, not a component.

Change review: date 2026-09-20, commits `f46b6f41..HEAD` on `eduardvalentin1996/gen-193-coach-sees-her-assessment-calls-on-the-dashboard`; scope the GEN-193 coach assessment-calls slice across C1, C5, C13 and C17; partial scope. It changes the C5, C13 and C17 rows below and adds no component.

| ID | Path | Published surface | Enforcement | Ring | Actors | Release unit |
|---|---|---|---|---|---|---|
| C1 | packages/domain | Eleven subpaths, no root barrel, one folder per entity. `./account` (Account, AccountRole, AccountSnapshot, Accounts, ProvisionAccountUseCase, ProvisionAccountResult, DeleteAccountUseCase); `./waitlist` (GetWaitlistUseCase, JoinWaitlistUseCase, JoinWaitlistResult, WaitlistIncidents, Waitlist, WAITLIST_REDUCED_PRICING_CAP, WaitlistConsentVersions, WaitlistOffer, WaitlistSignupPricing, WaitlistSnapshot, WaitlistConfirmation, SendWaitlistConfirmationCommand, WaitlistConfirmationResult, WaitlistEntries, ReducedPricingSignupResult, RegularPricingSignupResult); `./feature-flag` (FeatureFlag, FeatureFlagSet, WAITLIST_MODE_FEATURE_FLAG (the one flag key both `GetWaitlistUseCase` and `AssessmentCallBookingWindow` read), FeatureFlags, GetFeatureFlagsUseCase, FeatureFlagReader); `./email-address` (EmailAddress); `./product` (Product, PublishedProduct, isStoreCoverMimeType, ProductAsset, PublishedProductCover, StoreTaxonomyValue, ProductAssets, ProductAssetWriter, ProductAssetDigest, ProductAssetContent, ProductAssetOpenResult, StoreCatalog, StoreProductPublications, MAX_PUBLICATION_BYTES, resolvePublicationTarget, ProductPublication, ProductVersionMetadata, PublicationIssue, PublicationPlanResult, PublishingPrincipal, PublishProductResult, StoredPublicationRecord, StoreTaxonomySnapshot, PersistPublicationCommand, ProductCoverInput, ProductDownloadInput, ListPublishedProductsUseCase, FindPublishedProductUseCase, FindPublishedCoverUseCase, PlanNewProductUseCase, PlanProductRevisionUseCase, PublishNewProductUseCase, PublishProductVersionUseCase, RetireProductUseCase); `./acquisition` (AcquireProductsUseCase, AcquireProductsResult, AcquisitionIncidents, evaluateDeliveryLimit, StoreDeliveryLimitWindow, ProductDelivery, ProductDeliveryResult, CreateDownloadTokenResult, PayloadDigestGenerator, StoreAcquisitions, AcquisitionPreparation, PrepareAcquisitionCommand, ResolvedPriorAcquisition); `./download-grant` (DownloadGrant, DownloadGrantItem, DownloadGrants, DownloadTokenHasher, ResolveDownloadGrantUseCase, DownloadGrantResolution); `./cart` (Cart); `./assessment-call` (AssessmentCall, AssessmentCallSnapshot (carries the derived `endsAt`), AssessmentCallBookingWindow (reads `WAITLIST_MODE` through `FeatureFlagReader` on every request: booking is open iff the flag is not true, and a failed read counts as closed and is reported through `bookingModeReadFailed`), AssessmentCallIncidents, AssessmentCallNotifications, AssessmentCallNotificationResult, AssessmentCallReservations, ReservationResult, ReserveAssessmentCallCommand, MeetingRoomLink, BookAssessmentCallUseCase, BookAssessmentCallResult, ListOpenSlotsUseCase, OpenSlotsResult, ResolveJoinLinkUseCase, JoinLinkResult, ASSESSMENT_CALL_RULES — the assessment call's `SlotPolicy`: the duration, buffer, horizon and lead-time literals and the step derived from duration plus buffer, owned here and nowhere else); `./coach-availability` (CoachAvailability, CoachAvailabilityProps, Weekday — it steps in minutes and offers a start only if the call ends inside the window; CoachAvailabilitySource, CoachCalendar, TimeInterval, SlotPolicy (whose `of` rejects an invalid policy); it names no appointment kind); interface-only `./shared` (Clock only). | export map (eleven subpaths, no `.`) + dependency-absence (`package.json` declares no dependencies) + `"types": []` in its tsconfig (no ambient Node globals) + the `domain-slices` and `domain-no-externals` rules + `tools/domain-layout.mjs` (all six checks fixture-covered) | entities and use cases (account, waitlist, feature-flag, email-address, product, acquisition, download-grant, cart, assessment-call, coach-availability) | operator/platform, visitor, client, coach; vendor:clerk and vendor:resend only through ports | no |
| C2 | packages/db | createDatabaseClient, DatabaseClient (Drizzle `NodePgDatabase`), DatabaseTransaction (the handle a `transaction` callback receives), isCausedByDatabaseError (the one reader of a driver error's cause chain, `code` and `constraint`), createManagedDatabasePool, appSchema | exports (`.`) | adapters/frameworks | operator/platform | no |
| C3 | packages/config | Two entries. `.`: the concern types (AppConfig, DatabaseConfig, DatabaseBootstrapEnvironment, DatabaseConnection, WaitlistConfig, BotDetectionSettings, ProductEmailConfig, ManagementApiConfig, AssessmentCallsConfig, RuntimeEnvironment) plus TURNSTILE_TEST_RESPONSE_TOKEN, joinBasePath, buildRedirectPath. The `assessment-calls` concern (`concerns/assessment-calls.ts`) carries `ASSESSMENT_CALL_MEETING_LINK` and `ASSESSMENT_CALL_COACH_EMAIL`, both defaulting to opaque `example.invalid` placeholders, with no refinement: by owner decision D-A neither variable has a production guard, so a runtime that omits either boots on its placeholder. `./runtime`: loadRuntimeEnvironment, loadDatabaseBootstrapEnvironment, the database user/connection helpers, hasCompleteDatabaseConfiguration, resolveRuntimeDatabaseConnection | exports (`.`, `./runtime`) + `config-runtime-readers` | frameworks | operator/platform | no |
| C4 | packages/content | Legal document types (LegalDocument carries a preformatted `effectiveDateLabel`), PRIVACY_POLICY(+_VERSION), WEBSITE_AND_STORE_TERMS_DOCUMENT, WAITLIST_MARKETING_CONSENT(+_VERSION), STORE_MARKETING_CONSENT(+_VERSION), ELI_COACH_CONTACT_EMAIL, ELI_PORTRAIT_PATHS (the app-relative paths of Eli's two portrait sizes, served from `apps/platform/public/media/eli/`), EVOA_FITNESS_PRIVACY_EMAIL | exports (`.`) | entities (boundary-data) | operator/platform | no |
| C5 | packages/ui | No root barrel. Nine concern subpaths plus the stylesheet: `./appointments` (AppointmentCard, DashboardAppointmentRow — the appointment card and the compact dashboard row a portal panel lists, built on `./primitives` and `./lib`, drawing their calendar, clock and mail glyphs inline because C5 declares no icon vendor); `./calendar` (Calendar, CalendarProps — a themed wrapper over `react-day-picker`'s `DayPicker`, single-select, showing outside days in fixed six-week months, with the vendor's `timeZone` prop passed through); `./filters` (FilterChip, FilterChipGroup, FilterChipTone); `./layout` (AppShell, DeadEndPage — the one dead-end page the root error page, the access-denied page and the sign-in-failed page share, whose single action is the caller's `inverted` Button — NavigationDialog, NavigationMenu — the Radix-backed mobile menu the public site and the portals compose, whose optional `menuButtonVariant` picks the menu button's `IconButton` variant (the public site passes `plain`) — PhoneFrame, PortalShell, PortalNavigationLink, SidebarSurfaceLayout); `./lib` (cn, MAIN_CONTENT_ID, useDisplayTimeZone, useSearchParamsWriter, whose return carries both `writeSearchParams` and `replaceSearchParams`); `./motion` (createFadeUpVariants, publicEase, publicEaseOut, publicSectionRevealViewport, useClientReducedMotionPreference); `./overlays` (Sheet, SheetContent, SheetDescription, SheetTitle); `./primitives` (Alert, the one form-level error message; Button and buttonVariants, with `variant`, `size`, `width`, `weight`, `textSize`, `lettering`, `elevation` and `press` axes; Badge, in `accent` and `neutral` tones; Card and cardVariants, whose `portal-panel` variant is the portal surfaces' panel; Checkbox, IconButton, Input, Label, whose `htmlFor` is required, Link, linkVariants, the seven Pagination parts, SectionEyebrow, Textarea; `Avatar` is package-private, read only by the appointment card); `./tabs` (Tabs, TabsList, TabsTrigger, TabsContent — the segmented filter bar over `radix-ui` Tabs); `./styles.css`, which also owns the one focus-visible rule every focusable element shares (no component carries focus classes of its own; the lighter ring applies inside `data-surface="inverted"`) and a base layer that mirrors the prototype's (heading, label, button and input typography, and the default border and outline colours). One scale per axis: the framework's sizes, line heights and `rounded-full` plus the role tokens (`micro`, `caption` and `md` sizes; `checkbox`, `tile`, `field`, `compact`, `control`, `card` and `panel` corners; `card` and `action` shadows); the prototype's `theme.css` mirrors every token. `CalendarChevron` and `CalendarDayButton` are package-private in `calendar/calendar.tsx` | exports (ten entries) + `ui-subpaths`, `ui-primitives-import-only-lib`, `ui-lib-is-the-base` | frameworks | operator/platform (design system); coach/client via PortalShell; visitor via FilterChipGroup, public navigation and Calendar; vendor:react-day-picker (9.14.0, the package's only dated dependency — it converts `today`, `month`, `selected`, `disabled` and `modifiers` into the display zone itself, so C5 declares neither `date-fns` nor `@date-fns/tz`) | no |
| C6 | packages/infrastructure | No root barrel. Eight subpaths: `./pwa` (pwaSurfaceDefinitions, createPwaRegistration); `./bot-detection` (BotDetectionWidget, BotDetectionConfig, useBotDetectionSubmission and Turnstile constants — `TURNSTILE_RESPONSE_FIELD` and one action per protected form: `WAITLIST_TURNSTILE_ACTION`, `STORE_ACQUISITION_TURNSTILE_ACTION`, `ASSESSMENT_CALL_BOOKING_TURNSTILE_ACTION`); `./bot-detection/server` (BotVerifier, createBotDetectionConfig, createBotVerifier, resolveRequestRemoteIp); `./email/server` (ProductEmail, ProductEmailCommand, which carries optional `attachments` and an optional per-send `replyTo`, EmailAttachment, createProductEmail, InMemoryProductEmail and the Email* React primitives; `ResendProductEmail` maps the contract's `EmailAttachment.content` `Uint8Array` to a Resend `Attachment` with `Buffer.from`, the one place base64/Buffer encoding happens, and sends the command's `replyTo` when present, otherwise the configured `PRODUCT_EMAIL_REPLY_TO`); `./feature-flags/server` (PostgresFeatureFlagRepository); `./coach-calendar/server` (PostgresCoachCalendar, reserveCoachTime, releaseCoachTime over the `coach_time_reservations` table, whose no-overlap exclusion constraint lives in migration SQL and whose unique `(appointment_kind, appointment_id)` index keeps one reservation per appointment); `./management-auth/server` (ManagementAuthenticator, ManagementAuthenticationResult, ManagementAuthConfig, createManagementAuthenticator, createManagementAuthConfig, isSecureManagementTransport); `./http/server` (HttpJsonError, handleHttpErrorResponse, throwMethodNotAllowedResponse, createBadRequestResponse, readFormDataRequestBody). The concrete Turnstile, static-token, Resend and bearer-secret implementations remain private behind factories. | exports (eight subpaths) + `infrastructure-subpaths`, `infrastructure-browser-entries` + knip published-surface checks | adapters | operator/platform, visitor, client (pwa); vendor:cloudflare-turnstile, vendor:resend | no |
| C7 | apps/platform/src/features/store | `contracts/store.ts`, `contracts/store-management.ts`, `contracts/paths.ts` (STORE_ROUTE_SEGMENT, STORE_PATH, STORE_API_PATHS); `routes.ts` (storePublicRoutes, storeApiRoutes); `server/guards/store-context.server.ts` (storeContext); routed modules (`api/*.ts` eight resource routes; `ui/public/{catalog,product,download}-page.tsx`); `ui/public/{cart-drawer,cart-provider}.tsx` consumed by the public-site shell; `data/schema.server.ts` discovered by drizzle-kit glob. Controllers, repositories and email adapters are imported only by `server/store-composition.server.ts`; `ui/public/catalog-presenter.ts` is feature-private | `feature-internals`, `feature-schema-foreign-key`, `feature-server-private`, `feature-api-to-data`, `features-never-reach-server`, `guards-construct-nothing`, `browser-half`, `route-thinness`, `route-thinness-domain`, `surface-<s>-to-feature`, `surface-import` | adapters (ui/ is frameworks) | visitor, operator/platform (management API); vendor:resend, vendor:cloudflare-turnstile | no |
| C8 | apps/platform/src/features/waitlist | `contracts/waitlist.ts`, `contracts/paths.ts`; `routes.ts` (waitlistApiRoutes); `server/guards/waitlist-context.server.ts` (waitlistContext); `ui/shared/waitlist-presentation.ts` (presentWaitlist, WaitlistPresentation); `api/waitlist.ts` (route); `ui/public/email-form.tsx`, `ui/public/availability-status.tsx`; `data/schema.server.ts` (glob) | same rule set as C7 | adapters | visitor, operator/platform; vendor:resend | no |
| C9 | apps/platform/src/features/accounts | `contracts/account.ts`, `contracts/paths.ts` (SIGN_IN_FAILED_PATH, CLIENT_PORTAL_ROUTE_SEGMENT, COACH_PORTAL_ROUTE_SEGMENT and the portal recovery paths); `routes.ts` (accountsDeadEndRoutes, accountsApiRoutes); `server/guards/{accounts-context,session-context,require-account,require-portal-access}.server.ts`; `server/account-resolution-middleware.server.ts` (root.server.ts); `ui/public/auth-nav-actions.tsx` (C11); `ui/shared/access-denied-page.tsx` (C15 root.tsx); routes `api/account.ts`, `api/clerk-webhooks.ts`, `ui/public/sign-in-failed-page.tsx` (registered by the public site outside its layout, as a dead-end page) | same rule set as C7 | adapters | client, coach, visitor, operator/platform; vendor:clerk | no |
| C11 | apps/platform/src/surfaces/public-site | Route modules only: `routes.ts` (publicSiteRoutes), `paths.ts` (PRICING_PATH), `shell/layout.tsx` (+ `layout.server.ts` loader, shouldRevalidate), pages home, pricing, blog, privacy, terms. `routes.ts` registers the accounts feature's sign-in-failed route after the layout route, outside the public shell; `pages/pricing.tsx`, like the booking page, asks the layout for a full-bleed content frame through its route handle | `surface-to-surface`, `surface-import`, `surface-public-site-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | visitor, operator/platform (waiting-list mode, marketing and legal copy) | no |
| C12 | apps/platform/src/surfaces/client-portal | Route modules only: `routes.ts` (clientPortalRoutes), `shell/layout.tsx` (middleware), `pages/home.tsx`, `api/{manifest,sw,readyz}.ts` | `surface-to-surface`, `surface-import`, `surface-client-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | client, operator/platform | no |
| C13 | apps/platform/src/surfaces/coach-portal | Route modules only: `routes.ts` (coachPortalRoutes, which spreads C17's `assessmentCallsCoachRoutes` inside the shell), `shell/layout.tsx` (middleware), `pages/home.tsx` (loader and view: the dashboard reads C17's coach listing and composes its greeting and upcoming-calls widget, the recorded page-home exception), `api/readyz.ts` | `surface-to-surface`, `surface-import`, `surface-coach-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | coach, operator/platform | no |
| C14 | apps/platform/src/server | `container.server.ts` (getPlatformContainer, createPlatformContainer, PlatformContainer; constructs one Postgres feature-flag repository and reader shared by platform, waitlist and assessment calls), `feature-contexts.server.ts`, `platform-composition.server.ts` (accepts the shared reader), `database.server.ts`, `runtime-environment.server.ts`, `logger.server.ts` (createConsoleLogger, one console adapter implementing `AcquisitionIncidents`, `WaitlistIncidents` and `AssessmentCallIncidents`), guards and platform API routes/controllers. | `composition-root`, `server-guards-consumers`, `root-registry-to-server`, `config-runtime-readers` | composition (container, feature contexts) with adapters (controllers and console logger) and frameworks (routes) | operator/platform | no |
| C15 | apps/platform app root: `src/root.tsx`, `src/root.server.ts`, `src/root-error-page.tsx`, `src/routes.ts`, `src/app.css`, `src/types/`, `db/drizzle.config.ts`, `vite.config.ts`, `react-router.config.ts`, `package.json` | Root route module (Root, Layout, ErrorBoundary, meta, links, middleware, loader); the route registry, a concatenation of eight fragments with no path literals; build and migration configuration; package deployment allowlist containing only `build` | `root-registry`, `root-registry-to-server`, `composition-root`, npm/pnpm `files` semantics and the Docker post-deploy assertion | frameworks (root.server.ts is a second composition point) | operator/platform, visitor, client, coach | no |
| C16 | packages/test-support | `.`: CLERK_TEST_ENVIRONMENT | export map + `no-production-import-of-tests` and `not-to-dev-dep` (declared only as a devDependency of `packages/config` and `apps/platform`, so a production import fails both the rule and, after `pnpm --prod deploy`, resolution) | frameworks (test fixture) | none in production | no |
| C17 | apps/platform/src/features/assessment-calls | `contracts/assessment-calls.ts` (the wire schemas and their inferred types), `contracts/paths.ts` (BOOK_ROUTE_SEGMENT, BOOK_PATH, ASSESSMENT_CALL_API_PATHS, assessmentCallJoinPath, COACH_ASSESSMENT_CALLS_ROUTE_SEGMENT, COACH_ASSESSMENT_CALLS_PATH, the last two read by the coach surface's navigation links), `contracts/call-moment.ts` (formatCallMoment, read by the email builders and built on formatDayFirstDate and formatClockTime; formatDayFirstDate, also read by the booking calendar for its days' accessible names; formatClockTime, formatMonthFirstDate, formatMonthFirstDay and nameTimeZone, read by the booking page; formatShortDay and formatClockTime, read by the coach's two surfaces; no other component reads any); `routes.ts` (assessmentCallsPublicRoutes, assessmentCallsCoachRoutes, assessmentCallsApiRoutes); `server/guards/assessment-calls-context.server.ts` (assessmentCallsContext); routed modules (`api/slots.ts`, `api/bookings.ts`; `ui/public/book/book-page.tsx` at `/book`, `ui/public/join/join-page.tsx` at `/book/:bookingId/join`, both registered inside the public-site shell; `ui/coach/assessment-calls/assessment-calls-page.tsx` at `/coach/assessment-calls`, registered inside the coach-portal shell and so behind its access middleware; the join route renders no page of its own and answers an unknown id with the root's standard 404); `data/schema.server.ts` discovered by the drizzle-kit glob. The controller, the three `data/` adapters (`PostgresAssessmentCallRepository`, `StaticCoachAvailability`, `ConfiguredMeetingRoomLink`) and the notification factory `email/create-assessment-call-notifications.server.ts` are imported only by `server/assessment-calls-composition.server.ts`, which reaches `email/` through that factory alone and takes the container's shared feature-flag reader and incidents handle for the domain's `AssessmentCallBookingWindow`. The factory imports the adapter `EmailAssessmentCallNotifications`, and the adapter imports the calendar-invite builder and the two content builders; `data/schema.server.ts` is imported by the repository and discovered by drizzle-kit. The repository reserves the coach's time through C6 `./coach-calendar/server`, and the composition builds C6's `PostgresCoachCalendar` for the slot list. Every module under `ui/public/book/` other than the page is feature-private, and so is every module under `ui/coach/` other than the page — except that the coach surface composes `ui/coach/dashboard/coach-greeting.tsx`, `ui/coach/dashboard/upcoming-calls-widget.tsx`, `ui/coach/assessment-call-listing.ts` and `ui/coach/use-coach-clock.ts` on its own dashboard under the recorded page-home exception. `ui/shared/day-key.ts` is the one module both actor halves read. The public site imports no `ui/` of its own: it takes only `BOOK_PATH` from `contracts/paths.ts` and the route fragment from `routes.ts`, and at runtime reads the booking page's `handle.publicContentFrame` through `useMatches`, which crosses no import | same rule set as C7 | adapters (ui/ is frameworks) | visitor, operator/platform; vendor:cloudflare-turnstile, vendor:resend | no |

Not components: `tools/dependency-cruiser.config.cjs`, `tools/boundaries.test.mjs`, `tools/boundary-fixtures/`, `tools/domain-layout.mjs`, `tools/domain-layout.test.mjs`, `tools/domain-layout-fixtures/`, `knip.json` and the Docker build are the enforcement layer; the React Router Docker builder asserts the deployed package contains the built server, excludes top-level `src` and `e2e`, recursively excludes test/spec files from runtime workspace dependencies, and omits `@eli-coach-platform/test-support`. Test files are the tests pseudo-ring; `designs/react-reference-app` is a separate deployable outside scope.
