# Components

Header: date 2026-09-18, commit f4f3dc16, scope apps/platform/src, apps/platform/db, packages/{config,content,db,domain,infrastructure,test-support,ui}/src plus the enforcement layer (tools/dependency-cruiser.config.cjs, tools/dependency-cruiser.tsconfig.json, tools/boundaries.test.mjs, tools/boundary-fixtures/, tools/domain-layout.mjs, tools/domain-layout.test.mjs, tools/domain-layout-fixtures/, knip.json, eslint.config.mjs, workspace package.json export maps, tsconfigs, vite/react-router/vitest configs), mode record update for GEN-191 over `843d8261..f4f3dc16` — the slices recorded at 422c7797, corrected after the review fix round `422c7797..f4f3dc16` — on top of change review run 8 at e8690f45.

A component is a group of units behind one published surface that tooling enforces. Workspace packages are enforced by their `package.json` export maps (a deep import does not resolve). App-level folders are enforced by named rules in `tools/dependency-cruiser.config.cjs` and by `knip.json`, proven by `tools/boundaries.test.mjs` and run by `pnpm check:boundaries` inside `pnpm typecheck`, `pnpm build`, every vitest run and the Docker builder stage; a crossing fails the build, not only `pnpm lint`. Nothing is a release unit: every package is `private` and consumed as `workspace:*` by path. Tests are the outermost pseudo-ring, not a component.

| ID | Path | Published surface | Enforcement | Ring | Actors | Release unit |
|---|---|---|---|---|---|---|
| C1 | packages/domain | Eleven subpaths, no root barrel, one folder per entity. `./account` (Account, AccountRole, AccountSnapshot, Accounts, ProvisionAccountUseCase (+ProvisionAccountResult), DeleteAccountUseCase); `./waitlist` (GetWaitlistUseCase, JoinWaitlistUseCase (+JoinWaitlistResult), Waitlist, WaitlistConsentVersions, WaitlistOffer, WaitlistOfferPlan, WaitlistSignupPricing, WaitlistSnapshot, WaitlistConfirmation (+SendWaitlistConfirmationCommand, WaitlistConfirmationResult), WaitlistEntries (+ReducedPricingSignupResult, RegularPricingSignupResult)); `./feature-flag` (FeatureFlag, FeatureFlags, GetFeatureFlagsUseCase (+FeatureFlagReader)); `./email-address` (EmailAddress); `./product` (Product, PublishedProduct, isStoreCoverMimeType, ProductAsset, PublishedProductCover, StoreTaxonomyValue, ProductAssets, ProductAssetWriter, ProductAssetDigest, ProductAssetContent, ProductAssetOpenResult, StoreCatalog, StoreProductPublications, MAX_PUBLICATION_BYTES, resolvePublicationTarget, ProductPublication, ProductVersionMetadata, PublicationIssue, PublicationPlanResult, PublishingPrincipal, PublishProductResult, StoredPublicationRecord, StoreTaxonomySnapshot, PersistPublicationCommand, ProductCoverInput, ProductDownloadInput, ListPublishedProductsUseCase, FindPublishedProductUseCase, FindPublishedCoverUseCase, PlanNewProductUseCase, PlanProductRevisionUseCase, PublishNewProductUseCase, PublishProductVersionUseCase, RetireProductUseCase); `./acquisition` (AcquireProductsUseCase (+AcquireProductsResult), evaluateDeliveryLimit, StoreDeliveryLimitWindow, ProductDelivery, ProductDeliveryResult, CreateDownloadTokenResult, PayloadDigestGenerator, StoreAcquisitions, AcquisitionPreparation, PrepareAcquisitionCommand, ResolvedPriorAcquisition); `./download-grant` (DownloadGrant, DownloadGrantItem, DownloadGrants, DownloadTokenHasher, ResolveDownloadGrantUseCase (+DownloadGrantResolution)); `./cart` (Cart); `./assessment-call` (AssessmentCall, AssessmentCallSnapshot (carries the derived `endsAt`), AssessmentCallNotifications (+AssessmentCallNotificationResult), AssessmentCallReservations (+ReservationResult, ReserveAssessmentCallCommand), MeetingRoomLink, BookAssessmentCallUseCase (+BookAssessmentCallResult), ListOpenSlotsUseCase (+OpenSlotsResult), ResolveJoinLinkUseCase (+JoinLinkResult)); `./coach-availability` (CoachAvailability (+CoachAvailabilityProps, Weekday), CoachAvailabilitySource, ASSESSMENT_CALL_RULES — the duration, buffer, horizon and lead-time literals and the step derived from duration plus buffer, owned here and nowhere else); `./shared`, interface-only (`export type` only: Clock, Logger, BotVerifier, ProductEmail (+EmailAttachment), ManagementAuthenticator and their boundary data) | export map (eleven subpaths, no `.`) + dependency-absence (`package.json` declares no dependencies) + `"types": []` in its tsconfig (no ambient Node globals) + the `domain-slices` and `domain-no-externals` rules + `tools/domain-layout.mjs` (every folder but `shared` has `index.ts` and `<folder>.ts`; no `*-service.ts` file or `*Service` export; every `*-use-case.ts` exports exactly one `*UseCase` class with an `execute` method) | entities and use cases (account, waitlist, feature-flag, email-address, product, acquisition, download-grant, cart, assessment-call, coach-availability) | operator/platform, visitor, client, coach; vendor:clerk and vendor:resend only through ports | no |
| C2 | packages/db | createDatabaseClient, DatabaseClient (Drizzle `NodePgDatabase`), createManagedDatabasePool, appSchema | exports (`.`) | adapters/frameworks | operator/platform | no |
| C3 | packages/config | Two entries. `.`: the concern types (AppConfig, DatabaseConfig, DatabaseBootstrapEnvironment, DatabaseConnection, WaitlistConfig, BotDetectionSettings, ProductEmailConfig, ManagementApiConfig, AssessmentCallsConfig, RuntimeEnvironment) plus TURNSTILE_TEST_RESPONSE_TOKEN, joinBasePath, buildRedirectPath. The `assessment-calls` concern (`concerns/assessment-calls.ts`) carries `ASSESSMENT_CALL_MEETING_LINK` and `ASSESSMENT_CALL_COACH_EMAIL`, both defaulting to opaque `example.invalid` placeholders, with no refinement: by owner decision D-A neither variable has a production guard, so a runtime that omits either boots on its placeholder. `./runtime`: loadRuntimeEnvironment, loadDatabaseBootstrapEnvironment, the database user/connection helpers, hasCompleteDatabaseConfiguration, resolveRuntimeDatabaseConnection | exports (`.`, `./runtime`) + `config-runtime-readers` | frameworks | operator/platform | no |
| C4 | packages/content | Legal document types (LegalDocument carries a preformatted `effectiveDateLabel`), PRIVACY_POLICY(+_VERSION), WEBSITE_AND_STORE_TERMS_DOCUMENT, WAITLIST_MARKETING_CONSENT(+_VERSION), STORE_MARKETING_CONSENT(+_VERSION), ELI_COACH_CONTACT_EMAIL, EVOA_FITNESS_PRIVACY_EMAIL | exports (`.`) | entities (boundary-data) | operator/platform | no |
| C5 | packages/ui | No root barrel. Seven concern subpaths plus the stylesheet: `./calendar` (Calendar, CalendarProps — a themed wrapper over `react-day-picker`'s `DayPicker`, single-select, with the vendor's `timeZone` prop passed through); `./filters` (FilterChip, FilterChipGroup, FilterChipTone); `./layout` (AppShell, PhoneFrame, PortalShell, PortalNavigationLink, SidebarSurfaceLayout); `./lib` (cn, MAIN_CONTENT_ID, useSearchParamsWriter); `./motion` (createFadeUpVariants, publicEase, publicEaseOut, publicViewportOnce, useClientReducedMotionPreference); `./overlays` (Sheet, SheetContent, SheetDescription, SheetTitle); `./primitives` (Button, buttonVariants, Card, Checkbox, IconButton, Input, inputClasses, Link, linkVariants — whose `placement` axis, `prose` or `standalone`, sizes a link that stands on its own line as a touch target — SectionEyebrow, and Textarea, which reuses `inputClasses`); `./styles.css`. `resolveFocusTrapTarget` is package-private in `lib/focus-trap.ts`, `MonthNavButton` in `calendar/calendar.tsx` | exports (eight entries) + `ui-subpaths`, `ui-primitives-import-only-lib`, `ui-lib-is-the-base` | frameworks | operator/platform (design system); coach/client via PortalShell; visitor via FilterChipGroup and Calendar; vendor:react-day-picker (9.14.0, the package's only dated dependency — it converts `today`, `month`, `selected`, `disabled` and `modifiers` into the display zone itself, so C5 declares neither `date-fns` nor `@date-fns/tz`) | no |
| C6 | packages/infrastructure | No root barrel. Seven subpaths: `./pwa` (pwaSurfaceDefinitions, createPwaRegistration); `./bot-detection` (browser: BotDetectionWidget, BotDetectionConfig, useBotDetectionSubmission, Turnstile constants — `TURNSTILE_RESPONSE_FIELD` and one action per protected form: `WAITLIST_TURNSTILE_ACTION`, `STORE_ACQUISITION_TURNSTILE_ACTION`, `ASSESSMENT_CALL_BOOKING_TURNSTILE_ACTION`); `./bot-detection/server` (createBotDetectionConfig, createBotVerifier, resolveRequestRemoteIp); `./email/server` (createProductEmail, InMemoryProductEmail, the Email* React primitives; `ResendProductEmail` maps the port's `EmailAttachment.content` `Uint8Array` to a Resend `Attachment` with `Buffer.from`, the one place base64/Buffer encoding happens); `./feature-flags/server` (PostgresFeatureFlagRepository); `./management-auth/server` (createManagementAuthenticator, createManagementAuthConfig, isSecureManagementTransport, ManagementAuthConfig); `./http/server` (HttpJsonError, handleHttpErrorResponse, throwMethodNotAllowedResponse, createBadRequestResponse, readFormDataRequestBody). TurnstileBotVerifier, StaticTokenBotVerifier and BearerSecretManagementAuthenticator are un-exported behind their factories. C6 owns no port | exports (seven subpaths) + `infrastructure-subpaths`, `infrastructure-browser-entries` | adapters | operator/platform, visitor, client (pwa); vendor:cloudflare-turnstile, vendor:resend | no |
| C7 | apps/platform/src/features/store | `contracts/store.ts`, `contracts/store-management.ts`, `contracts/paths.ts` (STORE_ROUTE_SEGMENT, STORE_PATH, STORE_API_PATHS); `routes.ts` (storePublicRoutes, storeApiRoutes); `server/guards/store-context.server.ts` (storeContext); routed modules (`api/*.ts` eight resource routes; `ui/public/{catalog,product,download}-page.tsx`); `ui/public/{cart-drawer,cart-provider}.tsx` consumed by the public-site shell; `data/schema.server.ts` discovered by drizzle-kit glob. Controllers, repositories and email adapters are imported only by `server/store-composition.server.ts`; `ui/public/catalog-presenter.ts` is feature-private | `feature-internals`, `feature-schema-foreign-key`, `feature-server-private`, `feature-api-to-data`, `features-never-reach-server`, `guards-construct-nothing`, `browser-half`, `route-thinness`, `route-thinness-domain`, `surface-<s>-to-feature`, `surface-import` | adapters (ui/ is frameworks) | visitor, operator/platform (management API); vendor:resend, vendor:cloudflare-turnstile | no |
| C8 | apps/platform/src/features/waitlist | `contracts/waitlist.ts`, `contracts/paths.ts`; `routes.ts` (waitlistApiRoutes); `server/guards/waitlist-context.server.ts` (waitlistContext); `ui/shared/waitlist-presentation.ts` (presentWaitlist, WaitlistPresentation); `api/waitlist.ts` (route); `ui/public/email-form.tsx`, `ui/public/availability-status.tsx`; `data/schema.server.ts` (glob) | same rule set as C7 | adapters | visitor, operator/platform; vendor:resend | no |
| C9 | apps/platform/src/features/accounts | `contracts/account.ts`, `contracts/paths.ts` (SIGN_IN_FAILED_PATH, CLIENT_PORTAL_ROUTE_SEGMENT, COACH_PORTAL_ROUTE_SEGMENT and the portal recovery paths); `routes.ts` (accountsPublicRoutes, accountsApiRoutes); `server/guards/{accounts-context,session-context,require-account,require-portal-access}.server.ts`; `server/account-resolution-middleware.server.ts` (root.server.ts); `ui/public/auth-nav-actions.tsx` (C11); `ui/shared/access-denied-page.tsx` (C15 root.tsx); routes `api/account.ts`, `api/clerk-webhooks.ts`, `ui/public/sign-in-failed-page.tsx` | same rule set as C7 | adapters | client, coach, visitor, operator/platform; vendor:clerk | no |
| C11 | apps/platform/src/surfaces/public-site | Route modules only: `routes.ts` (publicSiteRoutes), `paths.ts` (PRICING_PATH), `shell/layout.tsx` (+ `layout.server.ts` loader, shouldRevalidate), pages home, pricing, blog, privacy, terms | `surface-to-surface`, `surface-import`, `surface-public-site-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | visitor, operator/platform (waiting-list mode, marketing and legal copy) | no |
| C12 | apps/platform/src/surfaces/client-portal | Route modules only: `routes.ts` (clientPortalRoutes), `shell/layout.tsx` (middleware), `pages/home.tsx`, `api/{manifest,sw,readyz}.ts` | `surface-to-surface`, `surface-import`, `surface-client-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | client, operator/platform | no |
| C13 | apps/platform/src/surfaces/coach-portal | Route modules only: `routes.ts` (coachPortalRoutes), `shell/layout.tsx` (middleware), `pages/home.tsx`, `api/readyz.ts` | `surface-to-surface`, `surface-import`, `surface-coach-portal-to-feature`, `route-thinness`, `route-thinness-domain`, `server-guards-consumers` | frameworks | coach, operator/platform | no |
| C14 | apps/platform/src/server | `container.server.ts` (getPlatformContainer, createPlatformContainer, PlatformContainer), `feature-contexts.server.ts` (createFeatureContextMiddleware), `platform-composition.server.ts` (composePlatformFeature, PlatformFeature), `database.server.ts`, `runtime-environment.server.ts`, `logger.server.ts` (createConsoleLogger), `guards/platform-context.server.ts` (platformContext), `guards/runtime-config-context.server.ts` (runtimeConfigContext), `api/` (readyz, meta and feature-flags routes with their controllers and the relocated feature-flag controller and contract) and `api/routes.ts` (platformApiRoutes) | `composition-root`, `server-guards-consumers`, `root-registry-to-server`, `config-runtime-readers` | composition (container, feature contexts) with adapters (controllers) and frameworks (routes) | operator/platform | no |
| C15 | apps/platform app root: `src/root.tsx`, `src/root.server.ts`, `src/root-error-page.tsx`, `src/routes.ts`, `src/app.css`, `src/types/`, `db/drizzle.config.ts`, `vite.config.ts`, `react-router.config.ts` | Root route module (Root, Layout, ErrorBoundary, meta, links, middleware, loader); the route registry, a concatenation of seven fragments with no path literals; build and migration configuration | `root-registry`, `root-registry-to-server`, `composition-root` | frameworks (root.server.ts is a second composition point) | operator/platform, visitor, client, coach | no |
| C16 | packages/test-support | `.`: CLERK_TEST_ENVIRONMENT | export map + `no-production-import-of-tests` and `not-to-dev-dep` (declared only as a devDependency of `packages/config` and `apps/platform`, so a production import fails both the rule and, after `pnpm --prod deploy`, resolution) | frameworks (test fixture) | none in production | no |
| C17 | apps/platform/src/features/assessment-calls | `contracts/assessment-calls.ts` (the wire schemas and their inferred types), `contracts/paths.ts` (BOOK_ROUTE_SEGMENT, BOOK_PATH, ASSESSMENT_CALL_API_PATHS, assessmentCallJoinPath), `contracts/call-moment.ts` (formatCallMoment, formatCallDay, formatCallTime, describeTimeZone — the one wording of a call's date, time and zone, shared by the email builders and the booking UI and read by no other component); `routes.ts` (assessmentCallsPublicRoutes, assessmentCallsApiRoutes); `server/guards/assessment-calls-context.server.ts` (assessmentCallsContext); routed modules (`api/slots.ts`, `api/bookings.ts`; `ui/public/book/book-page.tsx` at `/book`, `ui/public/join/join-page.tsx` at `/book/:bookingId/join`, both registered inside the public-site shell); `data/schema.server.ts` discovered by the drizzle-kit glob. The controller, the three `data/` adapters (`PostgresAssessmentCallRepository`, `StaticCoachAvailability`, `ConfiguredMeetingRoomLink`) and the notification factory `email/create-assessment-call-notifications.server.ts` are imported only by `server/assessment-calls-composition.server.ts`, which reaches `email/` through that factory alone. The factory imports the adapter `EmailAssessmentCallNotifications`, and the adapter imports the calendar-invite builder and the two content builders; `data/schema.server.ts` is imported by the repository and discovered by drizzle-kit. Every module under `ui/public/book/` other than the page is feature-private. No surface imports its `ui/`: the public site takes only `BOOK_PATH` from `contracts/paths.ts` and the route fragment from `routes.ts` | same rule set as C7 | adapters (ui/ is frameworks) | visitor, operator/platform; vendor:cloudflare-turnstile, vendor:resend | no |

Not components: `tools/dependency-cruiser.config.cjs`, `tools/boundaries.test.mjs`, `tools/boundary-fixtures/`, `tools/domain-layout.mjs`, `tools/domain-layout.test.mjs`, `tools/domain-layout-fixtures/` and `knip.json` are the enforcement layer; test files are the tests pseudo-ring; `designs/react-reference-app` is a separate deployable outside scope.
